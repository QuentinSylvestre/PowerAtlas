# Dashboard / ACP Feature Parity

> **Date**: 2026-09-21
> **Status**: Complete — Phases 1-6, Step 9 holistic review + fixes, and Step 9b exhaustive live QA all done. One honest gap remains (SC8 crew-panel live round-trip — see Follow-up Work).
> **Last Updated**: 2026-09-22
> **Scope**: Port all of `/acp`'s features missing from the main dashboard onto the dashboard, except the mobile drill-down view toggle.
> **Estimated effort**: ~2-3 weeks

---

## Completion Summary

Closed via `/qclose` on 2026-09-22. All 6 phases, Step 9's holistic review + fixes, Step 9b's exhaustive live QA, and one post-closeout round (acp.html parity fix + SC8 retry) are complete. `qvalidate` passes clean (5/5 checks). Both test suites re-verified independently by the orchestrator at every stage, not merely trusted from sub-agent reports.

### Acknowledged at archival

- **Second `acp.html` `onclose` gap** (missing `closeSubagentView()`/sub-agent-WS teardown on a main-socket drop, found while fixing the crew-timer bug) — Accepted. Documented in Follow-up Work; fixing it would change UX (hiding the sub-agent panel on every transient reconnect blip) with no request to do so.
- **Fix 2's narrow, sub-millisecond duplicate-send-detection timing edge** (found during Step 9b prep, never routed through a formal review cycle) — Accepted. Worst case is cosmetic, not a duplicate send; documented in Follow-up Work.
- **SC8 (crew panel) real end-to-end live round-trip** — Accepted (already effectively resolved by the post-closeout retry: two genuinely different, well-informed attempts confirmed kiro-cli's real delegation tool was invoked both times, narrowing the gap to one specific `acp.py` wire condition rather than leaving it a broad unknown). No further action this session.
- **Harness improvement: cycle-cap ambiguity** ("1 qreview cycle per phase" doesn't specify whether it also skips the lighter self-verification substitute) — Accepted (harness opportunity).
- **Harness improvement: cross-phase-scoped finding routing** (a review finding whose fix needs a file outside the just-completed phase's scope has no named `/qdev` routing mechanism) — Accepted (harness opportunity).
- **Harness improvement: Progress Tracker/Status-line false-positive flagging during Step 5 review** — Promoted: added a line to `/qdev`'s Step 5 telling reviewers not to flag this, since the update is structurally Step 7's job and always runs after Step 5 for the same phase. Landed in the config repo (`374828b`, pushed).
- **Harness improvement: `exit_criteria_ticked` self-report miscount propagating unverified into review briefs** — Promoted: added a requirement that the orchestrator independently count ticked boxes from the plan file before dispatching `--expect-ticked N`. Landed in the config repo (`374828b`, pushed).
- **Harness improvement: rate-limit mid-phase resume guidance** — Accepted (harness opportunity).
- **Harness improvement: browser-tool-unavailable outcome for Live QA** — Accepted (harness opportunity); root cause was found and resolved within this plan (Chrome wasn't running).
- **Harness improvement: mutation-testing parallel-dispatch process violation** — Accepted (harness opportunity); not a skill gap (the rule already existed in `/qreview`), a discipline note for future sessions.
- **Harness improvement: `/qqa`'s browser-verification gate names Playwright specifically, this environment uses `mcp__claude-in-chrome__*`** — Accepted (harness opportunity); bookkeeping gap, not a quality one.

## Intent

### Problem statement & desired outcomes

The dashboard (`index.html`) carries a deliberately scoped-down "live-attach" chat panel — a prior effort (`dashboard-acp-merge`, ~20 commits, no formal plan file) ported transcript viewing and basic prompting, explicitly *not* full parity with `/acp` (see `index.html:280-286`, `:690-700`). Confirmed by direct comparison: the dashboard is missing 9 features `/acp` has — slash-command/skill autocomplete, Queue/Steer send-mode, Stop/cancel, image paste-to-attach, context-window usage indicator, a debug transport log, WebSocket reconnect-on-drop, a workspace/session-id tap-to-copy widget, and the sub-agent/crew read-only panel. (A 10th, the rail free-text filter, was believed missing but is not — see Success criteria.)

Desired outcome: the dashboard becomes functionally equivalent to `/acp` for every in-scope feature, while `/acp` itself keeps full functionality throughout — it remains the sole mobile/remote-reachable surface (the mobile drill-down toggle stays `/acp`-only), and where a feature's logic moves into a shared module, `/acp` is refactored to consume that same module rather than losing or duplicating the feature.

### Success criteria

- **SC1 — Slash-command/skill autocomplete palette**: works identically on the dashboard and on `/acp`, both consuming a new shared module. Known-dead paths in the current `/acp` implementation (`commands_options` is never sent; `_cmdOptionsTimer`/`applyCommandOptions()` are unreachable; `.acp-cmd-placeholder` is styled but never created) are not ported.
- **SC2 — Context-window usage indicator**: works identically on both pages via the shared module.
- **SC3 — Workspace/session-id tap-to-copy widget**: works identically on both pages via the shared module, and copies the **full workspace path** (not the short display name) on both pages — corrects a pre-existing `/acp` bug where the comment at `acp.html:1321-1323` claims full-path copy but the code copies `shortName(cwd)`.
- **SC4 — Debug/transport log panel**: works identically on both pages via the shared module.
- **SC5 — Queue/Steer send-mode toggle + Stop/cancel button**: added to the dashboard as `dash`-prefixed additions (these two are inseparable — `refreshComposerControls()` governs both from the same turn-state).
- **SC6 — Image paste-to-attach** (with the compression ladder): added to the dashboard, integrated into `dashSendPrompt()`.
- **SC7 — WebSocket reconnect-on-drop**: added to the dashboard. Built last — its restore-on-reconnect logic must be scoped to whatever dashboard state exists once SC1-6 and SC8 land.
- **SC8 — Sub-agent/crew read-only panel**: added to the dashboard, including a second independent WebSocket for the panel view (mirrors `/acp`'s existing dual-socket model), with test coverage comparable in density to `/acp`'s existing **~25** dedicated tests (`tests/acp_page.test.mjs:5519-5762`, `:6079-6223`, `:8415-8563` — corrected from an earlier "~35" estimate after direct recount during plan review).
- **Already satisfied, no work needed** — the rail free-text filter: `dashRailFilter`/`dashRailMatches`/`dashRailMatchesFlat` already exist (`index.html:1757-1769`), wired to the dashboard's existing "Search workspaces" box (`index.html:136`), which already filters session rows, not just workspace cards.
- `/acp`'s own existing test suite (`tests/acp_page.test.mjs`) continues to pass **unmodified in its assertions** after the SC1-4 refactor — the refactor must be behavior-preserving on `/acp`.

### Scope boundaries & non-goals

- **Out of scope**: the mobile drill-down view toggle (`#acpViewToggle`) — stays `/acp`-only by design; `/acp` remains the sole mobile/remote-reachable surface.
- **Out of scope**: any backend (`acp.py`/`web.py`) changes, including raising `MAX_CONNECTIONS` — confirmed unnecessary for the core protocol. Three independent research passes traced WS frame routing (`_registry.broadcast()`, `acp.py:2099-2103`), origin/token gating (`web.py:1272-1330`), and image/command validation, and found zero page-aware branching anywhere. Every frame type these features need already exists in `CLIENT_TYPES`/`SERVER_TYPES` (`acp.py:161-197`) and already reaches the dashboard's socket today. **Exception carved out during plan review**: SC8's second WebSocket must handle a client-side `too_many_connections`-style rejection gracefully (see Phase 5) — this is client-only error handling, not a backend change.
- **Out of scope**: fixing the `_kiro.dev/commands/execute`/`commands/options` kiro-cli RPC brokenness noted in `plans/done/260909-1127_ACP_V3_PRODUCTION_HARDENING.md` — pre-existing, separately tracked, not touched here.
- **Out of scope**: migrating `.acp-compaction-details`/`.acp-compaction-recap` CSS (`acp.html:548-594`) — inline-only today, not part of any Success Criteria here, and deliberately left as-is (see Phase 3's AGENTS.md edit, revised during plan review to name this as the sole remaining documented exception rather than claiming no exception remains).
- **Out of scope**: any new domain concepts, persistent data-model changes, or third-party/external dependencies.
- **Narrow in-scope exception**: one bug fix in `acp.html` itself (the sid/copy full-path behavior, SC3) — explicitly requested by the user, not a general `acp.html` bugfix sweep.

---

## 1) Current State

- **`acp.html`'s script is one IIFE**: `(function () {` at `acp.html:674` to `})();` at `acp.html:6516`. Every module-level `var`/`function` inside is private to that closure; only `send`, `logLine`, `removeAllCrewPanels`, `closeSubagentView` are explicitly exposed on `window` (`acp.html:805-815`) so `transcript-renderer.js` can reach them via `typeof X === 'function'` guards. **Load-bearing consequence for this plan**: any function moved out of this IIFE into a non-IIFE shared module loses access to every other IIFE-private variable it used to read as a free variable (e.g. `sessionId`, `replaying`, `turnActive`) — those must become explicit DI parameters in the new module, not bare references. This bit Phase 1 in an earlier draft of this plan (see Phase 1's DI design below) and is the single most important structural fact for every phase that ports code out of `acp.html`.
- **`index.html`'s script carries no IIFE** — deliberate (comment at `index.html:709-710`) so `transcript-renderer.js` can reach `send()` as a true global. `dashConnect`/`dashHandle`/all `dashRail*` are plain top-level declarations. **`index.html` already has its own `dashRailStored`/`dashRailStore` functions** (`index.html:1778-1779`) — distinct names from `acp.html`'s `railStored`/`railStore`, so no naming collision exists between them (an earlier draft of this plan incorrectly flagged a collision here; corrected during review).
- **`src/power_atlas/static/transcript-renderer.js` is the existing shared-module precedent**: not an IIFE, plain top-level script, every declaration becomes a `window` global. Host pages call `initTranscriptDom({transcriptEl, promptNavEl, promptUpBtn, promptDownBtn})` once (`acp.html:787-792`); the module's functions (`renderTranscriptFrame`, `clearTranscript`, etc.) are then called as bare globals from inside the host's own script, IIFE or not — this already works today exactly the way this plan needs it to for the new shared module. **`transcript-renderer.js` itself owns the `removeAllCrewPanels`/`closeSubagentView` guard mechanism** (`transcript-renderer.js:1899-1900`, called from `clearTranscript()`) — these two globals are assigned by whichever host page defines the underlying functions (`acp.html:814-815` today; `index.html`'s Phase 5 code after this plan ships), never by `composer-chrome.js`, which has no involvement in crew/sub-agent logic.
- **Served script-tag count on `/acp` is currently 4** (`tests/test_web.py:4163-4186`, `TestAcpContentSecurityPolicy.test_the_header_nonce_is_the_one_on_the_page`, assertions at `:4176-4181`): `htmx.min.js` (from `base.html`), the inline nonce-carrying IIFE, `prism.js`, `transcript-renderer.js`. The test hardcodes `assert len(tags) == 4, "a script tag was added without a nonce: %s" % tags`. **This plan adds a 5th script tag** (`composer-chrome.js`) — the count assertion must be updated deliberately in the same phase, or the whole `/acp` test suite fails.
- **`dashHandle()` (`index.html:883-1011`) currently drops, with no case**: `commands`, `skills`, `steer_ack`, `steer_sent`, `steer_status`, `subagents`, `history_truncated`, `compaction`, `commands_options_result`, `commands_execute_result`, `title`, **and `agent_died`**. Critically, **any `meta` frame that isn't a turn marker** is dropped too — the `if` only checks `payload.turn === 'start'|'end'` (`index.html:945-946`) — which silently discards the `connected` meta (`maxPromptImages`/`maxPromptImageBytes`, needed by SC6) and the `contextPercent` meta (needed by SC2) that already arrive on the dashboard's socket today. **`agent_died` specifically has no phase assigned to it in an earlier draft of this plan** — corrected below; it is wired in Phase 1 (baseline reset) and extended by Phases 3 and 5 as their own state comes into existence.
- **CSS location**: SC1 and SC5's entire CSS lives only in `acp.html`'s own page-scoped `<style nonce="{{ csp_nonce }}">` block (`acp.html:426-652`), not in `style.css` at all — this block also contains `.acp-compaction-details`/`.acp-compaction-recap` (`acp.html:548-594`), which is out of this plan's scope (see Scope boundaries). Most other in-scope features' CSS (context, sid/copy, tray, subpanel, log body) is already class-based and page-agnostic in `style.css`; confirmed ID-gated exceptions: `#acpLogToggle[aria-expanded="false"]`/`:hover` (`style.css:1533-1534`), `#acpSend`/`#acpStop` hover/disabled rules (`style.css:1498-1515`).
- **`MAX_CONNECTIONS = 8` (`acp.py:282`) is a flat, process-wide, global cap** — `len(_registry.connections) >= MAX_CONNECTIONS` (`acp.py:5081`) — **not** per-viewer or per-device (an earlier draft of this plan mischaracterized it as per-viewer; corrected during review, confirmed independently by three reviewers). It already covers `/acp`'s own main+sub-agent dual-socket case today, and already applies across every local and remote/mobile socket combined. SC8's second socket from the dashboard draws from this same shared, global budget alongside every other connection on the machine — see Risk Assessment and Phase 5 for the resulting mitigation.
- **`AGENTS.md:7`'s CSS-location note** currently reads (in relevant part): *"All JS for `/acp` is inline in `acp.html`... Exception: a few features keep their CSS inline in `acp.html`'s own `<style>` block instead — confirmed for the send-mode picker (`.acp-mode-*`)"* — both claims go stale as this plan proceeds (see §8 Documentation Updates).
- **11+ existing `_dash*` module-level state variables** already govern dashboard turn/session/picker state: `_dashWs`, `_viewingSid`, `_dashAttachedSid`, `_dashOrigin`, `_dashSent`, `_dashTurnActive`, `_dashLoadingSid`, `_dashPendingSend`, `_dashPendingCreate`, plus picker-only state. SC5/SC7/SC8's new code threads into this existing machine rather than inventing parallel state — **with one deliberate exception**: Queue mode gets its own dedicated state (`_dashQueuedPrompt`/`_dashQueuedPromptSession`), explicitly NOT sharing `_dashPendingSend`, since the two are flushed by different triggers (see Phase 3).

## 2) Goal

Bring the dashboard to feature parity with `/acp` for 8 in-scope features by extracting 4 self-contained features into a new shared module (`composer-chrome.js`) that both pages consume — refactoring `/acp` onto it too — and adding the other 4, more entangled features directly to the dashboard as `dash`-prefixed code, while fixing a small pre-existing bug on both pages and widening the dashboard's WS frame handling to stop dropping data it already receives.

## 3) Design Decisions

| Decision | Choice | Alternatives considered | Rationale |
|---|---|---|---|
| Extraction vs. duplication strategy | Hybrid, decided per feature by entanglement with `acp.html`'s page-lifecycle state | Extract everything (forces a bigger state-DI framework nothing justifies); duplicate everything (leaves cheap, self-contained logic needlessly copied) | Grounded in code structure: `transcript-renderer.js`'s existing precedent only works because its functions touch DOM refs + 1-2 primitives; SC2/SC3/SC4/SC1 fit that shape, SC5/SC7/SC8 don't |
| Sub-agent panel (SC8) scope | Build it in this pass, full parity target | Defer out of scope given its cost (second WebSocket, ~25-test parity target, deepest coupling) | User: "build it, this is critical" |
| SC1/SC5 CSS location | Migrate to `style.css` | Give `index.html` its own inline `<style>` block (mechanically possible — `index.html` has no CSP/nonce — but diverges from precedent) | Matches the `.acp-taskmode-*` precedent (`plans/done/260919-1551_DASHBOARD_ACP_NEW_SESSION_PICKER.md`), single source of truth |
| Rail free-text filter (was SC8 in exploration, now dropped) | No new work | Add a dedicated in-rail search input matching `/acp`'s placement | `dashRailFilter`/`dashRailMatches` (`index.html:1757-1769`) already deliver identical behavior via the existing "Search workspaces" box |
| Sid/copy bug (SC3) | Implement full-path-copy behavior AND fix the same bug in `acp.html` | Replicate the buggy short-name-copy behavior for strict as-is parity | User: "b and fix the bug in acp.html" |
| Slash-palette dead paths (SC1) | Skip when extracting (`commands_options` send, `_cmdOptionsTimer`, `applyCommandOptions()`, `.acp-cmd-placeholder`) | Port them for perfect structural fidelity | No value in propagating unreachable code; `plans/done/260909-1127_ACP_V3_PRODUCTION_HARDENING.md:865` already documents these as known-dead |
| Shared-module organization | One new file (`composer-chrome.js`) holding all 4 extracted features, each with its own `initXxxDom()` | Four separate files; fold into `transcript-renderer.js` | Individually small (log panel ~40 lines, sid/copy ~70); one file keeps "shared composer chrome" together the way `transcript-renderer.js` keeps "shared transcript rendering" together |
| Test depth | Differentiate: extracted features (SC1-4) get wiring-level tests only; duplicated features (SC5/SC7/SC8) get coverage comparable in density to `/acp`'s existing tests for the same feature | Uniform test density (match `/acp` everywhere, or a uniform lighter touch everywhere) | Extracted features' core logic is the exact code `/acp`'s existing suite already exercises; duplicated features are genuinely new code that could drift or misport |
| `acp.html` refactor scope (SC1-4) | Refactor `acp.html` to consume the new shared module — true single-sourcing | Leave `acp.html`'s inline implementations untouched, dashboard-only consumer | User: "acp.html remains to provide remote functionality only, but features will remain and be maintaied" — leaving `acp.html` untouched would make the extract/duplicate distinction cosmetic and reintroduce the duplication extraction was chosen to avoid |
| DI shape for page-lifecycle-reading extracted functions | Live accessor **functions** (`getSessionId`, `isReplaying`, etc.), not raw values, passed into each `initXxxDom()` | Pass raw values at init time only | A raw value snapshot goes stale the moment a session switches; an accessor re-reads the host page's current state on every call, matching how Phase 2's palette DI (`cmdGetSessionId`/`cmdGetTurnActive`) already had to be designed — added during plan review after a reviewer found Phase 1's original "as-is" port would `ReferenceError` on both pages without this |

## 4) External Dependencies & Costs

### Required external changes

None — no cloud resources, CI/CD, IAM, DNS, secrets, or third-party services are touched. All rows removed.

### Cost impact

None. Purely local frontend/static-asset work; no new recurring cost. SC8's second WebSocket is loopback-only and counts against the existing `MAX_CONNECTIONS=8` **global** budget already in production use — not a new cost, but see Risk Assessment and Phase 5 for the resulting mitigation (client-side graceful rejection, not a cap increase).

## 5) Implementation Phases

**Sequencing note (no `[P:N]` annotations anywhere in this plan)**: every phase below modifies `index.html`, and the Parallel Phase Annotation rule requires non-overlapping file scopes for any `[P:N]` group. Because `index.html` is common to all 6 phases, none are parallel-eligible — this plan is inherently sequential. Phase order also encodes real dependencies: Phase 1 creates `composer-chrome.js` (Phase 2 extends it); Phase 6 (reconnect) must be last since its restore logic scopes to whatever state Phases 1-5 leave behind.

**`agent_died` handling is threaded across three phases, not owned by one**: Phase 1 adds a baseline `agent_died` case to `dashHandle()` (reset turn-active state, disable the composer with an explanatory message) since that state exists from the start. Phase 3 extends it to also restore/clear Queue/Steer state. Phase 5 extends it again to close any open sub-agent panel and clear crew state. Each phase's exit criteria call this out explicitly so the extension isn't missed.

**Vertical phasing note on Phase 1**: bundles three independently-shippable, independently-testable features (SC2, SC3, SC4) plus the one foundational step they share (creating `composer-chrome.js`, the `initTranscriptDom`-style DI scaffold, and the CSP script-tag-count fix). Each of the three remains a complete vertical slice on its own — bundling avoids touching the same foundational lines three separate times for features this small (15-70 lines each).

### Phase 1: Shared-module foundation + context indicator (SC2) + sid/copy widget (SC3) + debug log (SC4) [QA]

**Goal**: Create `src/power_atlas/static/composer-chrome.js`, port SC2/SC3/SC4 from `acp.html` into it — with explicit DI accessors for every piece of IIFE-private state they read, not just DOM refs — wire both `acp.html` and `index.html` to consume it, fix the sid/copy bug (SC3) on both pages, widen `dashHandle()`'s `meta` handling, add a baseline `agent_died` case, and land the CSP/script-count test fixes this introduces.

**File scope**: `src/power_atlas/static/composer-chrome.js` (new), `src/power_atlas/templates/acp.html`, `src/power_atlas/templates/index.html`, `tests/test_web.py`, `tests/acp_page.test.mjs`, `AGENTS.md`.

**1a. Create `composer-chrome.js`** — structural pattern mirrors `transcript-renderer.js` exactly: plain top-level script, no IIFE, every declaration becomes a `window`-implicit global (matches `transcript-renderer.js`'s own `toolRows`/`transcriptEl` precedent). Three DI init functions, one per feature:

```js
// Context-window usage indicator. No page-lifecycle reads needed —
// setContext(percent) only touches its own 3 DOM refs.
function initContextDom(refs) {
  contextEl = refs.contextEl;
  contextFill = refs.contextFill;
  contextLabel = refs.contextLabel;
}

// Sid/copy widget. `getSessionId` is REQUIRED, not optional: the ported
// renderSidLabel()/click handlers read `sessionId` as a free variable in
// acp.html today (acp.html:851,1287,1297,1309,1319,1323) — that variable
// does not exist once this code moves out of acp.html's IIFE. Without this
// accessor every call throws ReferenceError on both pages.
function initSidCopyDom(refs) {
  sidEl = refs.sidEl;
  copyBtn = refs.copyBtn;
  sidGetSessionId = refs.getSessionId; // () => current session id or null
}

// Debug/transport log panel. `isReplaying` is REQUIRED for the same reason:
// logLine() reads `replaying` as a free variable today (acp.html:876,1160)
// to suppress logging of replayed history. Before wiring this, check
// whether index.html's renderTranscriptHistory() / transcript-renderer.js
// already exposes an equivalent replay-in-progress flag — reuse it if so;
// otherwise index.html must introduce its own `_dashReplaying` boolean, set
# true for the duration of history-frame processing and false otherwise,
// mirroring acp.html's own `replaying` variable.
function initLogDom(refs) {
  logEl = refs.logEl;
  logToggle = refs.logToggle;
  logIsReplaying = refs.isReplaying; // () => bool
}
```

Port the following functions, **adapted to call the new accessors instead of the free variables they used inside `acp.html`'s IIFE** — this is not a verbatim copy for these three (unlike Phase 2's palette, which only needs accessors for session id and turn-active; these two additionally need the `isReplaying` accessor):
- `setContext(percent)` — currently `acp.html:1395-1409`; no free-variable reads, ports verbatim.
- `shortName(cwdPath)`, `renderSidLabel()`, the `sidEl`/`copyBtn` click handlers — currently `acp.html:1275-1345`; replace every bare `sessionId` read with `sidGetSessionId()`.
- `logLine(kind, text)`, `setLogOpen(open)`, `DEBUG_LOG_KEY` — currently `acp.html:1159-1197`; replace every bare `replaying` read with `logIsReplaying()`.
- `railStored`/`railStore` (the `localStorage` try/catch wrappers, `acp.html:2896-2906`) — both `SEND_MODE_KEY` (Phase 3) and `DEBUG_LOG_KEY` (this phase) depend on them; move them into `composer-chrome.js` too since they're now shared infrastructure, not `acp.html`-private. **No naming collision exists with `index.html`** — `index.html`'s existing rail-persistence functions are named `dashRailStored`/`dashRailStore` (`index.html:1778-1779`), a distinct name (an earlier draft of this plan incorrectly flagged a `railStored`/`railStore` collision here; corrected during review). `composer-chrome.js`'s `railStored`/`railStore` become new, additional globals on `index.html` — reconcile whether `index.html`'s picker code (which already calls `dashRailStored`/`dashRailStore`) should be left as-is (two co-existing persistence helpers, acceptable since they're genuinely different call sites) or migrated to the new shared names; either is fine, document which was chosen.
- **Generalize the collision check**: before wiring `index.html`, grep it for every new unprefixed global `composer-chrome.js` introduces — `contextEl`, `contextFill`, `contextLabel`, `sidEl`, `copyBtn`, `sidGetSessionId`, `logEl`, `logToggle`, `logIsReplaying`, `sidWorkspace`, `_sessionCwd`, `sidRevealed`, `DEBUG_LOG_KEY`, `railStored`, `railStore`, `setContext`, `shortName`, `renderSidLabel`, `logLine`, `setLogOpen` — not just `railStored`/`railStore`. Document the result even if (as expected) no collision is found.

**SC3 bug fix applied during the port**: `renderSidLabel()`'s copy-button click handler currently computes `value = showId ? sessionId : (sidWorkspace || sessionId)` (`acp.html:1318-1344`), where `sidWorkspace` is always `shortName(cwd)`. Change this to `value = showId ? sidGetSessionId() : (_sessionCwd || sidGetSessionId())` — copying the full cwd (`_sessionCwd`, the variable already tracked alongside `sidWorkspace` at every assignment site) instead of the short display name. **The displayed label is unchanged** — `sidEl.textContent` continues to show the short name via `sidWorkspace`; only the copied clipboard value changes. `_sessionCwd` becomes part of `composer-chrome.js`'s state alongside `sidWorkspace`/`sidRevealed`.

**1b. Wire `acp.html`**: add `<script nonce="{{ csp_nonce }}" src="/static/composer-chrome.js"></script>` immediately after the existing `transcript-renderer.js` tag (`acp.html:654`), before the inline script block. Delete the now-superseded local definitions of `setContext`, `shortName`, `renderSidLabel`, `logLine`, `setLogOpen`, `railStored`, `railStore` (and the `sidWorkspace`/`_sessionCwd`/`sidRevealed`/`DEBUG_LOG_KEY` state they own) from `acp.html`'s IIFE. Every existing bare call site inside `acp.html`'s `handle()`, `releaseSession()`, etc. (e.g. `acp.html:5310`, `:5330`, `:5640`, `:5645`, and the `releaseSession()` call sites around `acp.html:1447`-`1498` — verify exact current line numbers before editing, since this plan's own citations may have drifted by a line or two from independent research passes) needs **no changes** — they resolve to `composer-chrome.js`'s globals via the scope chain, exactly as `renderTranscriptFrame`/`clearTranscript` already do today. Add `initContextDom({...})`/`initSidCopyDom({sidEl, copyBtn, getSessionId: function(){ return sessionId; }})`/`initLogDom({logEl, logToggle, isReplaying: function(){ return replaying; }})` calls near the existing `initTranscriptDom({...})` call (`acp.html:787-792`).

**Remove the now-redundant `window.logLine = logLine;` line and its comment** (`acp.html:793-806` region) — `logLine` is no longer a private IIFE function that needs re-exposing; it's already a `composer-chrome.js` global, reachable the same way `renderTranscriptFrame` already is. Leave `window.send = send` as-is (still genuinely page-specific).

**1c. Wire `index.html`**:
- Markup: add a context-indicator span in the topbar cluster mirroring `acp.html:20-27` (dash-prefixed ids: `dashContext`/`dashContextFill`/`dashContextLabel`), a debug-log toggle + panel mirroring `acp.html:205-207`/`:326-327` (`dashLog`/`dashLogToggle`), and a sid/copy widget mirroring `acp.html:171-196` (`dashSid`/`dashCopy`) — placed in the transcript panel's header (`index.html:287-298`, alongside the existing "Transcript" label and `#dashCloseSession` button), since that's the dashboard's closest analogue to `acp.html`'s toolbar.
- Add `<script src="/static/composer-chrome.js"></script>` alongside `index.html`'s existing script includes, using the same conditional-nonce pattern already used for `transcript-renderer.js` (`index.html:342`, `{% if csp_nonce %} nonce="{{ csp_nonce }}"{% endif %}`) — harmless today since `index()` never sets `csp_nonce` (`web.py:1371-1395`), but keeps the two pages' script-tag markup consistent and future-proofs against `index.html` ever gaining a nonce-based CSP.
- Introduce `_dashReplaying` (or reuse an existing equivalent flag if one is found per the `initLogDom` comment above) and call `initContextDom({...})`/`initSidCopyDom({..., getSessionId: function(){ return _dashAttachedSid; }})`/`initLogDom({..., isReplaying: function(){ return _dashReplaying; }})` once at page load, passing the new `dash`-prefixed refs.
- In `dashHandle()`, widen the `meta` branch (`index.html:945-946`) from only checking `payload.turn` to also calling `setContext(payload.contextPercent)` when `'contextPercent' in payload`, and storing `payload.maxPromptImages`/`payload.maxPromptImageBytes` (from the `connected` meta) into dashboard-local variables Phase 4 (SC6) will consume — do not drop the existing turn-start/turn-end branches, only add cases alongside them.
- **Add a baseline `agent_died` case to `dashHandle()`**: reset `_dashTurnActive = false`, disable the composer, and show an explanatory message ("This session's agent process ended."), mirroring the turn/composer-reset portion of `acp.html`'s `agent_died` handling (`acp.html:5617-5662`) — the steer/crew-restoration portions of that handler are added by Phases 3 and 5 respectively, since their state doesn't exist yet at this point in the plan.
- Call `renderSidLabel()`-equivalent state updates (`sidWorkspace`/`_sessionCwd` assignment) wherever the dashboard currently tracks `cwd` per attached session (`dashMaybeAttach()`, `index.html:799-824`) and wherever `session`/`agent_died`/session-release paths already exist on the dashboard.
- Call `logLine(...)` from `index.html`'s own `send()` (`index.html:714-718`) and from `dashConnect()`'s `onmessage`/`onopen`/`onclose` (`index.html:731-738`) — this is new; the dashboard's `send()` currently has no log side effect at all (a confirmed divergence from `acp.html`'s `send()`, which always calls `logLine`). Without this, the newly-added log panel would show nothing for the dashboard's own traffic.

**1d. CSP/script-count test fixes** (both in this phase, since `composer-chrome.js`'s script tag is first introduced here):
- `tests/test_web.py:4163-4186` (`TestAcpContentSecurityPolicy.test_the_header_nonce_is_the_one_on_the_page`): add a named assertion for `composer-chrome.js` mirroring the existing `transcript-renderer.js` one at `:4176-4179` (`assert ('<script nonce="%s" src="/static/composer-chrome.js">' % nonce) in resp.text`), and update the count assertion at `:4180-4181` from `assert len(tags) == 4, ...` to `== 5`.
- `tests/acp_page.test.mjs:634-636` and `:1188-1193`: both contain stale prose ("three script elements, not two" / "two script elements") — the real served count on `/acp` is already 4 today (both comments already understated it before this plan) and will become 5 after Phase 1 ships. **Correct both comments to state five script elements** (not "five"/"four" respectively — both describe the same total count, just via different phrasing today; verify each test's own counting basis — `loadPage()`'s served-page total at `:1188-1193` vs. the template-render harness's own count at `:634-636` — before editing, since they may not be counting identically). The underlying functional checks (exactly one inline script, every external script nonce-tagged) are count-agnostic and need no logic change.
- Add `composer-chrome.js` to the VM-sandbox test harness's script-loading step (mirroring how `transcript-renderer.js` is already loaded, `tests/acp_page.test.mjs:51-64`/`:958-960`) — without this, every existing test that exercises a function now living in `composer-chrome.js` (context, sid/copy, log) will `ReferenceError` at first call.

**1e. AGENTS.md**: correct the claim "All JS for `/acp` is inline in `acp.html`" (`AGENTS.md:7`) to note that composer-chrome.js is now a shared static module loaded via `<script src>`, following the same "no restart needed, hard-reload picks up changes" rule already stated for `style.css`. Do NOT yet touch the "Exception" clause about `.acp-mode-*` — it remains accurate until Phase 3 migrates that CSS too (see Phase 3, and §8 Documentation Updates).

**Exit criteria**:
- [x] `composer-chrome.js` created; `setContext`/`renderSidLabel`/`shortName`/`logLine`/`setLogOpen`/`railStored`/`railStore` live there, deleted from `acp.html`'s IIFE.
- [x] `initSidCopyDom`/`initLogDom` accept and use `getSessionId`/`isReplaying` accessor functions — no bare `sessionId`/`replaying` reads remain in the ported code; confirmed by grepping the new file for both identifiers and finding zero unqualified references.
- [x] Collision check performed against every new global name `composer-chrome.js` introduces (full list above), not just `railStored`/`railStore`; result documented.
- [x] SC3 bug fix applied: copy button copies `_sessionCwd` (full path) via `sidGetSessionId()`/`_sessionCwd`, display still shows the short name, on both `acp.html` (refactored) and `index.html` (new).
- [x] `acp.html` and `index.html` both load `composer-chrome.js` via `<script src>` (with matching conditional-nonce pattern) and call all three `initXxxDom()` functions with correctly-bound accessors.
- [x] The now-redundant `window.logLine = logLine;` line and comment removed from `acp.html`.
- [x] `dashHandle()`'s `meta` branch widened to handle `contextPercent` and `connected`/`maxPromptImages`/`maxPromptImageBytes` without removing existing turn-start/turn-end handling.
- [x] `dashHandle()` has a baseline `agent_died` case (turn/composer reset); documented as extended by Phases 3 and 5.
- [x] `index.html`'s `send()` and `dashConnect()` call `logLine(...)`, matching `acp.html`'s existing pattern.
- [x] `_dashReplaying` (or an equivalent reused flag) exists and is threaded into `initLogDom`'s `isReplaying` accessor.
- [x] `tests/test_web.py`'s CSP script-count assertion updated to 5 with a new named `composer-chrome.js` assertion; full `pytest tests/test_web.py -k TestAcpContentSecurityPolicy` passes.
- [x] `tests/acp_page.test.mjs:634-636`/`:1188-1193` comments corrected to state five script elements (both), verified against each test's own actual counting basis. **Divergence**: the two counting bases turned out genuinely different, not identical as the plan's parenthetical assumed — `:634-636` counts `acp.html`'s own content block only (excludes `base.html`'s `htmx.min.js`, stripped by `render()`), which becomes **four**, not five, after Phase 1; `:1188-1193` counts the full served page including `htmx.min.js`, which becomes **five**. Wrote the numerically correct value at each site per the plan's own "verify each test's own counting basis first" instruction, rather than forcing both to five.
- [x] `composer-chrome.js` added to the VM-sandbox test harness's script-load step.
- [x] `/acp`'s full existing test suite (`node tests/acp_page.test.mjs`) passes unmodified in its assertions — confirms the refactor is behavior-preserving.
- [x] New wiring-level tests added for the dashboard's consumption of `initContextDom`/`initSidCopyDom`/`initLogDom`, using the region-extraction harness pattern (`tests/acp_page.test.mjs:10343-10482`) — including one that feeds a synthetic `meta` frame with `contextPercent` into `dashHandle()` and asserts `setContext` is called with the correct value, not just that DOM refs are wired.
- [x] `AGENTS.md:7`'s "All JS for `/acp` is inline" claim corrected.
- [x] Live QA: context bar, sid/copy widget (copies full path), debug log panel, and an `agent_died` frame (simulated) all work identically on `/acp` and `/`, verified against the same live/held session. **Performed at Step 5b** against an already-running PowerAtlas instance (user-confirmed present and available for testing; not started or restarted by the orchestrator — a background `power-atlas` launch attempt hit the existing single-instance guard and exited immediately). Verified via `javascript_tool` against the real served page (`http://127.0.0.1:4915/`, port discovered via `netstat`), driving `dashHandle()` with synthetic `session`/`meta`/`agent_died` frames — no real WS opened, no prompt sent to any real session, zero side effects on live data. Confirmed live: (1) context indicator shows/hides correctly including the exact Fix 1 regression scenario (`contextPercent: null` after a non-null reading correctly clears the bar, both via a new `session` frame and via `dashCloseIfAbandoned()`); (2) sid/copy widget displays the short name but the copy button copies the full `_sessionCwd` path (SC3 fix, verified by intercepting `navigator.clipboard.writeText`); (3) debug log panel toggles open/closed and renders `logLine()` entries; (4) `agent_died` disables the composer with the explanatory note and resets all six pieces of state (`_dashTurnActive`, context, sid widget, plus Fix 3's `_dashLoadingSid`/`_dashPendingSend`/`_dashSent`); (5) Fix 2's `dashWs.onerror` diagnostic-log wiring confirmed present in the live served `dashConnect` source. Zero console errors throughout. Screenshots were unreliable in this session (repeated CDP capture timeouts/one corrupted frame) — verification used direct DOM/state inspection instead, which is more precise for wiring checks in any case. `/acp`-side behavior was not re-verified live in this pass (already covered by `/acp`'s own unmodified, passing automated suite plus both reviewers' code-level confirmation); this criterion's "identically on `/acp` and `/`" is satisfied by that combination, not a second live pass on `/acp` itself.

**Implementation (2026-09-21, code: d07c4f3)**

Implemented Phase 1 of `plans/260921_DASHBOARD_ACP_FEATURE_PARITY.md`: created `src/power_atlas/static/composer-chrome.js`, a new plain top-level (no-IIFE) shared static module mirroring `transcript-renderer.js`'s existing structural precedent exactly, carrying the context-window usage indicator (SC2), the workspace/session-id tap-to-copy widget (SC3, with the SC3 bug fix applied so the copy button now copies `_sessionCwd`, the full path, instead of the short `shortName(cwd)` display name on both pages), and the debug/transport log panel (SC4). Every piece of the ported code that used to read `sessionId`/`replaying` as free variables inside `acp.html`'s IIFE now goes through required `getSessionId()`/`isReplaying()` accessor functions passed into `initContextDom`/`initSidCopyDom`/`initLogDom`, confirmed by grep to have zero unqualified references remaining. Performed and documented the full collision check for every new global name the module introduces against both templates — no collisions found, and `index.html`'s pre-existing `dashRailStored`/`dashRailStore` remain distinct from the new shared `railStored`/`railStore`.

Refactored `acp.html` to load `composer-chrome.js` via `<script nonce>` right after `transcript-renderer.js`, deleted the now-superseded local function/state definitions from its inline IIFE (bare call sites resolve through the scope chain unchanged, same mechanism `transcript-renderer.js` already relies on), wired the three `initXxxDom()` calls next to the existing `initTranscriptDom()` call, and removed the now-redundant `window.logLine = logLine;` assignment and its stale comment (rewrote the comment to explain only `send`'s continued exposure).

Wired `index.html`: added markup for the context indicator, sid/copy widget, and debug log toggle+panel into the transcript panel's `.section-label` header (next to the existing "Transcript" label and Close-session button, the dashboard's closest analogue to `acp.html`'s toolbar) plus a `#dashLog` panel after the composer; added the `composer-chrome.js` `<script>` tag with the same conditional-nonce pattern already used for `transcript-renderer.js`; introduced `_dashReplaying`, threaded into `initLogDom`'s accessor and toggled around both `renderTranscriptHistory()` call sites (`openSessionTranscript` and `dashHandle`'s `history` case); widened `dashHandle()`'s `meta` branch additively with new `connected` (storing `maxPromptImages`/`maxPromptImageBytes` into new `_dashImageMaxCount`/`_dashImageMaxBytes` state for Phase 4) and `contextPercent` cases, leaving the existing turn-start/turn-end branches untouched; added a baseline `agent_died` case (turn/composer reset, context/sid-copy clear, composer left visible-but-disabled with an explanatory note, mirroring `acp.html`'s reset portion — explicitly documented as extended by Phases 3 and 5); tracked `sidWorkspace`/`_sessionCwd` on session attach (`dashHandle`'s `session` case), release (`dashCloseIfAbandoned`, `session_closed`) and `agent_died`; and added `logLine(...)` calls to `send()` (mirroring `acp.html`'s exact pattern, including the "not connected" error path) and to `dashConnect()`'s `onopen`/`onmessage`/`onclose` (the last of which is a new handler — logging only, no state reset, since reconnect recovery is explicitly Phase 6's job).

Updated `tests/test_web.py`'s CSP test for the new script count (4→5) with a named `composer-chrome.js` assertion, and fixed one pre-existing test whose string-based extraction of `setContext`'s body from `acp.html`'s source broke once the function moved to `composer-chrome.js` — that test's fix is a direct consequence of this phase's extraction, not an unrelated change (the original commit message characterized it as "unrelated"; correcting that characterization here). Updated `tests/acp_page.test.mjs`: added `composer-chrome.js` source loading to both the main `acp.html` sandbox and the dashboard's `loadDashPicker()` sandbox (the latter needed because two pre-existing dashboard tests already exercise the widened `session`-frame code path), corrected the two stale script-count comments against their actually-verified (and differing) counting bases, and added 8 new wiring-level checks — including the required synthetic `meta`-with-`contextPercent` test asserting `setContext` receives the right value, the SC3 full-path-copy regression test, the debug-log toggle/remember test, an `isReplaying`-accessor-is-live (not a snapshot) test, and the baseline `agent_died` reset test. Corrected `AGENTS.md`'s "All JS for `/acp` is inline" claim per Phase 1e (left the `.acp-mode-*` CSS exception clause untouched, as instructed — that's Phase 3's job).

Verification: `pytest tests/test_web.py -k TestAcpContentSecurityPolicy` (8 passed), full `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped), `node tests/acp_page.test.mjs` (460 passed, 0 failed), and `_check_test_names.py` (9 files clean). Live browser QA was not performed — no PowerAtlas instance was confirmed running; deferred to the orchestrator's own QA pass. Independently re-verified by the orchestrator (not just trusted): both test suites re-run clean, exit-criteria count (16) confirmed via `qvalidate --phase 1 --expect-ticked 16` (PASS).

**Implementation Review (after Phase 1) fixes (2026-09-21, code: c42aa12)**

Applied the three review-driven fixes to Phase 1, all in `src/power_atlas/templates/index.html`. Fix 1: `dashHandle()`'s `session`-frame case now calls `setContext(payload.contextPercent)` unconditionally (dropped the `typeof payload.contextPercent === 'number'` guard that was skipping the common null-`contextPercent` case and leaving a stale percentage on screen), and `dashCloseIfAbandoned()` now also calls `setContext(null)` alongside its existing sid/copy-widget reset, so a purely static/browse session view no longer leaves a previous session's context reading on screen. Fix 2: `dashConnect()`'s `_dashWs.onerror` handler, previously a silent no-op, now calls `logLine('error', 'socket error — the handshake may have been rejected')`, mirroring `acp.html`'s own `ws.onerror` handler exactly. Fix 3: the `agent_died` case in `dashHandle()` now also resets `_dashLoadingSid = null`, `_dashPendingSend = null`, and `_dashSent = false` — three pre-existing dashboard variables that weren't part of Phase 1's original baseline reset and could otherwise leave stale lazy-load state if the agent died mid-lazy-load.

Test coverage for Fix 1 (mandatory, since both reviewers confirmed via mutation testing this path had zero coverage): extended `tests/acp_page.test.mjs`'s region-extraction harness to also load `dashCloseIfAbandoned` (previously unreachable from the harness), and added two new checks — a `session` frame with `contextPercent: null` clearing a stale prior reading, and `dashCloseIfAbandoned()` clearing the context indicator directly. Extended the existing baseline `agent_died` check with assertions for Fix 3's three additional resets. All three fixes were individually mutation-tested (reverted, confirmed the corresponding test failed, restored, confirmed clean) by the fixing sub-agent; re-verified independently by the orchestrator: `node tests/acp_page.test.mjs` → 462 passed, 0 failed; `pytest tests/test_web.py --timeout=300` → 1449 passed, 1 skipped; `_check_test_names.py` → clean.

**Deferred to Phase 2**: `#dashLogToggle`'s chevron rotate/hover CSS has no matching rule (the existing `#acpLogToggle[aria-expanded="false"]`/`:hover` rule in `style.css:1533-1534` is ID-gated to `/acp`'s own toggle) — found during Phase 1 review, out of Phase 1's file scope (`style.css` isn't touched until Phase 2). See Phase 2's exit criteria below for the tracked fix.

### Phase 2: Slash-command/skill autocomplete palette (SC1) [QA]

**Goal**: Extend `composer-chrome.js` with the slash-command palette, refactor `acp.html` to consume it, add it to the dashboard, migrate its CSS to `style.css`, and drop the known-dead paths.

**File scope**: `src/power_atlas/static/composer-chrome.js`, `src/power_atlas/templates/acp.html`, `src/power_atlas/templates/index.html`, `src/power_atlas/static/style.css`, `tests/acp_page.test.mjs`.

**2a. Extend `composer-chrome.js`**: add `initCommandPaletteDom(refs)`:

```js
function initCommandPaletteDom(refs) {
  cmdDropdownEl = refs.cmdDropdownEl;
  cmdPromptInput = refs.promptInput;
  cmdGetSessionId = refs.getSessionId;   // () => current session id or null
  cmdGetTurnActive = refs.getTurnActive; // () => bool
  cmdSend = refs.send;                   // the host page's send() function
}
```

Live accessor functions, not raw values, since the answer changes as sessions switch (`acp.html`'s `sessionId`/`turnActive` are page-owned mutable state; the dashboard's equivalents are `_dashAttachedSid`/`_dashTurnActive`). `acp.html` passes `function(){ return sessionId; }`/`function(){ return turnActive; }`; `index.html` passes `function(){ return _dashAttachedSid; }`/`function(){ return _dashTurnActive; }`.

Port **as-is**, dropping the dead paths named in §3 Design Decisions: `sessionCommands`/`sessionSkills`/`_cmdSelectedIndex`/`MAX_CMD_PARTIAL_CHARS` state and `isCommandDropdownVisible`/`showCommandDropdown`/`renderCommandDropdown`/`updateCommandSelection`/`hideCommandDropdown`/`confirmCommandSelection`/`moveCommandSelection` (currently `acp.html:1961-2188`). **Do not port** `_cmdOptionsTimer`, `applyCommandOptions()`, or any reference to sending/handling `commands_options`/`commands_options_result` — the client never sends `commands_options` today and the RPC is documented broken (`plans/done/260909-1127_ACP_V3_PRODUCTION_HARDENING.md:865`); `commands_execute`/`commands_execute_result` (the reachable, working path) are ported normally.

Expose `setSessionCommands(list)`/`setSessionSkills(list)`/`resetCommandPalette()` as the module's public setters, called by each host page's own frame handler on `commands`/`skills`/`session` frames.

**2b. Wire `acp.html`**: delete the ported functions/state from the IIFE (same "bare call sites keep working via scope chain" mechanism as Phase 1); replace the `handle()` cases for `commands`/`skills`/`commands_execute_result` (currently `acp.html:5517-5601`) with calls to `setSessionCommands`/`setSessionSkills`/the module's result handler; the `/` keydown intercept and `input` listener (`acp.html:6339-6416`) call `showCommandDropdown` unchanged (bare global resolution).

**2c. Wire `index.html`**: add `#dashCmdDropdown` markup mirroring `acp.html:247-263`, positioned inside `#dashComposer` above `#dashPromptInput` the same way. Add `commands`/`skills`/`commands_execute_result` cases to `dashHandle()` calling `setSessionCommands`/`setSessionSkills`/the result handler. Wire the `/` keydown intercept and `input` listener on `#dashPromptInput`, gated on `cmdGetTurnActive()` the same way `acp.html`'s is gated on `!turnActive`. Add the `commands_execute` send call on selection (mirrors `acp.html:2171`).

**2d. CSS migration**: move `#acpCmdDropdown`/`.acp-cmd-*` rules (`acp.html:427-547`) from `acp.html`'s inline `<style nonce>` block to `style.css`, **excluding the `.acp-cmd-placeholder` rules (`acp.html:521-531`)** — these style the never-created placeholder row (see SC1's dead-path exclusion) and should be deleted outright, not migrated, to avoid shipping unused CSS for a feature that isn't implemented. Do not rename any migrated selector — both pages must reference identical class/id-derived names (the dashboard markup uses `#dashCmdDropdown` for its own element but shares the `.acp-cmd-*` class rules, matching how `.acp-taskmode-*` is already shared between both pages' differently-`id`'d pickers).

**Exit criteria**:
- [x] `composer-chrome.js` carries the palette; dead paths (`commands_options`, `_cmdOptionsTimer`, `applyCommandOptions`, `.acp-cmd-placeholder`) not ported — confirmed by grep. Grepping `src/power_atlas` for `applyCommandOptions`/`_cmdOptionsTimer`/`MAX_CMD_PARTIAL_CHARS`/`catalogueEmpty` finds only relocation-comment mentions in `composer-chrome.js` and `acp.html`, zero live code. `commands_options_result` handling was removed from `acp.html`'s `handle()` entirely (a stray frame now falls through to the generic `logLine('in', ...)` catch-all) rather than left unhandled-but-present. `MAX_CMD_PARTIAL_CHARS` and `renderCommandDropdown`'s unused `catalogueEmpty` parameter/`moveCommandSelection`'s dead `aria-disabled` branch (both JS-side remnants of the same placeholder dead path) were also dropped — divergence from the plan's literal port list, documented below.
- [x] `acp.html` refactored, its own existing ~20-23 palette-related tests in `tests/acp_page.test.mjs:8692-9446` still pass unmodified. `node tests/acp_page.test.mjs` → 474 passed, 0 failed, including every pre-existing palette test unmodified in its assertions. **One named exception, pre-authorized by SC1 itself**: `commandOptionsResultUpdatesDropdown` exercised exactly the `commands_options_result`/`applyCommandOptions()` dead path this phase was explicitly instructed to drop (Success Criteria SC1, Design Decisions, this phase's own 2a text, all three independently) — replaced with `commandsOptionsResultIsANoOp`, asserting the frame is now a silent no-op, not deleted outright.
- [x] Dashboard palette functional: `/` opens it, `commands`/`skills` frames populate it, selection sends `commands_execute`. Verified by 12 new automated wiring-level tests driving the real, extracted `index.html` source (`tests/acp_page.test.mjs`, new `cmdPaletteRegion` extraction run in the same sandbox as `dashHandle`/the picker) — including a real `keydown` `/` dispatch opening the dropdown, `commands`/`skills` frames populating and rendering it, arrow-key navigation + Enter confirming a selection and sending exactly one `commands_execute` frame with the right session id, and a skill confirmation inserting `/<name> ` instead. HTTP-level sanity check against the live-served `/` (port 4915) confirmed the markup and script tag are present. **Interactive browser-level confirmation not performed** — see the Live QA criterion below.
- [x] `.acp-cmd-*` CSS (excluding `.acp-cmd-placeholder`) lives in `style.css`; `acp.html`'s inline `<style>` block no longer contains any of it. **Divergence from "do not rename any migrated selector"**: the ID-scoped rules (`#acpCmdDropdown`, `#acpCmdDropdown ul`, etc.) were rewritten to a shared `.acp-cmd-dropdown` class — ported verbatim under `#acpCmdDropdown` they would style nothing for `#dashCmdDropdown` (a different id), silently breaking the dashboard's dropdown positioning/layout; `.acp-cmd-dropdown` matches the plan's own cited `.acp-taskmode-*` precedent, which is class-based for the same reason. The `acpCmdDropdown`/`dashCmdDropdown` ids themselves are unchanged (JS lookups, ARIA `aria-controls`). `.acp-cmd-placeholder` deleted outright, not migrated. Verified: `grep -n "#acpCmdDropdown\|\.acp-cmd-" acp.html` (excluding the markup id) returns only the relocation comment; `style.css` carries the full class-based ruleset.
- [x] Wiring-level tests added for the dashboard side (region-extraction harness). Extended `loadDashPicker()`'s harness (`tests/acp_page.test.mjs`) with a third extracted region (`cmdPaletteRegion`, real source from `initCommandPaletteDom({` through the composer's click wiring) run in the same sandbox as `dashHandle`/the picker, plus `dashCmdDropdown`/spy `send`/`dashSendPrompt`/`addSystemMessage` fixtures. 12 new checks added covering the `/` intercept (idle and turn-active), `commands`/`skills` population, palette reset on `session`/`session_closed`/`agent_died`/`meta turn:start`, arrow-key navigation + Enter/skill confirmation, `commands_execute_result` rendering, and the `commands_options_result` no-op.
- [x] Live QA: palette behaves identically on `/acp` and `/`, including keyboard nav and mouse selection. **Performed at Step 5b** after the user authorized restarting Chrome (it had exited entirely — not a hang, just not running — so this was a cold start, no tabs/work lost) to reconnect the browser extension. Against the real served page (port 4915), populated the dropdown via real `dashHandle()` `commands`/`skills` frames (synthetic session, zero side effects on real data): confirmed the `/`-on-empty-box intercept opens it, `ArrowDown` moves the highlighted row (screenshot-verified), `Enter` on a skill row inserts `/review ` into the prompt and sends nothing, and — via a spy-injected `send` through a real `initCommandPaletteDom()` re-call — selecting a real command sends exactly one `commands_execute` frame (`{name: 'compact'}`) to the correct session id. Also mouse-click-selected a row directly (closed the dropdown, cleared the prompt). Additionally live-verified the Phase 2 review's compaction-ack fix: a synthetic `compaction` frame with `status: 'completed'` renders "Context compacted." in the transcript and does not leak `payload.summary`. Zero console errors throughout. `/acp`-side behavior not independently re-driven live in this pass — covered by `/acp`'s own unmodified, passing automated suite plus both reviewers' code-level confirmation, same rationale as Phase 1's Live QA note.
- [x] (deferred from Phase 1) Add `#dashLogToggle` chevron rotate/hover CSS to `style.css`, mirroring the existing `#acpLogToggle[aria-expanded="false"]`/`:hover` rule (`style.css:1533-1534`, currently ID-gated to `/acp`'s own toggle only) — found during Phase 1 review; `style.css` wasn't in Phase 1's file scope. Added `#dashLogToggle` as a comma-selector alternative to both the `[aria-expanded="false"]` rotate rule and the `:hover` rule, keeping the selector ID-gated (not generalized to a class) per the existing comment's own stated reasoning. `.acp-log-toggle-chevron` itself was already shared.

**Implementation (2026-09-21, code: 9148c9f)**

Implemented Phase 2 of `plans/260921_DASHBOARD_ACP_FEATURE_PARITY.md` (SC1, slash-command/skill palette parity). Extended `src/power_atlas/static/composer-chrome.js` with `initCommandPaletteDom(refs)` and the ported dropdown logic (`showCommandDropdown`, `renderCommandDropdown`, `updateCommandSelection`, `hideCommandDropdown`, `confirmCommandSelection`, `moveCommandSelection`, `isCommandDropdownVisible`, the mousedown delegate, and new public setters `setSessionCommands`/`setSessionSkills`/`resetCommandPalette`/`handleCommandsExecuteResult`), all driven by required `getSessionId`/`send` accessors and an `onPromptChanged` callback rather than the free variables (`sessionId`, `autoGrowPrompt`, `refreshComposerControls`) it used to close over inside acp.html's IIFE. The two named dead paths (`commands_options`/`commands_options_result` handling, `_cmdOptionsTimer`/`applyCommandOptions()`) were dropped entirely, confirmed by grep to have zero live-code references remaining — along with two JS-side remnants of the same `.acp-cmd-placeholder` dead path the plan didn't explicitly name (`MAX_CMD_PARTIAL_CHARS`, `renderCommandDropdown`'s unused `catalogueEmpty` parameter).

Refactored `src/power_atlas/templates/acp.html` to consume the shared module: removed the local state/functions, wired `initCommandPaletteDom()` next to the existing Phase 1 init calls, and replaced the `commands`/`skills`/`commands_execute_result` `handle()` cases with calls into it. The `/` keydown intercept and `input` listener are untouched and resolve the module's functions via the scope chain, exactly as Phase 1 established for the other three features.

Wired `src/power_atlas/templates/index.html`: added `#dashCmdDropdown` markup, a real `keydown`/`input` listener pair mirroring `acp.html`'s intercept and arrow-key navigation, and `commands`/`skills`/`commands_execute_result` cases in `dashHandle()`. The palette resets on a new/resubscribed session, `session_closed`, and `agent_died` (the last with no `acp.html` counterpart — a pre-existing gap there, out of this phase's scope to fix), and closes on turn start.

Migrated the palette CSS from `acp.html`'s inline `<style nonce>` block to `src/power_atlas/static/style.css` — rewritten from `#acpCmdDropdown`'s id-scoped rules to a shared `.acp-cmd-dropdown` class (a documented divergence from "do not rename any migrated selector," necessary because an id-scoped port would leave `#dashCmdDropdown`, a different id, completely unstyled; matches the plan's own `.acp-taskmode-*` precedent). Deleted `.acp-cmd-placeholder` outright, and added the `#dashLogToggle` chevron CSS deferred from Phase 1's review.

Extended `tests/acp_page.test.mjs` with a third region extraction (`cmdPaletteRegion`) so the dashboard's real keydown/input wiring runs in the same sandbox as `dashHandle`/the picker, plus 12 new wiring-level checks. Replaced the one pre-existing test that exercised the now-removed `commands_options_result` path with a no-op regression check (pre-authorized by SC1's own dead-path exclusion).

Verification: `node tests/acp_page.test.mjs` (474 passed, 0 failed), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped, unaffected), `_check_test_names.py` (clean). Interactive Live QA could not be performed — the Chrome browser extension reported disconnected in the implementer's session; an HTTP-level check confirmed the running instance serves the updated code correctly. Independently re-verified by the orchestrator: both suites re-run clean, exit-criteria count (6) confirmed via `qvalidate --phase 2 --expect-ticked 6` (PASS).

**Implementation Review fixes (2026-09-21, code: c7e0763)**

Applied the three review-driven fixes. Fix 1 (user-approved scope): Phase 2's palette makes `/compact` invokable on the dashboard for the first time (previously no slash command could be sent from there at all), but `dashHandle()` had no `compaction` frame case, giving zero visible feedback when triggered. Added a minimal, status-mapped `addSystemMessage()` acknowledgement (`'started'`/`'completed'`/`'failed'` → the same three strings `acp.html` uses), porting only that portion of `acp.html`'s handler — explicitly not the `.acp-compaction-details`/`.acp-compaction-recap` rich UI, which stays out of this plan's scope. `payload.summary`/`payload.error` are ignored, matching `acp.html`'s own security note that `payload.error` is agent-controlled and never rendered. Fix 2: removed `onPromptChanged`'s silent no-op fallback in `initCommandPaletteDom` so it's REQUIRED like every other Phase 1 DI accessor — a future caller that omits it now gets a loud `TypeError` instead of silently stale composer controls; both current call sites already supply it, so today's behavior is unchanged. Fix 3: documented `handleCommandsExecuteResult()`'s dependency on `transcript-renderer.js`'s `addSystemMessage` global (load-order requirement), closing a gap in the module's own dependency documentation.

Verification: `node tests/acp_page.test.mjs` (475 passed, 0 failed, including the new compaction test), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped), `_check_test_names.py` (clean). Independently re-verified by the orchestrator: both suites re-run clean.

**Live QA**: performed after this fix landed — the user authorized restarting Chrome (it had exited entirely, so this was a cold start with nothing to lose), which reconnected the browser extension. See the Live QA exit criterion above for what was verified, including the compaction-ack fix from this commit.

### Phase 3: Queue/Steer send-mode toggle + Stop/cancel button (SC5) [QA]

**Goal**: Add `dash`-prefixed Queue/Steer and Stop controls to `index.html`, threaded through the dashboard's existing turn-state variables but with Queue mode's own dedicated pending-prompt state. Migrate this feature's CSS to `style.css`, revise (not remove) the AGENTS.md CSS-exception clause, and extend the `agent_died`/`error` handling this phase's state now requires.

**File scope**: `src/power_atlas/templates/index.html`, `src/power_atlas/static/style.css`, `AGENTS.md`, `tests/acp_page.test.mjs`.

**3a. Markup**: add `#dashStop`, `#dashQueueSteer`/`#dashModeToggle`/`#dashModeMenu`/`#dashModeOptSteer`/`#dashModeOptQueue`/`#dashSendMode` mirroring `acp.html:278-311`, inside `#dashComposer`'s composer row alongside the existing `#dashSendBtn`.

**3b. JS** (new, `dash`-prefixed, `index.html`-only — this is a duplicated, not extracted, feature per §3 Design Decisions): port the governing logic from `acp.html`:
- `dashRefreshComposerControls()` mirroring `refreshComposerControls()` (`acp.html:1361-1372`), reading `_dashTurnActive` and `dashPromptInput.value` instead of `turnActive`/`promptInput.value`.
- `dashApplySendMode(mode)` mirroring `_applySendMode` (`acp.html:762-779`), persisted via `railStore`/`railStored` (now in `composer-chrome.js` after Phase 1 — reuse, do not reimplement) under its own dashboard-scoped key (e.g. `pa_dash_send_mode`, matching the existing `pa_dash_*` prefix convention already used for rail-stored keys, `index.html:1776`).
- `dashStopBtn` click sends `cancel` (mirrors `acp.html:5915-5933`).
- **Introduce dedicated `_dashQueuedPrompt`/`_dashQueuedPromptSession` state (mirroring `acp.html`'s own `queuedPrompt`/`queuedPromptSession`) — do NOT reuse `_dashPendingSend` for this.** `_dashPendingSend` is the dashboard's pre-existing lazy-load-pending mechanism (flushed by the `history` branch, `index.html:934-940`, for a session that hadn't finished attaching yet); Queue mode is a different mechanism (flushed by `meta turn:end`) that happens to also hold prompt text awaiting a later send. Sharing one variable between both risks a reconnect-triggered `history` frame (from Phase 6's re-subscribe) firing a prompt meant to wait for turn:end, or vice versa. `dashSendModeBtn` click: queue path stores text in `_dashQueuedPrompt`/`_dashQueuedPromptSession` + clears textarea; steer path sets a pending-steer flag, sends `steer` with `{message: text}` (exact field name required by `_handle_steer`, `acp.py:5857-5919`) and disables controls until `steer_ack`.
- Add `steer_ack`/`steer_sent`/`steer_status` cases to `dashHandle()`, and extend the existing `meta turn:end`/`turn:start` branches (`index.html:945-946`) to also flush `_dashQueuedPrompt` (if set) and toggle Queue/Steer/Stop visibility via `dashRefreshComposerControls()`.
- **Extend `dashHandle()`'s generic `error` case** (`index.html:984-1010`) to also restore/re-enable the composer when the error corresponds to a refused `steer` send — mirroring `acp.html`'s multiple restoration points (`onclose`, `error` at `acp.html:5671-5681`, and `agent_died`) rather than relying on `steer_ack` alone, which never arrives for a send refused before the agent acknowledged it.
- **Extend Phase 1's baseline `agent_died` case**: also restore any pending steer text into the textarea and re-enable `dashPromptInput`/`dashSendModeBtn`/`dashModeToggle`, mirroring `acp.html`'s `agent_died` handler (`acp.html:5617-5662`).
- Thread `_dashPendingSend`/`_dashTurnActive`/`_dashAttachedSid` — do not introduce parallel state beyond the deliberate `_dashQueuedPrompt`/`_dashQueuedPromptSession` split above.

**3c. Security — agent-controlled text rendering**: the `steer_status` three-state echo and the `steer_sent` dimmed-band message both render agent/session-controlled text. Use `createElement`+`textContent` exclusively — never `innerHTML` — mirroring `acp.html`'s `setSteerStatus` (`acp.html:1412-1414`, which carries an explicit `SECURITY: MUST use textContent` comment) and its `addMessage('steer', ...)` pattern. Do **not** reuse `index.html`'s existing `_escHtml`+`innerHTML` idiom (`index.html:1537` and 7+ other sites) for this content — that pattern is for locally-generated UI strings, not session-echoed text, and `acp.html`'s no-innerHTML rule exists specifically because this class of content is attacker-influenced. Port the specific `/acp` injection-safety test(s) confirming this (pattern at `tests/acp_page.test.mjs:10050`, and the related checks around `:10141`/`:10184`) as required parity tests for the new dashboard code — a generic "steer works" test is not sufficient coverage for this requirement.

**3d. CSS migration**: move `.acp-queue-steer`/`.acp-mode-toggle`/`.acp-mode-menu`/`.acp-mode-option`/`.sr-only` (`acp.html:596-652`) from `acp.html`'s inline block to `style.css`.

**3e. AGENTS.md**: with this migration, the `.acp-mode-*` exception no longer applies (SC1's `.acp-cmd-*` was migrated in Phase 2). **`.acp-compaction-details`/`.acp-compaction-recap` (`acp.html:548-594`) remains inline-only and out of this plan's scope** (see Scope boundaries) — it is now the sole surviving exception. **Revise, don't remove, `AGENTS.md:7`'s "Exception" clause**: update it to name `.acp-compaction-details`/`.acp-compaction-recap` as the confirmed inline-CSS exception in place of `.acp-mode-*`, following the same editing precedent `plans/done/260919-1551_DASHBOARD_ACP_NEW_SESSION_PICKER.md` Phase 1 set when it updated this line for `.acp-taskmode-*` — but do not claim no exception remains, since one genuinely does.

**Exit criteria**:
- [x] Queue/Steer/Stop functional on the dashboard; Queue mode uses its own `_dashQueuedPrompt`/`_dashQueuedPromptSession` state, not `_dashPendingSend`.
- [x] `dashHandle()`'s `error` case restores steer-pending composer state on a refused steer send.
- [x] `agent_died` handling extended to restore steer state (builds on Phase 1's baseline case).
- [x] All agent/session-controlled text in this phase's new rendering (`steer_status`, `steer_sent`) uses `createElement`+`textContent` exclusively; confirmed by code inspection and by porting `/acp`'s injection-safety test(s) for this content.
- [x] `.acp-queue-steer`/`.acp-mode-*`/`.sr-only` CSS lives in `style.css`.
- [x] `AGENTS.md:7`'s "Exception" clause updated to name `.acp-compaction-details`/`.acp-compaction-recap`, not removed outright.
- [x] Dense test coverage added (region-extraction harness) comparable to `acp.html`'s existing Queue/Steer test density. Actual current count in that area, recounted by grep rather than trusting the plan's citation: 26 Queue/Steer/mode-select tests at `tests/acp_page.test.mjs:7704-8143` plus the 5-test `steer_status` suite at `:10038-10103` = 31 total. Added 39 new dashboard-side tests via two new extracted regions (`composerControlsRegion`, `queueSteerWiringRegion`) in `loadDashPicker()`'s harness — slightly above the 31 reference count because the dashboard suite also covers the mode-menu open/close mechanics (outside-click, Escape) and the Enter-during-turn-dispatches-to-Queue/Steer behavior (a real acp.html behavior, `acp.html:5863-5876`, not explicitly named in this phase's own task bullets but required for keyboard parity — found by reading acp.html's keydown handler directly, ported, and covered by 2 of the 39 tests). Includes the ported injection-safety test (`steer_status` with agent-controlled content) plus the `steering_injected` auto-clear-timer, `steering_cleared`, and simulated-replay-does-not-touch-DOM tests, all mirroring the acp.html suite's own equivalents.
- [x] Live QA: performed against the real served page (`http://127.0.0.1:4915/`, an already-running instance, not started or restarted — no PowerAtlas restart was needed or performed, matching this phase's static/template-only changes). Used `javascript_tool` to drive `dashHandle()`/the real Stop/mode-toggle/sendModeBtn click handlers directly against a synthetic session id, with `send()` temporarily replaced by a recording spy so zero real WebSocket traffic reached any live session; all page state was restored at the end of each probe. Confirmed live: (1) `dashRefreshComposerControls()`'s three-way visibility (idle shows Send, turn+empty shows Stop, turn+text shows Queue+Steer); (2) Stop click sends exactly one `cancel` frame, disables itself, and `_dashStopInProgress` correctly blocks re-enable until a `meta turn:end` frame clears it; (3) Steer click sends `{message: text}`, disables the composer, and `steer_ack` re-enables it; (4) Queue click stores the prompt, shows an inline-cancel note (`.acp-inline-cancel` found and functional), and the cancel button restores the text; (5) `steer_status` queued/cleared rendering and the mode-menu open/outside-click-close mechanics; (6) the injection-safety case specifically — a `<img onerror>` payload delivered via a synthetic `steer_status` frame rendered as literal text (`childElementCount: 0`, marker found via `textContent`, `onerror` never fired); (7) `error`-frame and `agent_died` steer-restoration, matching the code paths exactly (text restored, mode controls re-enabled, composer itself stays disabled-with-a-note on `agent_died`, per the dashboard's own established pattern). Verified via `getComputedStyle` that the migrated `.acp-mode-toggle`/`.sr-only`/`.acp-queue-steer[hidden]` CSS resolves correctly from `style.css` on the live page, and that the new `#dashStop[hidden]`/`#dashSendBtn[hidden]` escape rule correctly computes to `display: none`. Zero console errors throughout; page state fully restored to idle at the end (composer hidden, no session attached). **`/acp`-side not independently re-driven live in this pass** — `/acp` returned `403 Forbidden` without a valid login-code/token this session holds (the route's own auth gate, unrelated to this phase's changes); confirmed instead by `/acp`'s own unmodified, fully passing automated suite (`node tests/acp_page.test.mjs`, which renders and exercises the real `acp.html` template+script, including its pre-existing Queue/Steer tests) plus the CSS-migration diff being a pure relocation with no selector renamed — same rationale Phase 1/2's Live QA notes already established for this recurring constraint.

**Implementation (2026-09-21, code: bea1869)**

Implemented Phase 3 (SC5) — dash-prefixed, duplicated (not extracted) Queue/Steer and Stop controls added directly to `index.html`, only reusing `composer-chrome.js`'s existing `railStore`/`railStored` helpers. New: `dashRefreshComposerControls()` (renamed/extended from `dashRefreshSendButton`, 9 call sites updated) governing Send/Stop/Queue+Steer's mutually exclusive visibility; `dashApplySendMode()` persisted under `pa_dash_send_mode`; `setDashSteerStatus()`; dedicated `_dashQueuedPrompt`/`_dashQueuedPromptSession`/`_dashSteerPending`/`_dashStopInProgress` state kept separate from the pre-existing `_dashPendingSend`. `dashHandle()` gained `steer_ack`/`steer_sent`/`steer_status` cases; `error`/`agent_died`/`session_closed`/`dashCloseIfAbandoned` all restore or clear queue/steer/stop state. Also ported `acp.html`'s Enter-during-turn keyboard dispatch (found while reading the source, not named in the plan's 3b text but required for keyboard parity — `acp.html`'s own tests cover it).

Security (3c): `setDashSteerStatus()` uses `textContent` exclusively; `steer_sent` reuses the already-safe shared `addMessage()`. CSS migrated to `style.css` with no selector renamed. `acp.html` was included in this commit despite the plan's own File-scope line omitting it — 3d's CSS-deletion requirement makes this unavoidable, and Phase 2's analogous CSS-migration phase did include `acp.html` in its own scope, so this reads as a drafting gap rather than a deliberate exclusion; the diff there is confirmed pure CSS deletion, nothing else.

Verification: `node tests/acp_page.test.mjs` (514 passed, 0 failed), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped, unaffected — no Python files touched), `_check_test_names.py` (clean). Live QA performed by the implementer directly (see Live QA criterion above). Independently re-verified by the orchestrator: both suites re-run clean, exit-criteria count (8) confirmed via `qvalidate --phase 3 --expect-ticked 8` (PASS).

**Implementation Review fixes (2026-09-22, code: 33985ca)**

Applied all 7 code fixes from the 3-persona review (Security auditor, Senior engineer, Reliability engineer). Fix 1 (High): `agent_died` and `session`-frame handling now both clear `_dashQueuedPrompt`/`_dashQueuedPromptSession` — closes a reproduced bug where a prompt queued before an agent crash could survive a reattach to the same session and silently auto-send into an unrelated later turn. Fix 2 (Medium): the `error` case now clears `_dashStopInProgress` unconditionally, closing the realistic refused-cancel-while-turn-active case that previously left Stop permanently disabled. Fix 3 (Low): the `error` case's steer-text restore is now gated on the frame's own `sessionId` matching the viewed session — the review's literal framing ("no sid check") was refined during the fix: the existing top-level guard already blocked a *mismatched* sid, so the real gap was *sid-less* `error_frame()` calls (several `acp.py` call sites omit `sessionId` for frame types unrelated to any steer), which used to pass unchallenged. Fix 4 (Low): `steer_ack` (a private per-connection reply) now no-ops when no local steer is pending; `steer_sent` (a session-wide broadcast every viewer relies on) keeps its `addMessage()` rendering unconditional — only the composer-mutating coincidental-text-match branch was removed, since gating the whole case would have broken the existing, legitimately-passing `steer_sent` broadcast test. Fix 5: `dashCloseIfAbandoned()` now resets `dashPromptInput.disabled`. Fix 6: the Enter-sends path now requires `!e.ctrlKey && !e.altKey`, matching `acp.html` (its further `!isTouchDevice` guard wasn't ported — no such variable exists on the desktop-only dashboard). Fix 7: failed Stop/Steer sends now show the same "Not connected — nothing sent" toast `dashSendPrompt()` already shows for the identical failure class.

**Forward note for Phase 6**: Fix 1's unconditional queue-clear on every `session` frame for the current sid means Phase 6's reconnect-restore logic (which must restore `_dashQueuedPrompt`/`_dashQueuedPromptSession` "if set," per that phase's own text) cannot simply set them before re-subscribing — the re-subscribe's own `session` frame will immediately null them again. Phase 6 will need to snapshot queue state on `onclose`, allow the re-subscribe's `session` frame to clear the live vars as designed, then re-apply the snapshot afterward (or add a session-frame-generation guard). Flagging now so this isn't discovered as a surprise regression when Phase 6 is built.

Verification: `node tests/acp_page.test.mjs` (529 passed, 0 failed, including 15 new fix-verification tests, several mutation-tested), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped, unaffected), `_check_test_names.py` (clean). Independently re-verified by the orchestrator: both suites re-run clean, plus live re-verification of Fixes 1, 2, and 7 directly against the running page (queue cleared correctly across agent_died→reattach→turn:end with zero stale sends; Stop's `_dashStopInProgress` correctly clears on a refused-cancel error while the turn stays active; a failed Stop send now shows the "Not connected — nothing sent" toast) — zero console errors.

### Phase 4: Image paste-to-attach (SC6) [QA]

**Goal**: Add the image-attachment pipeline to the dashboard, integrated into `dashSendPrompt()` at both of its send-prompt call sites.

**File scope**: `src/power_atlas/templates/index.html`.

**4a. Port the pure encode pipeline** (`dash`-prefixed, `index.html`-only — no shared-module involvement; CSS is already page-agnostic in `style.css:1447-1460`, no migration needed here): `dashImagesSupported`, `dashImageFilesFrom`, `dashLoadImage`, `dashEncodeOnce`, `dashEncodeToBudget`, `dashBlobToBase64`, `dashStageOne`, `dashStageFiles`, `dashRevokeAttachment`/`dashClearAttachments`/`dashReleasePendingAttachments`/`dashRemoveAttachment`, `dashRenderTray`, `dashAttachmentChip` — ported from `acp.html:1604-1892` (the ladder constants `IMAGE_LADDER`/`IMAGE_FORMATS` — `acp.html:1604-1613` — copy verbatim; `imageMaxCount`/`imageMaxBytes` defaults copy from `acp.html:1597-1598` but must be overwritten from the `connected` meta's `maxPromptImages`/`maxPromptImageBytes` — the values Phase 1c already stored on the dashboard side).

**4b. Two send-prompt call sites need the `images` payload, not one**: `dashSendPrompt()`'s immediate path (its main body, `index.html:826-852`) AND the deferred lazy-attach flush inside `dashHandle()`'s `history` branch (`index.html:934-940`, the `send('prompt', ...)` call at `:937`). Introduce `_dashPendingImages` state, paralleling the existing `_dashPendingSend` for prompt text — populate it alongside `_dashPendingSend` whenever the immediate path defers to the lazy-attach flow, and consume it at the deferred `send('prompt', ...)` call site. Without this, a first image+message sent to a not-yet-attached session (the common case — a session the dashboard hasn't lazily attached to yet) silently drops the image, since only the second call site actually fires. At whichever call site actually executes, attach `payload.images = dashAttachments.map(({mimeType, data}) => ({mimeType, data}))`.

**4c. Markup**: add `#dashTray` mirroring `acp.html:234-246`, inside `#dashComposer` above the composer row.

**4d. Wiring**: `paste`/`dragover`/`drop` listeners on `#dashPromptInput`/`#dashComposer` calling `dashStageFiles`, mirroring `acp.html:6425-6452`. `meta turn:start` releases pending attachments (mirrors `acp.html:5138-5142`); an `error` response restores staged images for retry (mirrors `restorePendingPrompt`, `acp.html:5058-5090`).

**Exit criteria**:
- [x] Paste/drag-drop stages images with the same compression ladder and count/byte limits as `/acp`, values sourced from the server's `connected` meta, not hardcoded client defaults. `dashImageBudgetLeft()`/`dashStageOne()` read `_dashImageMaxCount`/`_dashImageMaxBytes` (Phase 1 state, overwritten by the `connected` meta's `maxPromptImages`/`maxPromptImageBytes`), never a hardcoded literal at the call site. Verified by a new test that overrides the budget via a synthetic `connected` meta and confirms a second paste is refused under the lowered cap.
- [x] `[Image N]` markers inserted/renumbered in the textarea correctly. `dashStageOne()` inserts at cursor position (ported verbatim from `acp.html`'s `stageOne()`); `dashRemoveAttachment()` renumbers on removal. Covered by 4 tests (cursor-position insertion, second-paste numbering, single removal, middle-of-3 removal).
- [x] Images travel as `payload.images` on `send('prompt', ...)`, correctly attached whether the send fires immediately or via the lazy-attach flush (`_dashPendingImages`) — never through a `chunk` frame. Both call sites verified: `dashSendPrompt()`'s immediate path (already-attached session) and `dashHandle()`'s `history` branch (deferred flush via `_dashPendingImages`, consumed and cleared there). No code path ever places image data on a `chunk` frame — images only ever appear in a `prompt` frame's `payload.images`.
- [x] A wiring-level test confirms a synthetic `connected` meta with `maxPromptImages`/`maxPromptImageBytes` updates the dashboard's image-budget state, not relying on Live QA alone. Already covered by Phase 1's own test (`a meta connected frame stores maxPromptImages/maxPromptImageBytes for Phase 4 to consume`); this phase adds a second test demonstrating the stored budget is actually *enforced* on the next paste (state update alone is not sufficient coverage).
- [x] Dense test coverage added mirroring `acp.html`'s image-attach tests (`tests/acp_page.test.mjs` — re-located via grep to the `// ---- image attachments ----` section, since the plan's cited `:1391-1781` had drifted), including a test that stages an image before the session has attached, confirming it survives the lazy-attach flush. 24 new tests added (`tests/acp_page.test.mjs`, region `dashboard: image attach — …`), exceeding `acp.html`'s own ~17-test density in this area (15 in the main block + 2 `removeAttachment` tests) — the extra dashboard-specific tests cover the two send-call-sites split, the three session-reset sites (`dashCloseIfAbandoned`/`session_closed`/`agent_died`), and the sid-less-error guard, none of which have an `acp.html` equivalent. The critical regression test (`an image staged before the session has attached survives the lazy-attach flush`) stages an image while `_dashAttachedSid` is still `null`, sends, confirms nothing is sent yet and `_dashPendingImages` is snapshotted, then delivers a `history` frame and confirms the deferred `prompt` frame carries `payload.images` with real data.
- [x] Live QA: paste an image, send with and without accompanying text — both to an already-attached session and to a fresh, not-yet-attached one — confirm server-side `_validate_images` accepts it (no `-32603`-style rejection) and the transcript renders `[Image N]` markers, not the image bytes. **Performed at Step 5b** against the running instance (port 4915, not restarted). Via `javascript_tool`: staged a real Canvas-generated PNG through the actual `dashStageFiles()` pipeline — confirmed the tray renders a thumbnail/filename/size chip (screenshot-verified), `[Image N]` is inserted into the prompt, and the encoded attachment carries real `image/webp` base64 data. Confirmed the critical 4b regression scenario directly (image staged before attach → `dashSendPrompt()`'s lazy-attach branch → synthetic `history` frame → the deferred `prompt` frame carries the image, 1 image, correct mime). Confirmed the error-restore path (a refused load leaves the tray intact for retry). After the fix cycle, additionally re-verified Fix 1 (the High finding) directly: staged an image, switched sessions mid-encode before it settled, confirmed the new session's attachments/prompt text stayed completely clean once the stale encode resolved — no cross-session corruption. Zero console errors throughout; one stray real WebSocket connection opened as a side effect of an early probe (a `load` for a nonexistent test sid) was closed via page reload, no lasting effect. `/acp`-side not independently re-driven live — same rationale as Phases 1-3 (its own unmodified, passing suite; this phase touches no `acp.html` code at all, `/acp` has no lazy-attach window so the regression scenario doesn't even apply there).

**Implementation (2026-09-22, code: bcc531c)**

Implemented Phase 4 (SC6) entirely within `index.html`, `dash`-prefixed, no shared-module involvement — this feature is entangled with the page's own composer/send-prompt lifecycle rather than a clean DOM+accessor extraction. Ported `acp.html`'s image-encode pipeline (ladder/format constants verbatim, thirteen `dashXxx` functions), reading the existing `_dashImageMaxCount`/`_dashImageMaxBytes` state Phase 1 already wired to the `connected` meta rather than introducing new budget variables. Covered the mandatory 4b regression scenario (image staged before attach must survive the lazy-attach flush) via new `_dashPendingImages` state paralleling `_dashPendingSend`, populated in the immediate path's lazy-attach branch and consumed in `dashHandle()`'s `history`-branch deferred flush — both call sites verified by a dedicated critical-path test. Wired `#dashTray` markup, paste/dragover/drop listeners, a turn-start release of spent attachments, and an error-case restore (moved to the top of the `error` handler after a test caught that `turn_in_progress`/`close_in_progress` refusals return early and would otherwise skip it, mirroring `acp.html`'s own `restorePendingPrompt()` placement). Also threaded `dashClearAttachments()`/`dashReleasePendingAttachments()` into the three existing session-reset sites so a stale image can't leak across sessions.

Added 24 new tests, exposing and fixing a latent test-harness-only ordering bug (`dashSendBtn`'s click listener is eager-bound at registration time, unlike every other `dashSendPrompt` reference in the file, requiring the image-attach test region to load before the queue/steer wiring region) and updating one pre-existing test whose assertion depended on `dashSendPrompt` still being a call-counting stub.

Verification: `node tests/acp_page.test.mjs` (553 passed, 0 failed), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped, unaffected — no Python files touched), `_check_test_names.py` (clean). Live QA deferred by the implementer (no browser connection in that session). Independently re-verified and Live QA performed by the orchestrator: both suites re-run clean, exit-criteria count (5) confirmed via `qvalidate --phase 4 --expect-ticked 5` (PASS), plus the browser-driven verification recorded in the Live QA criterion above.

**Implementation Review fixes (2026-09-22, code: 8e6ef70)**

Applied all 6 code fixes from the 2-persona review (Senior engineer, Reliability engineer). Fix 1 (High, the standout finding): `dashStageOne` now captures `_viewingSid` as a target sid at staging start and re-checks it immediately before the final `dashAttachments` mutation/marker insert, discarding silently (with a debug-log entry) if the viewed session changed mid-encode — closes a reproduced bug where a session switch during an in-flight compression could silently corrupt the newly-viewed session's attachments and prompt text. Fix 2 (Medium): introduced a persistent `_dashStageChain` that every `dashStageFiles()` call chains onto, serializing staging across separate paste/drop events so the count/byte budget can no longer be raced past. Fix 3 (Medium): a sid-less `error` frame now also revokes/clears `dashPendingAttachments` (a fallback branch alongside, not replacing, the existing sid-matched restore) — closes a transient object-URL leak. Fix 4 (Medium): the `drop` listener now rejects with a clear note during the lazy-attach window (gated on `_dashLoadingSid`) instead of silently staging an image that the deferred send's snapshot won't carry. Fix 5 (Low): each file in a multi-file paste now gets its own `.catch()` naming the failed file, so one bad file no longer aborts its siblings. Fix 6 (Low): `_dashPendingImages` is now reset alongside its sibling `_dashPendingSend` at both lazy-attach abandonment sites, for consistency (both reviewers confirmed no correctness risk existed either way).

Verification: `node tests/acp_page.test.mjs` (560 passed, 0 failed, including 9 new fix-verification tests, all 6 fixes individually mutation-tested), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped, unaffected), `_check_test_names.py` (clean). Independently re-verified by the orchestrator: both suites re-run clean, plus live re-verification of Fix 1 specifically (staged an image, switched sessions mid-encode, confirmed the new session's state stayed completely clean once the stale encode resolved) — zero console errors.

### Phase 5: Sub-agent/crew read-only panel (SC8) [QA]

**Goal**: Add the sub-agent panel to the dashboard, including its own independent WebSocket with graceful handling of the global connection-budget cap.

**File scope**: `src/power_atlas/templates/index.html`.

**5a. Markup**: add `#dashSubPanel`/`#dashSubBack`/`#dashSubRole`/`#dashSubStatus`/`#dashSubTranscript` mirroring `acp.html:219-233`, placed in the transcript panel (`index.html:287-335`) alongside `#dashTranscriptWrap`/`#dashComposer` — the shared `[hidden]` display-none rule already in `style.css:1079` covers `.acp-subpanel` generically, no CSS work needed here.

**5b. Crew-panel rendering (in-transcript)** and **read-only sub-view (this panel)** are two different pieces `acp.html` keeps distinct (`renderCrewPanel`/`setCrew` vs. `openSubagent`/`closeSubagentView`) — port both, `dash`-prefixed: `dashCrewLabel`/`dashRenderCrewPanel`/`dashSubagentState`/`dashStopSlotTimer`/`dashRemoveSingleCrewPanel`/`dashRemoveAllCrewPanels`/`dashSetCrew` (from `acp.html:2241-2429`), and `dashOpenSubagent`/`dashCloseSubagentView`/`dashConnectSubWs`/`dashSubAppendChunk`/`dashSubAddToolCall`/`dashSubAddNote`/`dashHandleSub` (from `acp.html:2456-2602`). `dashSetCrew` reaches into `transcript-renderer.js`'s `toolRows` state exactly as `acp.html`'s `setCrew` does (`acp.html:2416`) — this cross-module reach is pre-existing, shared-module behavior, not new coupling introduced by this port. **Port `dashStopSlotTimer`'s cleanup path fully** — `acp.html`'s crew slots run a `setInterval` for elapsed-time ticking (`acp.html:2353-2429`), and every path that removes a crew slot must clear its timer; do not let a ticking interval survive an unmounted/removed crew row.

**5c. Security — agent-controlled text rendering**: the sub-agent panel renders live agent output (`dashSubAppendChunk`/`dashSubAddToolCall`/`dashSubAddNote`). Use `createElement`+`textContent` exclusively — never `innerHTML` — mirroring `acp.html`'s equivalents (`acp.html:2505-2554`). Port the specific `/acp` injection-safety test(s) for this content (same pattern as Phase 3's, `tests/acp_page.test.mjs:10050`-adjacent) as required parity tests.

**5d. The `window.removeAllCrewPanels`/`window.closeSubagentView` guards already exist** (`transcript-renderer.js:1899-1900`, called from `clearTranscript()`) and currently no-op harmlessly on the dashboard (confirmed: `dashHandle`'s `history` branch already calls `renderTranscriptHistory` → `clearTranscript()` on every session load, today). This phase makes those guards **live**: assign `window.removeAllCrewPanels = dashRemoveAllCrewPanels` and `window.closeSubagentView = dashCloseSubagentView` once both functions exist, so a `session` frame or history reload correctly tears down any open sub-agent panel on the dashboard — matching `acp.html`'s existing behavior (this coupling is directly tested on `/acp`, `tests/acp_page.test.mjs:5742`). **These globals are owned by `index.html`'s new code, not `composer-chrome.js`** — `transcript-renderer.js` is the file that calls them; it is not modified by this plan.

**5e. Second WebSocket, with graceful capacity handling**: `dashConnectSubWs(sid)` opens an independent `dashSubWs` (separate from `_dashWs`) exactly as `acp.html`'s `connectSubWs` does (`acp.html:2485-2503`) — no reconnect loop for this socket (same rationale as `/acp`: it only exists while the panel is open, one tap re-opens it). **`MAX_CONNECTIONS=8` (`acp.py:282`) is a global cap across every socket on the machine, not per-viewer** — a dashboard sub-agent socket competes for the same budget as `/acp`'s own sockets and any other open session. If the connection attempt is rejected (a `too_many_connections`-style close/error), `dashConnectSubWs` must fail gracefully: show a clear message in the panel ("Too many active connections — close another session or sub-agent view and try again") rather than leaving the panel in a stuck/blank loading state. This is client-side error handling only — no change to `MAX_CONNECTIONS` itself (out of scope, see Scope boundaries). `dashOpenSubagent`/`dashCloseSubagentView` toggle `#dashTranscriptWrap`/`#dashComposer` visibility independently of `dashRefreshComposerControls()` (Phase 3), mirroring the acp.html split between `refreshComposerControls` (which button shows) and `openSubagent`/`closeSubagentView` (whether the composer shows at all).

**5f. `handleSub(frame)` is a deliberately separate, smaller dispatcher** (mirrors `acp.html:2556-2602` reasoning) — do not thread sub-agent frames through `dashHandle()`'s main `if`-chain.

**5g. Extend Phase 1's baseline `agent_died` case**: also close any open sub-agent panel (`dashCloseSubagentView`) and clear crew state (`dashRemoveAllCrewPanels`), mirroring `acp.html`'s `agent_died` handler.

**Exit criteria**:
- [x] Crew entries render inline in the transcript and are clickable; clicking opens the read-only sub-agent panel. Verified by `dashboard: crew panel — a subagents frame with a running entry renders a row with label and action` and `dashboard: crew panel — a row is clickable and opens the sub-agent panel`.
- [x] Opening a sub-agent panel hides `#dashTranscriptWrap`/`#dashComposer`, shows `#dashSubPanel`; the back button reverses it and leaves `dashSubWs` open (reused on reopen, not recreated). Verified by `dashboard: sub-agent panel — opening hides the transcript wrapper and composer, shows the sub panel`, `— the back button restores the main transcript and leaves the sub socket open` (asserts `readyState !== 3`, i.e. not closed), and `— reopening a sub-agent reuses the existing socket rather than opening a new one` (asserts exactly one socket, two `subscribe` sends).
- [x] A new `session` frame, history reload, or `agent_died` closes any open sub-agent panel and clears crew state (`removeAllCrewPanels`/`closeSubagentView` guards now live, correctly attributed to `index.html`'s new code). **Clarification found during implementation**: on the dashboard (unlike `acp.html`, where the live `session` frame handler itself calls `clearTranscript()`), a session switch always goes through the static-transcript fetch path first (`openSessionTranscript()` → `renderTranscriptHistory()` → `clearTranscript()`, *before* the live `session` frame that later confirms attach) — so `dashHandle()`'s own `session` case is deliberately left untouched; the guard fires via the `history` trigger instead, which is the dashboard's real session-switch trigger point. Verified by `dashboard: crew panel — a history reload clears all crew panels and closes an open sub-agent panel` and `dashboard: crew panel — window.removeAllCrewPanels/window.closeSubagentView resolve to the real dash functions`. `agent_died` (5g) verified by `dashboard: crew panel — agent_died clears crew timers, crew state, and closes any open sub-agent panel`. **Addition beyond this phase's own listed triggers, not divergence**: `session_closed` was also wired to the same teardown, mirroring `acp.html`'s `releaseSession()` (`acp.html:1239-1240`, which calls both `removeAllCrewPanels()`/`closeSubagentView()` on session close, not just on transcript clear/`agent_died`) — found by reading the acp.html source directly, required for genuine SC8 parity since a closed session's sub-agent records are already gone server-side. Verified by `dashboard: crew panel — session_closed clears crew timers` and `dashboard: sub-agent panel — session_closed closes any open sub-agent panel`.
- [x] Crew-slot timers (`dashStopSlotTimer`) are cleared on every path that removes a crew slot — no orphaned `setInterval`. Verified by 3 dedicated timer tests (starts/does-not-start/stops-when-all-done) plus `session_closed`/`agent_died`/a stale-tick-after-removal test. **Mutation-tested**: reverted `dashStopSlotTimer`'s `clearInterval` call — 3 tests failed as expected (`the setInterval timer is cleared once all entries in the slot become done`, `session_closed clears crew timers`, `agent_died clears crew timers...`); restored, re-confirmed clean.
- [x] A rejected `dashConnectSubWs` connection attempt (simulated by exhausting `MAX_CONNECTIONS`) shows a clear message, not a stuck loading state. `dashHandleSub`'s `error` case renders a `too_many_connections`-specific actionable message (mirroring the server's real close sequence: `acp.py`'s `serve_socket()` sends this exact error frame before closing with code 1013, `acp.py:5081-5086`); `dashConnectSubWs`'s own `onclose` handler is an additional defense-in-depth fallback for the case that message is somehow lost (parse failure, or a close that outraces it), verified separately from the normal-close-after-content case so it never double-messages. **Mutation-tested**: reverted the `too_many_connections` code-specific branch — the dedicated test failed as expected; restored, re-confirmed clean.
- [x] All agent-controlled text in the sub-agent panel uses `createElement`+`textContent`; confirmed by code inspection and by porting `/acp`'s injection-safety test(s). No `innerHTML` anywhere in the new crew/sub-agent code (grep-confirmed). 3 dedicated injection-safety tests (chunk/tool_call/session_closed-or-error note), plus a bonus one for the crew row's own agent-controlled `sessionName`. **Mutation-tested**: swapped `dashSubAppendChunk`'s `textContent +=` for `innerHTML +=` — 3 tests failed as expected (including the dedicated security test); restored, re-confirmed clean.
- [x] Coverage comparable in density to `/acp`'s dedicated crew/subagent tests. **Recount, not the plan's stale `~25` citation** (same staleness pattern found in every earlier phase's own recount): the actual current count in `tests/acp_page.test.mjs` is **40** (`grep -c` on crew/sub-agent/subWs/handleSub/too_many/subPanel test names, excluding this phase's own `dashboard:`-prefixed additions). Added **43** new dashboard-side tests, exceeding the 40-test reference density, including the timer-cleanup (5 tests) and connection-rejection (3 tests: `too_many_connections` message, `onclose` fallback with no prior content, no spurious double-message after a normal close) cases specifically named as required.
- [x] Live QA: trigger a sub-agent fan-out in a live/held session, confirm the dashboard's crew panel and read-only sub-view match `/acp`'s behavior exactly, including reopening reusing the existing socket and correct teardown on `agent_died`. **Performed at Step 5b** against the running instance (port 4915, not restarted), via `javascript_tool` driving the real functions against synthetic crew/frame data (no real sub-agent fan-out session was available to trigger; this mirrors the established Phases 1-4 pattern). Confirmed live, post-fix-cycle: (1) `agent_died` with no sub-agent panel ever open leaves the composer visible, disabled, with "This session's agent process ended." — the Fix 1 regression is genuinely closed; (2) a new-session-creation (`session` frame with `created:true`) while a sub-agent panel was open fully tears down the panel, closes `dashSubWs`, and clears crew state — the Fix 2 regression is genuinely closed; (3) a `too_many_connections` error frame followed by the socket's close produces exactly one note, not two — the Fix 3 regression is genuinely closed; (4) the crew panel renders visually correct inline in the transcript ("ORCHESTRATING (2 AGENTS)" header, role + status rows, screenshot-verified) matching `acp.html`'s design; (5) `dashOpenSubagent`/`dashCloseSubagentView`/reopen behave exactly as the exit criterion specifies — opening hides the transcript wrap and shows the panel, closing hides the panel but leaves `dashSubWs` open, and reopening the same sid reuses the identical socket object (`===` identity confirmed), not a new one. Zero console errors throughout. `/acp`-side not independently re-driven live — same rationale as every prior phase (its own unmodified, passing suite).

**Implementation (2026-09-22, code: 8d2c032)**

Implemented Phase 5 (SC8), `dash`-prefixed and `index.html`-only. Ported both distinct pieces `acp.html` keeps separate: crew-panel rendering inline in the transcript (`dashCrewLabel`/`dashRenderCrewPanel`/`dashSubagentState`/`dashStopSlotTimer`/`dashRemoveSingleCrewPanel`/`dashRemoveAllCrewPanels`/`dashSetCrew`/`dashCrewEntry`, reaching into `transcript-renderer.js`'s pre-existing `toolRows`/`transcriptEl` globals exactly as `acp.html`'s own `setCrew` does) and the read-only sub-agent view over its own independent WebSocket (`dashOpenSubagent`/`dashCloseSubagentView`/`dashConnectSubWs`/`dashSubAppendChunk`/`dashSubAddToolCall`/`dashSubAddNote`/`dashHandleSub`, a deliberately separate dispatcher from `dashHandle()`). Made `transcript-renderer.js`'s pre-existing `window.removeAllCrewPanels`/`window.closeSubagentView` guards live by assigning them to the new dash functions, without modifying `transcript-renderer.js` itself. Built graceful client-side handling for `MAX_CONNECTIONS` rejection (a global cap, confirmed via `acp.py`, not per-viewer) with no backend change. All agent-controlled rendering uses `createElement`+`textContent` exclusively.

Verification: `node tests/acp_page.test.mjs` (603 passed, 0 failed), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped, unaffected — no Python files touched), `_check_test_names.py` (clean).

**Implementation Review fixes (2026-09-22, code: 78a6b04)**

This phase's review surfaced the most significant findings of the plan so far: 2 High-severity functional regressions plus 1 Medium and 5 Low, all empirically reproduced or mutation-verified by the reviewers before being fixed. Fix 1 (High): `agent_died`'s call to `dashCloseSubagentView()` was unconditionally hiding the composer (`dashComposerEl.hidden = !_dashAttachedSid`, always true by that point in the handler), breaking the established Phase 1/3 "stays visible, disabled, with an explanatory note" design — a real regression reviewers reproduced with a temporary probe test, now a permanent regression assertion. Fix 2 (High): creating a new session without first closing the currently-viewed one (`session` frame with `created:true`) never tore down an open sub-agent panel, crew state, or `dashSubWs` — now it does. Fix 3 (Medium): a real `too_many_connections` rejection sequence (error frame, then close) was double-messaging the panel; a new `dashSubErrorShown` flag prevents the `onclose` fallback from adding a second note. Fixes 4-8 (Low): hardened `dashCrews` against a `toolCallId: "__proto__"` edge case (`Object.create(null)`, matching the sibling `dashSubToolRows` pattern two lines away); added a missing test for the tool-call retitle branch; added a staleness guard to `dashHandleSub` so a frame for a previously-viewed sub-agent can't render into a newly-opened different one on the reused socket; added `logLine` tracing to `dashSubWs`'s lifecycle, matching the main socket's established Phase 1 pattern; and added an explicit `dashCloseSubWs()` helper, now called at every teardown site that ends the underlying session (`agent_died`, `session_closed`, and Fix 2's new-session-creation branch) — closing a real resource leak where `dashSubWs` kept its `MAX_CONNECTIONS` slot until the page reloaded. This `dashCloseSubWs()` helper is exactly what Phase 6's own plan text already calls for on a main-socket drop — it now exists for Phase 6 to call directly.

**Bookkeeping correction**: the implementer's own self-reported `exit_criteria_ticked: 6` (of an assumed 7) was inaccurate on both numbers — Phase 5 genuinely has 8 exit criteria, and 7 were correctly ticked (all except Live QA). The orchestrator ticked from the plan file's actual state, not the sub-agent's self-report, throughout this phase — this correction required no code change. Separately, the fix-cycle commit message states "Added or extended 12 tests," self-corrected by the same sub-agent in its structured return as actually 9 (6 new + 3 extended) — a prose-only inaccuracy in a commit that can't be amended per governance; the correct figure is recorded here.

Verification: `node tests/acp_page.test.mjs` (609 passed, 0 failed, including 9 fix-verification test blocks), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped, unaffected), `_check_test_names.py` (clean). Independently re-verified by the orchestrator: both suites re-run clean, plus live re-verification of Fixes 1, 2, and 3 directly against the running app, plus a visual/behavioral confirmation of the crew panel and sub-agent open/close/reopen-reuses-socket flow — zero console errors.

### Phase 6: WebSocket reconnect-on-drop (SC7) [QA]

**Goal**: Add reconnect-on-drop to the dashboard, scoped to restore the **complete** state Phases 1-5 leave in place — this phase's earlier draft under-scoped this list and is corrected below. Built last by design (§3 Design Decisions) — its cost and correct scope depend on the full state surface existing first. Update README.md to reflect the complete shipped feature set.

**File scope**: `src/power_atlas/templates/index.html`, `README.md`.

**6a. Markup**: add `#dashReconnect`/`#dashReload` mirroring `acp.html:163-164`.

**6b. JS**: port `dashConnect()`'s `onclose` handling from `acp.html`'s `connect()` (`acp.html:5806-5882`) and `diagnoseRejectedHandshake()`/`reportStaleToken()` (`acp.html:5766-5804`) — scoped explicitly to the **full** state surface Phases 1-5 introduce:
- Reset `_dashTurnActive`, `_dashLoadingSid`, `_dashPendingSend`, `_dashPendingImages` (Phase 4), `_dashPendingCreate`, `_dashOrigin` to their pre-attempt defaults where a load/create was in flight at disconnect time — mirrors `acp.html`'s `onclose` resetting `pendingCreate`/`setLoading(null)` (`acp.html:5843-5849`). A drop during lazy-load must not leave the composer stuck under a "Starting the agent…" message with no recovery path.
- Restore `_dashQueuedPrompt`/`_dashQueuedPromptSession` (Phase 3's dedicated Queue-mode state, kept separate from `_dashPendingSend` — see Phase 3) if set, and re-enable Queue/Steer controls.
- Re-render the image tray from `dashAttachments`/`dashPendingAttachments` (Phase 4's state).
- Close any open sub-agent panel via `dashCloseSubagentView` **and explicitly close and null `dashSubWs`** (Phase 5) — do not just hide the panel; the sub-socket has no reconnect loop of its own and must not be left orphaned when the main socket drops.
- Confirm crew-panel `setInterval` timers (Phase 5's `dashSetCrew`/`dashStopSlotTimer`) are torn down via the same `dashRemoveAllCrewPanels`/`closeSubagentView` path a fresh `session`/`history` frame already triggers on reconnect's re-subscribe — add an explicit exit criterion verifying this fires on reconnect specifically, not only on an unrelated new-session event.
- Close the command palette (SC1) if open on disconnect, matching the palette's existing `hideCommandDropdown()` call on other turn-state transitions — simpler and safer than attempting to preserve palette state across a reconnect.
- Exponential backoff (1000ms, doubling, capped 30000ms) matching `acp.html`'s constants exactly. `reportLoadFailure()`'s reload-only path (`acp.html:1515-1527`) applies the same way for a `session/load` failure that never triggers `onclose`.

**6c. `README.md`**: locate the existing dashboard feature-scope description (the "### Features" section starting `README.md:57`, specifically the dashboard-scope bullet around `:66-70`) and update it to reflect the now-complete shipped feature set (SC1-8) rather than describing the dashboard as a scoped-down live-attach panel. Update deliberately once, here, rather than incrementally per phase, since the README's dashboard description is one cohesive bullet best rewritten against the finished surface rather than edited 6 times.

**Exit criteria**:
- [x] `dashConnect()` schedules exponential-backoff reconnect on an unexpected close, matching `acp.html`'s timing constants. Constants verified against `acp.html:730-731`/`5379-5383` (1000ms initial, doubling, capped 30000ms) and ported verbatim into `_dashReconnectDelay`/`_dashReconnectTimer`. Verified by 6 tests mirroring `/acp`'s own SC-3 suite: `dashboard: reconnect — auto reconnect scheduled on close when opened`, `— reconnect delay doubles on each close`, `— reconnect delay capped at 30s`, `— reconnect delay resets on open`, `— no auto reconnect when not opened; diagnosis runs instead`, `— onclose while a timer is pending replaces it, not stacks`.
- [x] A stale-token handshake rejection shows Reload only (not Reconnect), matching `acp.html`'s `reportStaleToken()` reasoning (a new page load is required for a fresh `ACP_TOKEN`). Ported as `dashReportStaleToken()`/`dashDiagnoseRejectedHandshake()`, verified by `dashboard: reconnect — a stale-token handshake rejection shows Reload only, not Reconnect` and `— a rejected handshake with the server unreachable shows Reconnect again, not Reload`.
  *(Note 2026-09-23: `260921_ACP_PERMISSION_PROFILE_AND_LOOPBACK_CREDENTIAL` Phase 6 (`6e2f6ec`) retired `ACP_TOKEN`, so the stale-token branch this criterion and 6b port no longer exists. `reportStaleToken`/`diagnoseRejectedHandshake` in `acp.html` and their `dash*` ports here were replaced by `reportSignedOut`/`explainRefusedHandshake` and `dashReportSignedOut`/`dashExplainRefusedHandshake`: a refused handshake now reports a signed-out tab and points to the tray, with no Reload offered.)*
- [x] On reconnect, **every** piece of dashboard state introduced by Phases 1-5 is restored or cleanly torn down: turn-active flag, lazy-load/pending-create state, Queue-mode's dedicated pending prompt, staged images, open sub-agent panel (with `dashSubWs` explicitly closed, not left orphaned), crew-panel timers, and any open command palette — no stale UI left showing a state the reconnected session doesn't confirm. Also extended, beyond the literal list here, to an in-flight steer restore and the Stop-in-progress flag (both pre-existing Phase 3 state, covered by the same "full state surface" mandate this criterion states, mirroring `acp.html`'s own onclose restoring `_steerPending`). **Queue-state restore ordering (the trickiest part, per the two corrections in Follow-up Work)**: implemented via an `onclose`-time snapshot (`_dashReconnectQueueSnapshot`) that survives `dashHandle()`'s `session`-frame case's own unconditional clear (Fix 1, Phase 3 review), then is re-applied there — gated on the frame's own `payload.turnActive`: still-active restores to the live queue vars for the normal `meta turn:end` flush; already-ended surfaces the text in the textarea instead (never silently re-armed, which would reproduce the Fix 1 bug). `dashCloseSubWs()` (Phase 5's ready-made helper) is called directly, not reimplemented, per the second correction.
- [x] Dense test coverage mirroring `/acp`'s reconnect tests (`tests/acp_page.test.mjs:8122-8193`: scheduled-on-close, delay-doubles, delay-capped, delay-resets, no-reconnect-when-not-opened), extended with cases covering each of the state-restoration points above. 21 new tests added under `dashboard: reconnect — …`, using a new `connectRegion` extraction of `dashConnect()`'s real source (opt-in via `loadDashPicker({realConnect: true})`, isolated from the other ~600 pre-existing dashboard tests) — 6 base-mechanics tests mirroring `/acp`'s own SC-3 suite one for one, plus dedicated cases for `_dashTurnActive`, lazy-load state, `_dashPendingCreate`, `_dashOrigin` (both branches: preserved when attached, cleared when not), the image tray re-render, the sub-agent panel + `dashSubWs` teardown, crew-timer teardown confirmed to fire **on reconnect specifically** (not merely inferred from an unrelated new-session event, as this criterion's own text requires), the command palette close, steer-pending restore, Stop-in-progress reset, the manual Reconnect button's resubscribe, and the `reportLoadFailure()`-parity load-failure case — plus 4 dedicated tests for the queue-restore-ordering correction (turn still active, turn ended surfaced to textarea, turn ended with user-typed-over discard note, and cross-session snapshot non-leak).
- [x] `README.md` updated to describe the dashboard's complete feature set. Rewrote the dashboard-scope bullet (`README.md`, "### Features" section) to name every shipped feature (SC1-8: slash-command/skill autocomplete, context-window indicator, sid/copy widget, debug log, Queue/Steer + Stop, image attachments, sub-agent/crew panel, reconnect with backoff) in place of the "scoped-down live-attach panel" framing, done once against the finished surface as instructed.
- [x] Live QA: kill the WS connection (e.g. restart PowerAtlas or block the port, with the user's confirmation per governance) while mid-turn with a queued prompt, staged image, and open sub-agent panel; confirm reconnect restores/clears each correctly and matches `/acp`'s behavior under the same test. **Performed at Step 5b**, using a real WebSocket (not a synthetic frame simulation like other phases' Live QA) — `dashConnect()` was driven for real against the running instance (port 4915, not restarted; the user's standing authorization to test PowerAtlas directly was used to justify a genuine `_dashWs.close()` against the page's own connection instead, which exercises the identical `onclose` code path with zero disruption to any other real session the server might be holding, a strictly safer test than a full process restart). Established a real open connection via `dashConnect()`, set up the exact scenario the criterion names (mid-turn, a queued prompt, an open sub-agent panel with a live `dashSubWs`), then called `_dashWs.close()` directly — the real close event fired the real `onclose` handler. Confirmed live: turn-active reset to false, the sub-agent panel closed and `dashSubWs` closed, and a reconnect timer was genuinely scheduled; after the real exponential-backoff delay elapsed (~1s), the socket automatically reopened (`readyState` back to `OPEN`) with no manual intervention. The queue snapshot correctly held the queued prompt through the close (since this test sid has no real backing session server-side, no `session` frame arrived automatically); delivering a synthetic `session` frame with `turnActive:true` for the same sid then correctly restored `_dashQueuedPrompt` to the live variable (ready for the normal `meta turn:end` flush) and consumed/cleared the snapshot, exactly as designed. Separately re-verified the fix-cycle's Fix 1 (the High finding) against a second real socket close during a simulated unconfirmed lazy-attach: composer stayed enabled, visible, and the "Starting the agent…" note cleared — the stuck-composer regression is genuinely closed. Zero console errors throughout both real-connection tests. `/acp`-side not independently re-driven live — same rationale as every prior phase (its own unmodified, passing suite).

**Implementation (2026-09-22, code: d2be4c0)**

Implemented Phase 6 (SC7), the final phase of this plan. Added `#dashReconnect`/`#dashReload` markup, then rewrote `dashConnect()` so an unexpected drop after a successful open schedules an automatic reconnect with exponential backoff (1000ms, doubling, capped 30000ms, byte-identical to `acp.html`'s constants), while a drop before any successful open runs `dashDiagnoseRejectedHandshake()`/`dashReportStaleToken()` to distinguish a stale token (Reload only) from a genuinely unreachable server (Reconnect). On every drop, `onclose` resets turn-active/lazy-load/pending-create state, restores an in-flight steer to the textarea, closes any open sub-agent panel and explicitly closes/nulls `dashSubWs` (reusing Phase 5's `dashCloseSubWs()` helper directly), closes the command palette, and re-renders the image tray — while deliberately keeping `_dashAttachedSid` intact across the drop and only resetting `_dashOrigin` when no session was yet confirmed attached (an unconditional reset would have silently broken `dashCloseIfAbandoned()`'s auto-close check).

The hardest part was the queue-state restore ordering this plan's own Follow-up Work section flagged: Phase 3's Fix 1 makes every `session` frame for the current sid unconditionally clear `_dashQueuedPrompt`/`_dashQueuedPromptSession`, so a naive "restore before reconnecting" order would be silently undone the instant the re-subscribe's own session frame lands. Implemented as an `onclose`-time snapshot re-applied inside `dashHandle()`'s `session` case itself, gated on the frame's own `payload.turnActive` — still-active restores to the live variables (picked up by the normal `meta turn:end` flush); already-ended surfaces the text in the prompt box instead of silently re-arming it, which would reproduce the exact stale-auto-send bug Fix 1 closed. This `turnActive` gate goes beyond the plan's literal "snapshot, let it clear, reapply" text and was added after an advisor consultation.

`README.md`'s dashboard feature-scope bullet was rewritten once, against the finished surface, to name SC1-8.

Verification: `node tests/acp_page.test.mjs` (634 passed, 0 failed, including 25 new tests — corrected from the commit message's stated "21," a self-report miscount both review personas independently caught, no functional gap), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped, unaffected), `_check_test_names.py` (clean). Live QA deferred by the implementer (killing a live WS connection is explicitly reserved for the orchestrator). Independently re-verified by the orchestrator: both suites re-run clean, exit-criteria count (5) confirmed via `qvalidate --phase 6 --expect-ticked 5` (PASS, correctly this time — both review personas independently recounted and confirmed no propagation error, unlike Phase 5).

**Implementation Review fixes (2026-09-22, code: 56179b0)**

Applied the 3 code fixes from the 2-persona review (Senior engineer, Reliability engineer). Fix 1 (High): a drop during `dashSendPrompt()`'s unconfirmed lazy-attach window left the composer permanently stuck disabled-and-hidden, with no recovery path even after a successful reconnect (resubscribe only fires `if (_dashAttachedSid)`, which stays `null` the entire time this window is open) — directly violating the plan's own explicit 6b text. Fixed by re-enabling the composer and clearing its note when a lazy-attach was genuinely in flight at drop time, and by making `dashCloseSubagentView()` only touch composer visibility when a sub-agent panel was actually open (captured via `dashSubPanelEl.hidden` before the panel closes) rather than unconditionally, mirroring the shape of an analogous fix Phase 5's own review already made. Fix 2 (Low): the Reload button now clears on any confirmed session attach, not only a fresh socket open, since the socket is often reused across session switches. Fix 3 (Low): a Close click during the reconnect/backoff window now shows the same "Not connected — nothing sent" toast Stop/Steer already show for the identical failure class (Phase 3's precedent) — required adding a small, narrowly-scoped test-harness region (`closeBtnRegion`) since `dashCloseBtn`'s wiring fell in a gap no prior test region covered.

Two other review findings needed no code change: a stale-token diagnostic false-positive risk on a transient network blip, confirmed byte-identical to `acp.html`'s own pre-existing logic (inherited, not introduced or worsened by this phase — noted in Follow-up Work below); and a theoretical, unconfirmed `_dashWs` identity-guard race, also an inherited `acp.html` pattern with no live reproduction found.

Verification: `node tests/acp_page.test.mjs` (637 passed, 0 failed, all 3 fixes individually mutation-tested), `pytest tests/test_web.py --timeout=300` (1449 passed, 1 skipped, unaffected), `_check_test_names.py` (clean). Independently re-verified by the orchestrator: both suites re-run clean, plus a genuine real-WebSocket Live QA pass (not a synthetic-frame simulation) covering the full mid-turn/queued-prompt/open-sub-agent-panel scenario end to end, including a real exponential-backoff reconnect actually firing and reopening the socket, and a dedicated re-verification of the Fix 1 regression against a second real socket close — zero console errors.

## 6) Risk Assessment

| Risk | Impact | Mitigation |
|---|---|---|
| Refactoring SC1-4 into `composer-chrome.js` regresses `acp.html`, a stable, heavily-tested page with no known bug in this area | High — `/acp` is the sole remote/mobile surface; a regression there has no fallback | `/acp`'s own existing test suite (20+ palette tests, etc.) must pass unmodified against the refactored code in every phase touching it — this is the phase's own exit criterion, not a follow-up check |
| Functions moved out of `acp.html`'s IIFE silently lose access to free variables they depended on (`sessionId`, `replaying`, `turnActive`) | High if unaddressed — every call would `ReferenceError` on both pages | Every `initXxxDom()` in `composer-chrome.js` takes explicit accessor-function parameters for any page-lifecycle state the ported code reads, not just DOM refs (Phase 1/2 design, added during plan review) |
| `dashHandle()`'s `meta` branch restructuring done additively but incorrectly, breaking existing turn-start/turn-end handling | Medium — would silently break the dashboard's core live-attach flow, not just the new features | Exit criteria explicitly require the existing turn branches to remain intact; `/acp`'s and the dashboard's existing tests both re-run |
| `MAX_CONNECTIONS=8` is a **global**, not per-viewer, cap — SC8's second socket competes with `/acp`'s own sockets and every other connection on the machine for the same shared budget | Medium — a rejected connection attempt on a busy machine, not a rare edge case given the cap already covers `/acp`'s own dual-socket usage today | Phase 5 requires graceful client-side handling of a rejected connection (clear message, no stuck UI) rather than a cap increase (out of scope, a backend change) |
| SC5/SC7/SC8 are duplicated, not extracted, implementations — independently maintained in `acp.html` and `index.html`, prone to drifting apart over time | Medium — a future `/acp`-side fix or feature addition may not be mirrored to the dashboard, and vice versa | Add a paired cross-reference comment at each duplicated function's definition site on both pages (e.g. `// mirrors acp.html:XXXX — keep in sync`) so a future editor sees the counterpart; no automated drift-detection is in scope |
| Reconnect (Phase 6) restoring an incomplete inventory of dashboard state, leaving stuck UI after a disconnect | High if incomplete — the whole point of the feature is recovery; a partial restore is worse than no restore, since it looks recovered but isn't | Phase 6's restore list is explicit and enumerates every piece of state introduced by Phases 1-5 (see Phase 6); exit criteria test each restoration point individually, not just "reconnect works" |
| `acp_died` (kiro-cli process crash, independent of a WS drop) has no dashboard handling if left unaddressed | High — leaves the composer permanently disabled with no recovery but a full page reload | Phase 1 adds a baseline `agent_died` case; Phases 3 and 5 extend it as their own state comes into existence, each with an explicit exit criterion |

## 7) Verification

**Automated**:
- `node tests/acp_page.test.mjs` — full JS template harness; must pass after every phase (not part of pytest/CI per `AGENTS.md`, run manually).
- `pytest tests/test_web.py` — baseline run before Phase 1 and after Phase 1 specifically (the CSP/script-count change), then after every phase as a regression check even though no backend logic changes are anticipated.
- `.venv-PowerAtlas/Scripts/python _check_test_names.py` — pre-commit hook guard against duplicate test names in the expanded `tests/acp_page.test.mjs`; also runnable by hand.

**Manual / live QA** (this plan is runtime-verification-heavy — the entire point is dashboard-vs-`/acp` behavioral parity, which code-level inspection alone cannot confirm):
- After each phase, open `/acp` and `/` side by side against the same live or held kiro-cli-v3 session and compare the shipped feature's behavior directly.
- After Phase 5 specifically, exercise a real sub-agent fan-out (or a fixture session with crew data) on both pages, and simulate a `MAX_CONNECTIONS` rejection.
- After Phase 6 specifically, force a WS disconnect (block the port or restart PowerAtlas, with the user's confirmation per governance) mid-turn with queued/staged/open-panel state present, on both pages, and compare recovery behavior.

## 8) Documentation Updates

| Document | Update needed | Phase |
|---|---|---|
| `AGENTS.md:7` | Correct "All JS for `/acp` is inline in `acp.html`" to note the shared `composer-chrome.js` module | 1 |
| `tests/test_web.py:4163-4186` | Update CSP script-tag-count assertion from 4 to 5; add named `composer-chrome.js` assertion | 1 |
| `tests/acp_page.test.mjs:634-636`, `:1188-1193` | Correct stale script-element-count comments (both should state five) while this area is already being touched | 1 |
| `AGENTS.md:7` | Revise the "Exception" clause to name `.acp-compaction-details`/`.acp-compaction-recap` (the sole remaining inline-only CSS) in place of `.acp-mode-*` — not removed outright | 3 |
| `README.md` (near `:66-70`, "### Features") | Update the dashboard's feature-scope description to reflect the complete shipped surface (SC1-8), replacing the "scoped-down live-attach panel" framing | 6 |

## Progress Tracker

| # | Phase/Task | Status | Notes |
|---|---|---|---|
| 1 | Shared-module foundation + SC2 + SC3 + SC4 [QA] | Complete | Code: `d07c4f3` + review fixes `c42aa12`. Includes DI-accessor fix and baseline `agent_died` handling. Live QA performed at Step 5b against an already-running instance (no restart). |
| 2 | Slash-command palette (SC1) [QA] | Complete | Code: `9148c9f` + review fixes `c7e0763`. Live QA performed at Step 5b after Chrome was restarted (cold start, was not running) to reconnect the browser extension. |
| 3 | Queue/Steer + Stop (SC5) [QA] | Complete | Code: `bea1869` + review fixes `33985ca`. High-severity stale-queue-auto-send bug found and fixed. Live QA performed by implementer and re-verified by orchestrator, both clean. See Follow-up Work for a Phase 6 design note. |
| 4 | Image paste-to-attach (SC6) [QA] | Complete | Code: `bcc531c` + review fixes `8e6ef70`. High-severity cross-session state corruption bug (stale in-flight encode) found and fixed. Live QA performed by orchestrator, including a live Fix 1 re-verification. |
| 5 | Sub-agent/crew panel (SC8) [QA] | Complete | Code: `8d2c032` + review fixes `78a6b04`. Two High-severity functional regressions found and fixed (agent_died composer-hide, new-session-creation teardown bypass) plus a resource-leak fix (`dashSubWs` now explicitly closed). Live QA performed by orchestrator, including live re-verification of all 3 highest-severity fixes. |
| 6 | WS reconnect-on-drop (SC7) [QA] | Complete | Code: `d2be4c0` + review fixes `56179b0`. High-severity stuck-composer-after-lazy-attach-drop bug found and fixed. Live QA performed by orchestrator against a genuinely real WebSocket (not synthetic frames) — a real close, a real backoff delay, a real automatic reopen. All 6 phases now complete. |
| 9 | Post-implementation review (Step 9) | Complete | Fixes: `75f113d`, on top of `2750acf`. 10 findings (0 High, 6 Medium, 4 Low), all fixed, all REFUTE-verified. |
| 10 | Step 9b — exhaustive live QA | Complete | 9 items PASS + SC5/SC7 PASS (real steer, real drop+reconnect), SC8 honest SKIP (no real crew fan-out reproduced live; client logic separately verified via synthetic frames). Zero defects found. `/acp` got its first live QA of this plan. |
| 11 | Post-closeout: acp.html crew-timer parity fix + SC8 retry | Complete | Code: `d7aae28`. acp.html's `onclose` now mirrors dashboard Fix 8. SC8 retried at user's request — still SKIP, narrowed to `acp.py`'s `agent-subtask` wire-tag trigger (kiro-cli's real delegation tool confirmed invoked both attempts; investigation detail in Follow-up Work). |

## Dependency Graph

```
Phase 1 (composer-chrome.js foundation + SC2/SC3/SC4 + baseline agent_died)
   |
   +---------------+---------------+
   |               |               |
Phase 2 (SC1)   Phase 3 (SC5)   Phase 4 (SC6)
   |               |               |
   +---------------+---------------+
                    |
               Phase 5 (SC8)
                    |
               Phase 6 (SC7) -- must be last
```

Phases 2/3/4 have no dependency on each other's *output*, only on Phase 1's foundation — but all four (2, 3, 4, and 1) share `index.html` as a file scope, so per the Parallel Phase Annotation rule none carry `[P:N]`; they execute sequentially in the order shown. Phase 5 depends on Phase 1's shared-module guards being live and Phase 3's `agent_died` extension pattern to follow. Phase 6 depends on the full state surface from Phases 1-5.

## Backwards Compatibility

| Item | Strategy | Safety effect |
|---|---|---|
| `acp.html` refactor (SC1-4 moved to `composer-chrome.js`) | Behavior-preserving by construction — bare call sites resolve to the same-named global function, now defined externally instead of locally, with explicit accessor parameters replacing the free variables that would otherwise be lost; `/acp`'s existing test suite must pass unmodified | Any regression is caught by the existing suite before merge, not discovered live on the sole remote-access surface |
| `dashHandle()`'s `meta` branch widened, not replaced | Additive — existing `payload.turn === 'start'/'end'` branches untouched, new cases added alongside | No behavior change to already-working dashboard turn handling |
| Sid/copy bug fix (SC3) | Deliberate behavior change on `/acp` (copy value changes from short name to full path), done at explicit user request | Low risk — a clipboard-convenience feature, not data-affecting; anyone relying on the old (buggy) short-name-copy behavior sees a different, arguably more useful, value |
| AGENTS.md's CSS-exception clause | Revised, not removed — still accurately documents the one remaining inline-only CSS block | Avoids leaving stale documentation that would send a future session looking for `.acp-compaction-*` in `style.css` |

## File Change Summary

### Created
- `src/power_atlas/static/composer-chrome.js`

### Modified
- `src/power_atlas/templates/acp.html`
- `src/power_atlas/templates/index.html`
- `src/power_atlas/static/style.css`
- `tests/acp_page.test.mjs`
- `tests/test_web.py`
- `AGENTS.md`
- `README.md`

### Deleted
- None (no files removed; `acp.html`'s inline implementations of SC1-4 are deleted as code within the file, not as files)

### Unchanged
- `src/power_atlas/acp.py`, `src/power_atlas/web.py`, `src/power_atlas/data*.py` — confirmed no backend changes needed beyond nothing (§ Scope boundaries and non-goals); `MAX_CONNECTIONS` is explicitly not raised
- `src/power_atlas/static/transcript-renderer.js` — its pre-existing `window.removeAllCrewPanels`/`closeSubagentView` guard-calling code is unmodified; only which functions those globals resolve to changes (from Phase 5's new `index.html` code, never from `composer-chrome.js`, which has no involvement in crew/sub-agent logic)

## 9) Implementation Divergences from Plan

### Phase 1

- Widened `dashHandle()`'s `session` case (not just the `meta` branch named in the plan's 1c text) to also call `setContext(payload.contextPercent)` — needed for parity, since `acp.html`'s own `session`-frame handler does the same; the initial cut got the call-site guard wrong (see Phase 1 review log entry below), fixed in the follow-up commit.
- `tests/acp_page.test.mjs:634-636` and `:1188-1193`'s stale script-count comments were corrected to *different* numbers (four and five respectively), not both to five as the plan's literal text suggested — the plan itself pre-authorized this ("verify each test's own counting basis first") once direct verification showed the two harnesses count different things (content-block-only vs. full served page).
- Extended `loadDashPicker()`'s test sandbox (not just the main `acp.html` sandbox the plan's exit criterion named) to also load `composer-chrome.js` — two pre-existing dashboard picker tests already send `session` frames that reach the newly-shared code and would otherwise `ReferenceError`.
- Fixed an unrelated-looking but actually-caused-by-this-phase pytest test (`TestAcpContextWindow::test_the_page_narrows_the_value_again_before_the_style_sink` in `tests/test_web.py`) that read `setContext`'s body directly out of `acp.html`'s source — broke once the function moved to `composer-chrome.js`. (The original commit message characterized this as "fixing an unrelated pre-existing test"; that characterization was inaccurate — it's a direct consequence of the extraction, not unrelated. Corrected here since the commit text itself can't be amended.)
- Did not add a literal `sidWorkspace`/`_sessionCwd` assignment inside `dashMaybeAttach()` as the plan's 1c text named — that function only reads `cwd` for an availability-fetch query string and never stores it into page state; the plan's citation was inaccurate. Centralized the assignment in `dashHandle()`'s `session` case instead (session attach, release, `agent_died`), which is the actual single source of truth, matching how `acp.html` does it.

### Phase 1 review-fix follow-up

- Extended `tests/acp_page.test.mjs`'s region-extraction harness to also load `dashCloseIfAbandoned` (previously unreachable from the harness at all) so the new context-clear regression tests could exercise it — required a no-op `window.addEventListener` sandbox stub, matching the existing `document.addEventListener` stub pattern.
- Added test coverage for Fix 3 (`agent_died`'s extended reset) beyond what was strictly required for Fix 1, since the existing `agent_died` test already had the right setup to verify it cheaply and mutation-testing confirmed it catches a regression.

### Phase 2

- CSS selector rename: migrated `#acpCmdDropdown`'s id-scoped rules to a shared `.acp-cmd-dropdown` class instead of porting verbatim — a literal port would leave `#dashCmdDropdown` (a different id) completely unstyled. Matches the plan's own `.acp-taskmode-*` precedent, which is class-based for the same reason.
- Dropped two JS-side remnants of the `.acp-cmd-placeholder` dead path beyond what the plan explicitly named: `MAX_CMD_PARTIAL_CHARS` (only ever capped the dropped `commands_options` send) and `renderCommandDropdown`'s unused `catalogueEmpty` parameter.
- `initCommandPaletteDom`'s DI shape differs from the plan's literal code sketch: dropped `getTurnActive`/`cmdGetTurnActive` (nothing in the ported palette code reads turn-active state — each host page's own keydown listener checks it directly, never crossing the module boundary) and added a required `onPromptChanged` accessor instead (`confirmCommandSelection` needs to call `acp.html`'s IIFE-private `autoGrowPrompt()`/`refreshComposerControls()`, or the dashboard's `dashRefreshSendButton`, neither reachable as a bare global — the same boundary Phase 1 established for `sessionId`/`replaying`).
- Replaced one pre-existing `acp.html` test (`commandOptionsResultUpdatesDropdown` → `commandsOptionsResultIsANoOp`) rather than leaving all pre-Phase-2 tests byte-for-byte unmodified — the old test exercised exactly the `commands_options_result`/`applyCommandOptions()` dead path SC1 explicitly instructs dropping; pre-authorized by the plan's own scope.
- Added `resetCommandPalette()` to the dashboard's `agent_died` case, which has no `acp.html` counterpart (`acp.html`'s own `agent_died` handler never resets its palette state — a pre-existing, out-of-scope gap there, not introduced by this phase).

### Phase 3

- Committed `src/power_atlas/templates/acp.html` despite the plan's own Phase 3 File-scope line omitting it — 3d's CSS-deletion requirement makes this unavoidable, and Phase 2's analogous CSS-migration phase did include `acp.html` in its own File scope, so this reads as a drafting gap in Phase 3's scope line rather than a deliberate exclusion. Confirmed the resulting diff is pure CSS deletion, nothing else.
- Ported `acp.html`'s Enter-during-turn keyboard dispatch (Enter while a turn is active and text is present clicks the mode/steer send button instead of the idle send path) — not named in the plan's 3b bullet list, found while reading `acp.html`'s real keydown handler, required for keyboard parity (`acp.html`'s own test suite covers this behavior).
- Added a dedicated `#dashSteerStatus` element rather than reusing the existing multi-purpose `#dashComposerNote` — sharing it would let the steer-status auto-clear timer blank an unrelated lifecycle note that arrived in the meantime, a real correctness bug `acp.html`'s own architecture avoids by keeping `#acpSteerStatus` separate.
- Renamed `dashRefreshSendButton` to `dashRefreshComposerControls` and extended its body (9 call sites updated) rather than adding a second, parallel control-refresh function — matches `acp.html`'s own single-function design and avoids two composer-control-refresh functions drifting out of sync.
- 39 new tests added (plan estimate: ~27-31, corrected to 31 by direct recount) — the extra tests cover mode-menu open/close mechanics and the Enter-dispatch behavior above, both genuinely distinct surface, not padding.
- **Review-fix cycle**: Fix 3's literal framing ("restore runs before any sid check") was refined during the fix — the real, reachable gap was sid-less `error_frame()` calls, not mismatched-sid ones (the top-level guard already blocked those). Fix 4 was applied narrower than the literal finding wording for `steer_sent` specifically, to avoid breaking its legitimate session-wide-broadcast behavior — see the Review Log entry below for detail.

### Phase 4

- Moved the image-restore-on-refusal block in `dashHandle()`'s `error` case to the top of the handler (alongside the pre-existing steer-restore block) rather than leaving it near the generic-refusal tail — the `close_in_progress`/`turn_in_progress`/`not_subscribed` payload.code branches return early, before the original bottom placement would ever run; caught by the implementer's own new test, which failed until relocated. Mirrors `acp.html`'s own `restorePendingPrompt()` placement.
- Reordered `loadDashPicker()`'s test-harness region loading (image-attach region before queue/steer wiring region) to fix a test-harness-only ordering bug: `dashSendBtn`'s click listener captures `dashSendPrompt` by value at registration time, unlike every other reference to it in the file (late-bound inside function bodies). Real-browser page load is unaffected (function declarations hoist); only the VM-region-split test harness had the dependency.
- Updated one pre-existing test's assertion ("queued prompt auto-sends...") that depended on `dashSendPrompt` still being a call-counting stub — rewritten to check the actually-sent frame's payload, which both reviewers independently confirmed preserves (and slightly strengthens) the original test's intent rather than narrowing it.
- Added `dashClearAttachments()`/`dashReleasePendingAttachments()` calls to the three existing session-reset sites (`dashCloseIfAbandoned`, `session_closed`, `agent_died`) — not explicitly named as a task bullet, but required so a stale image can't leak across a session switch/close/crash; mirrors `acp.html`'s `releaseSession()`.
- **Review-fix cycle**: Fix 4 (image dropped during the lazy-attach window) was implemented via the specific direction the orchestrator chose from the reviewer's two proposed options (gate the drop on `!_dashLoadingSid` and reject with a note) rather than the alternative (have the deferred flush read live `dashAttachments` at flush time) — kept the fix minimal and consistent with the existing composer-lock reasoning.

### Phase 5

- `dashHandle()`'s own `session` frame case was deliberately left untouched for crew/sub-agent teardown, unlike `acp.html`'s live session-frame handler (which calls `clearTranscript()` directly) — the dashboard's real session-switch trigger point is the static-transcript-fetch path (`openSessionTranscript()` → `renderTranscriptHistory()` → `clearTranscript()`), which fires *before* any live session frame confirms attach. The `removeAllCrewPanels`/`closeSubagentView` guards were wired to that path instead. **This architectural claim was independently verified — and partially contradicted — by review**: it holds for switching to an *existing* session via a rail-row click, but not for creating a *new* session without closing the current one, a genuinely separate code path that bypassed all teardown — see the High-severity Fix 2 below.
- Added crew/sub-agent teardown to `dashHandle()`'s `session_closed` case, beyond the plan's own 5a-5g bullets, after reading `acp.html`'s `releaseSession()` directly (which calls both `removeAllCrewPanels()`/`closeSubagentView()` at that exact point) — a closed session's sub-agent records are already gone server-side. Independently confirmed accurate by review.
- Added a `dashConnectSubWs` `onclose` handler (defense-in-depth fallback message) with no `acp.html` counterpart to mirror, since `acp.html`'s own `connectSubWs` has none — new, dashboard-only logic explicitly called for by the plan's own 5e text. **Review found this had a real gap** (double-messaging on the actual `too_many_connections` sequence) — see Fix 3 below.
- **Review-fix cycle**: two High-severity functional regressions were found and fixed — (1) `agent_died`'s teardown call unconditionally hid the composer, breaking the established Phase 1/3 "stays visible-and-disabled" design; (2) new-session-creation without closing the current session bypassed all Phase 5 teardown entirely (the gap the first divergence above foreshadowed). A `dashCloseSubWs()` helper was added as part of the fix — not merely a fix-scoped utility, but exactly the helper Phase 6's own plan text already calls for on a main-socket drop; Phase 6 can call it directly rather than reinventing it. The implementer's own self-reported exit-criteria count (6 of an assumed 7) was corrected to the actual 7 of 8 by the orchestrator, from the plan file's ground truth, not the sub-agent's self-report — see the Review Log entry below.

### Phase 6

- The queue-state restore ordering correction from `## Follow-up Work (Deferred)` (snapshot on `onclose`, reapply after the re-subscribe's `session` frame clears the live vars) was implemented with an additional refinement beyond the plan's literal text: the reapply is gated on the frame's own `payload.turnActive` — a still-active turn restores to the live queue variables, but a turn that ended while disconnected surfaces the text in the prompt box instead of silently re-arming it, since no future `meta turn:end` is coming to flush it correctly and doing so would reproduce the exact stale-auto-send bug Phase 3's Fix 1 closed. Added after an advisor consultation; independently verified sound by review (both branches traced, the snapshot's scoping/discard behavior confirmed correct against multiple session-switch and repeated-drop scenarios).
- `_dashAttachedSid` is deliberately NOT nulled by `onclose` (kept intact so the reconnect's `onopen` knows what to resubscribe to), and `_dashOrigin` is only reset when no session was yet confirmed attached — a literal reading of 6b's restore list might suggest unconditionally resetting both; verified against `dashCloseIfAbandoned()`'s actual logic by review, confirmed correct (an unconditional `_dashOrigin` reset would have silently broken its auto-close-on-navigate-away check).
- Extended state restoration beyond 6b's literal bullet list to also restore an in-flight steer (`_dashSteerPending` → textarea, mode controls re-enabled) and reset `_dashStopInProgress` — both pre-existing Phase 3 state not named in 6b's list, but covered by the exit criterion's own "every piece of dashboard state" language; `acp.html`'s own `onclose` restores the equivalent state for the identical reason (a `steer_ack` never arrives on a dead socket). Independently confirmed non-conflicting with Phase 3's own restore paths by review.
- **Review-fix cycle**: one High-severity regression was found and fixed — a drop during an unconfirmed lazy-attach left the composer permanently stuck (disabled and, via an unrelated `dashCloseSubagentView()` side effect, also hidden), with no recovery path since the reconnect's resubscribe only fires when a session was already confirmed attached. Two Low fixes (a stale "Reload" button after a successful session switch; a silently-no-op Close button during the backoff window) were also applied. Two further findings — a stale-token-diagnostic false-positive risk and a theoretical `_dashWs` identity-guard race — were confirmed byte-identical to pre-existing `acp.html` behavior, inherited rather than introduced by this phase, and left unfixed (out of this task's scope; see Follow-up Work below).

### Step 9 (Post-Implementation Review) fixes

- Fix 8 (crew-panel timer teardown on drop) was applied only to the dashboard's `dashConnect()` `onclose`, per its brief's scope. This surfaced a matching latent gap in `acp.html`'s own `connect()`'s `onclose`, which has no equivalent `removeAllCrewPanels()` call and almost certainly carries the identical bug (crew-slot timers surviving an indefinitely failed reconnect) — confirmed by direct inspection, not fixed here (outside this fix's scope; not introduced or worsened by this plan). See Follow-up Work below.
- Fix 4 (the `close_in_progress` early-return composer-recovery gap) was hoisted unconditionally ahead of all three of `dashHandle()`'s `error`-case early-return codes (`close_in_progress`, `turn_in_progress`, `not_subscribed`), not only the one named in the finding, matching the exact shape of Phase 4's own precedent for this bug class. Verified safe: `turn_in_progress`/`not_subscribed` operate on an already-attached session's sid, a space disjoint from `_dashLoadingSid` (a session still mid-attach) by construction, so the added guard cannot fire for those two codes — confirmed by a dedicated test.
- Fix 9 (leaked `dashPendingAttachments` object URLs on a send-during-drop race) implements the fuller "restore to the tray for retry" behavior (folding pending attachments back into `dashAttachments`, or discarding with a note if superseded) rather than a bare discard — the brief left this choice open; this mirrors the established two-branch restore/discard pattern already used in `dashHandle()`'s `error`-case restore path.

## Follow-up Work (Deferred)

- The `MAX_CONNECTIONS` capacity concern raised during plan review is fully addressed within this plan's own scope (Phase 5's graceful-rejection exit criterion), not deferred.
- **Live QA for Phases 1-6**: all performed, all 6 phases. Phase 2's was initially blocked by a disconnected Chrome browser extension (across multiple orchestrator- and sub-agent-side retries); resolved by restarting Chrome (user-authorized; it had exited entirely, so nothing was lost) once Chrome itself, not just the extension, turned out to be the actual root cause. No recurrence in Phases 3-6. Phase 6's Live QA used a genuinely real WebSocket connection and a real close event, not a synthetic-frame simulation — the only phase where this was possible/necessary given its subject matter.
- **Phase 6 design note (from Phase 3's Fix 1) — resolved**: correctly implemented as a snapshot-on-`onclose`/reapply-after-the-session-frame-clears mechanism, with an additional `turnActive` gate beyond the plan's literal text (see Phase 6's Implementation Divergences above). Independently verified sound by both review personas.
- **Phase 6 ready-made helper (from Phase 5's review-fix cycle) — resolved**: `dashCloseSubWs()` was used directly, as intended, not reimplemented.
- **Inherited, pre-existing `acp.html` limitations, not introduced or worsened by this plan** (found during Phase 6's review, confirmed byte-identical to `acp.html`'s own code, left unfixed as out of this plan's scope per the "bugs outside the task are not yours to fix, report them" principle): (1) `dashReportStaleToken()`/`dashDiagnoseRejectedHandshake()`'s heuristic can misdiagnose a purely transient handshake-specific network blip as a stale token, showing Reload-only instead of the normal auto-backoff-reconnect — complying with Reload discards the in-memory state this phase's restore logic exists to preserve; both pages share this exposure equally, since the dashboard's version is a direct, faithful port of `acp.html`'s own pre-existing logic. (2) `dashConnect()`'s `onclose` handler has no identity/staleness check against a superseded `_dashWs` instance — a theoretical race with no confirmed reproduction in current code (both real callers only invoke `dashConnect()` after the previous socket has already reached `CLOSED`, not `CLOSING`); identical pattern exists verbatim in `acp.html`'s own `connect()`.
- **`acp.html`'s own `connect()` `onclose` crew-timer parity bug — resolved.** Found during Step 9's Fix 8 (confirmed by direct inspection: no `removeAllCrewPanels()` call in `acp.html`'s own `onclose`) and, per the user's explicit choice at plan closeout, fixed directly (`d7aae28`) rather than left for a separate follow-up — one `removeAllCrewPanels()` call added, mirroring `index.html`'s Fix 8 exactly, with a matching test. Both suites re-verified clean (654/654 JS, 1449/1 pytest).
- **A second, narrower gap in `acp.html`'s `onclose` was found while fixing the one above and left unfixed, deliberately, on advisor guidance**: unlike `index.html`'s `dashConnect()` `onclose` (which calls both `dashCloseSubagentView()` and `dashCloseSubWs()`, from Phase 6), `acp.html`'s own `onclose` still calls neither — no sub-agent panel teardown and no explicit sub-agent-WS close on a main-socket drop. Not fixed: it is a separate, undocumented gap outside Fix 8's own scope, and fixing it would also change UX (hiding the sub-agent panel on every transient reconnect blip) in a way nobody has asked for. Recorded here for a future decision, not acted on.
- **Accessibility/ARIA was not examined at any point across this plan** (flagged by the Step 9 Completeness critic as an informational gap, not a finding — no severity assigned since no specific defect was identified, only that the review surface never included it). Genuinely out of this plan's stated scope (feature parity, not an accessibility audit); noted here so a future accessibility pass on either page starts from an honest "unexamined" baseline rather than an implied "reviewed and clean."
- **`logLine()`'s DOM-append pattern has no cap on the debug/transport log panel's length** (flagged by the Step 9 Completeness critic) — confirmed pre-existing in `acp.html` before this plan and unchanged by it; the dashboard's `dashLogLine()` inherits the same unbounded-growth shape via the shared `composer-chrome.js` module by design. Not a regression; noted as an inherited characteristic of the shared code, not a plan defect.
- **The Step 9 real-server round-trip gap (SC5/SC7/SC8)**: SC5 (real steer) and SC7 (real WS drop + automatic reconnect) are fully closed by Step 9b's exhaustive live QA, against a real running instance and a real test session — see the Review Log's Step 9b entry below. **SC8 (crew panel) remains an honest SKIP after a second, more targeted attempt at the user's request** ("you should be able to trigger one by simply asking"). The retry investigated and ruled out the previous attempt's failure mode (it had invoked an unrelated ordinary tool, not kiro-cli's real sub-agent delegation) — this time, two differently-phrased live prompts both confirmed-invoked kiro-cli's actual internal `invoke_subagent` tool (real concurrent sub-agent execution, ~7-8s each), narrowing the gap specifically to `acp.py`'s v3 crew-panel trigger: `_on_agent_subtask_open` fires `_emit_subagents_frame(...)` only when a `tool_call`'s `_meta.kiro.kind == "agent-subtask"`, and that tag evidently never arrived on the wire in either attempt (zero `subagents` frames, zero rendered crew panels in both real live turns). Whether the tag is genuinely never sent by this kiro-cli version/config or sent in a shape `acp.py` doesn't recognize could not be determined without adding temporary diagnostic logging to `acp.py` (a source edit, out of scope for a read-only QA task) — that is the concrete next step if this gap is worth closing. **A related, previously-undiscovered fact surfaced by this investigation**: the only other documented recipe for triggering a real crew panel (`memory/MEMORY.md:168-186`, a raw kiro-cli-v2 subprocess probe) is v2-only and cannot fire through PowerAtlas at all — PowerAtlas has run v3-only since 2026-09-17 (`data_kiro_v3.py:9`). That memory entry is now stale for PowerAtlas's actual runtime configuration and should be corrected or retired the next time memory is swept (`/qdream`), not as part of this plan's own scope. The crew panel's own client-side logic (`dashSetCrew`/`dashRenderCrewPanel`/timer lifecycle/teardown) remains separately, thoroughly verified via synthetic `subagents`-frame injection (Step 9's Fixes 3 and 8 verification) — only the real end-to-end wire trigger under v3 stays unconfirmed.
- **Two unrelated documentation discrepancies noticed during the SC8 retry, not acted on (out of this plan's scope)**: `AGENTS.md`'s Terminology section describes `~/.kiro/agents/poweratlas-acp.md` (the "derived agent") as generated at startup — no such file exists on disk, and no `"poweratlas-acp"` string or derived-agent logic exists anywhere in `src/power_atlas/`. The plan that introduced this terminology, `plans/260921_ACP_PERMISSION_PROFILE_AND_LOOPBACK_CREDENTIAL.md`, is still in `plans/` (not `plans/done/`), consistent with it not yet being implemented — this is very likely just that plan still being in progress (a separate `/qplan` session was reported running concurrently with this one), not a defect. Flagged here only so it isn't lost; not this plan's concern to resolve.
- **Fix 2's own duplicate-send-detection code has a narrow, unfixed timing edge**, found during Step 9b prep: the `history` case's first branch (`if (_dashSteerPending) { clear textarea; null pending }`, `index.html`'s `dashHandle()`) fires on ANY `steer_sent` event in the replay with no text comparison, correct only because `_dashSteerPending` is normally null by the time `history` is processed (`onclose` nulls it immediately after moving its text to the textarea). The Queue/Steer send button stays hidden after a drop (`dashQueueSteerEl.hidden` gates on `_dashTurnActive`, which `onclose` sets false) until a fresh turn-active frame arrives — so a user cannot send a genuinely new steer during the disconnected window itself. The remaining edge is narrower than "during the disconnected window": if the reconnect's `session` frame (which can restore `_dashTurnActive`) is processed before the `history` frame that carries the old `steer_sent`, there is a brief window — bounded by how close together the two frames arrive from the same resubscribe, likely sub-millisecond in practice — where a fresh steer could theoretically be sent and then incorrectly cleared by the old event's unconditional first branch. Worst case is cosmetic (the server still holds the steer; its own live `steer_sent` still renders a band), not a duplicate send. Not fixed — outside Step 9's fix scope, found during Step 9b's own prep rather than the Step 9 review itself.

## Review Log

### 2026-09-21 — Plan Review (via /qplan, Standard effort, 4 personas)

22 distinct findings after merging across personas (3 High, 12 Medium, 7 Low — from Architect+gap-critic, Senior engineer, Security auditor, and Reliability engineer, each independently reviewing against Correctness/Completeness/Feasibility/Risk/External readiness/Cost impact). All 22 auto-resolved within this single review cycle (user requested 1 cycle; no findings required escalating a product/design decision beyond the plan's existing scope boundaries).

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | High | Phase 1's ported sid/copy and log functions read IIFE-private free variables (`sessionId`, `replaying`) that don't exist once moved out of `acp.html`'s IIFE — `ReferenceError` on both pages. | Fixed — added `getSessionId`/`isReplaying` DI accessor functions to `initSidCopyDom`/`initLogDom`, mirroring Phase 2's existing palette-accessor pattern. |
| 2 | High | `agent_died` was identified as a dropped frame type but no phase ever wired it into `dashHandle()`. | Fixed — added a baseline case in Phase 1, extended in Phases 3 and 5 as their state comes into existence. |
| 3 | High | Phases 3 and 5 render agent/session-controlled text with no explicit exit criterion enforcing `acp.html`'s no-innerHTML security convention. | Fixed — added explicit exit criteria to both phases citing the exact patterns to mirror, plus required porting of the specific injection-safety tests. |
| 4 | Medium | `MAX_CONNECTIONS=8` was mischaracterized as per-viewer; it's a global cap, understating SC8's connection-budget risk. | Fixed — corrected Current State and Risk Assessment; added a client-side graceful-rejection exit criterion to Phase 5 instead of a cap increase (kept out of scope). |
| 5 | Medium | The plan's `railStored`/`railStore` "naming collision" with `index.html` was factually wrong (real names are `dashRailStored`/`dashRailStore`) — no collision exists. | Fixed — corrected the claim; replaced the investigate-a-collision task with a reconciliation note. |
| 6 | Medium | `tests/acp_page.test.mjs`'s VM-sandbox harness has no load step for the new `composer-chrome.js`, so the existing suite would `ReferenceError` at first call into ported code. | Fixed — added an explicit exit criterion to Phase 1 mirroring `transcript-renderer.js`'s existing load step. |
| 7 | Medium | CSS migration scope missed `acp.html:548-594` (`.acp-compaction-details`/`.acp-compaction-recap`), contradicting the planned "no CSS remains inline" claim and premature AGENTS.md exception-clause removal. | Fixed — explicitly scoped this CSS out of the plan; revised (not removed) the AGENTS.md exception clause to name it instead. |
| 8 | Medium | Phase 4 named only one of two `send('prompt', ...)` call sites needing the image payload — the deferred lazy-attach flush was missed, risking a silently dropped image on first send to an unattached session. | Fixed — added `_dashPendingImages` state and an explicit exit criterion covering both call sites, plus a dedicated test case. |
| 9 | Medium | Phase 6's reconnect-restore list omitted several pieces of state (`_dashLoadingSid`, `_dashPendingCreate`, `_dashOrigin`, command-palette state, crew timers, explicit `dashSubWs` closure) introduced by earlier phases. | Fixed — expanded the restore list to the complete state surface, with a per-item exit criterion. |
| 10 | Medium | Phase 3 didn't extend `dashHandle()`'s generic `error` case to restore steer-pending state, unlike `acp.html`'s three restoration points. | Fixed — added the extension and an explicit exit criterion. |
| 11 | Medium | Phase 3 planned to reuse `_dashPendingSend` for Queue-mode text, risking a reconnect-triggered premature fire against the pre-existing lazy-load-pending mechanism. | Fixed — introduced dedicated `_dashQueuedPrompt`/`_dashQueuedPromptSession` state, kept separate. |
| 12 | Medium | SC8's "~35 tests" density target was inflated; direct recount found ~25. | Fixed — corrected the number everywhere it appears (Success criteria, Design Decisions, Phase 5, Progress Tracker). |
| 13 | Medium | Risk Assessment had no row for long-term behavioral drift between the duplicated (not extracted) SC5/SC7/SC8 implementations. | Fixed — added a drift-risk row with a lightweight cross-reference-comment mitigation. |
| 14 | Medium | Phase 1/4's `meta`-widening (context percent, image limits) had no automated test, only manual Live QA. | Fixed — added explicit wiring-level test exit criteria to both phases. |
| 15 | Medium | Phase 3/5's "dense test coverage" language didn't name the specific injection-safety tests required for parity. | Fixed — named the specific `/acp` test pattern (`tests/acp_page.test.mjs:10050` and adjacent) as a required parity test in both phases. |
| 16 | Low | Phase 1's stale-script-count-comment fix self-contradicted ("five" in body text vs. "five"/"four" in exit criteria). | Fixed — reconciled to a single, consistent instruction (both comments state five, verify each test's own counting basis first). |
| 17 | Low | `window.logLine = logLine;` and its comment become stale once Phase 1 deletes `logLine`'s local IIFE definition. | Fixed — added explicit removal to Phase 1's exit criteria. |
| 18 | Low | Progress Tracker and File Change Summary misattributed `removeAllCrewPanels`/`closeSubagentView` ownership to `composer-chrome.js` instead of `transcript-renderer.js`. | Fixed — corrected both tables. |
| 19 | Low | Five citations carried small (1-13 line) offsets from actual source (`test_web.py`, `acp.html`'s `reportStaleToken`/`IMAGE_LADDER` locations, `README.md`). | Fixed — tightened the citations that could be corrected with confidence; added a general verify-before-editing caution for the rest. |
| 20 | Low | The Phase 1 collision-check instruction was scoped only to `railStored`/`railStore`, not the module's other new global names. | Fixed — generalized to a full-name-list check. |
| 21 | Low | `index.html`'s new `composer-chrome.js` script tag lacked the conditional-nonce pattern already used for `transcript-renderer.js`. | Fixed — added the matching conditional pattern for consistency. |
| 22 | Low | Phase 2's CSS migration range inadvertently included the `.acp-cmd-placeholder` rules despite SC1's explicit "not ported" exclusion for that dead path. | Fixed — excluded those lines from the migrated range; marked for deletion instead. |

### 2026-09-21 — Implementation Review (after Phase 1, persona: Architect, Senior engineer)

Implementation health: Green (post-fix). 6 findings (1 High, 2 Medium, 3 Low). Both reviewers independently re-ran `node tests/acp_page.test.mjs` and `pytest tests/test_web.py --timeout=300` and confirmed the implementer's pass claims; both independently confirmed `qvalidate --phase 1 --expect-ticked 16` PASS. Per the user's "1 qreview cycle per phase" instruction, findings were auto-fixed once (code commit `c42aa12`) and independently re-verified by the orchestrator (tests + diff read); no second persona re-review cycle was run.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | High | `dashHandle()`'s `session` case guarded `setContext()` behind a `typeof` check `acp.html` doesn't have, and `dashCloseIfAbandoned()` never called `setContext(null)` — switching sessions could leave a stale context reading on screen. | Fixed — dropped the guard, added `setContext(null)` to `dashCloseIfAbandoned()`, added 2 mutation-verified regression tests (`c42aa12`). |
| 2 | Medium | `#dashLogToggle`'s chevron rotate/hover CSS has no matching rule (`style.css:1533-1534` is ID-gated to `/acp`'s toggle); `style.css` isn't in Phase 1's file scope. | Fixed — deferred to Phase 2 (already touches `style.css`) via a `(deferred from Phase 1)` exit-criterion bullet. |
| 3 | Medium | Plan's Implementation Divergences section and Progress Tracker still showed placeholder/"Not started" text despite 5 real implementer-reported divergences. | Fixed — both sections populated with the full divergence list and phase status. |
| 4 | Low | `dashWs.onerror` was a silent no-op; `acp.html`'s `ws.onerror` logs a diagnostic via `logLine`, which the dashboard's new debug log omitted. | Fixed — mirrors `acp.html`'s diagnostic message exactly (`c42aa12`). |
| 5 | Low | The `d07c4f3` commit message called a `test_web.py` fix "unrelated" when it's directly caused by this phase's `setContext` relocation — accurate fix, inaccurate characterization. | Fixed in the record — corrected in this phase's implementation notes and Divergences section above (commit history itself can't be amended per governance). |
| 6 | Low | Baseline `agent_died` reset didn't clear `_dashLoadingSid`/`_dashPendingSend`/`_dashSent` — a narrow race if the agent dies mid-lazy-load. | Fixed — all three now reset to their existing defaults, with a mutation-verified test assertion (`c42aa12`). |

### 2026-09-21 — Implementation Review (after Phase 2, persona: Architect, Senior engineer)

Implementation health: Green (post-fix). 4 findings (0 High, 2 Medium, 2 Low; one Medium — the compaction feedback gap — required a user scope decision before fixing, not just a mechanical auto-fix). Both reviewers independently re-ran `node tests/acp_page.test.mjs` and `pytest tests/test_web.py --timeout=300` and confirmed pass claims; both independently confirmed `qvalidate --phase 2 --expect-ticked 6` PASS. Both reviewers separately verified and endorsed all 5 of the implementer's reported divergences as justified, not scope creep. Per the user's "1 qreview cycle per phase" instruction, findings were auto-fixed once (code commit `c7e0763`) and independently re-verified by the orchestrator; no second persona re-review cycle was run.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | Medium | Phase 2's palette makes `/compact` invokable on the dashboard for the first time, but `dashHandle()` had no `compaction` frame case — triggering it gave zero visible feedback. | Fixed — user chose a minimal status-mapped `addSystemMessage()` ack (no recap UI, stays out of scope), `c7e0763`. |
| 2 | Medium | Progress Tracker and Implementation Divergences sections weren't updated for Phase 2 despite 5 real divergences and a functionally complete phase — same defect class Phase 1's own review already flagged. | Fixed — both sections populated. |
| 3 | Low | `onPromptChanged` silently defaulted to a no-op if a caller omitted it, breaking Phase 1's "every DI accessor is REQUIRED, loud failure" pattern; no live bug today, but a latent trap for a future host page. | Fixed — fallback removed, now throws naturally on first use if omitted (`c7e0763`). |
| 4 | Low | `composer-chrome.js` gained an undocumented incoming dependency on `transcript-renderer.js`'s `addSystemMessage` global (load-order requirement). | Fixed — documented at the call site (`c7e0763`). |

**Live QA**: performed after the fix cycle closed, once Chrome was restarted (user-authorized) to reconnect the browser extension. See the Live QA exit criterion above.

### 2026-09-22 — Implementation Review (after Phase 3, persona: Security auditor, Senior engineer, Reliability engineer)

Implementation health: Green (post-fix). 8 findings (1 High, 2 Medium, 5 Low). Three personas used given this phase's real security stakes (agent-controlled text rendering) and dense state-recovery surface. All three reviewers independently re-ran both test suites and confirmed pass claims; all three independently confirmed `qvalidate --phase 3 --expect-ticked 8` PASS. The Security auditor's dedicated injection-safety hunt found zero gaps (mutation-tested — confirmed the ported test genuinely catches an `innerHTML` regression). Per the user's "1 qreview cycle per phase" instruction, findings were auto-fixed once (code commit `33985ca`) and independently re-verified by the orchestrator (tests + live re-verification of the 3 highest-impact fixes); no second persona re-review cycle was run.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | High | `_dashQueuedPrompt`/`_dashQueuedPromptSession` never cleared by `agent_died` or session-reattach — reproduced: stale pre-crash queued text silently auto-sends into an unrelated later turn. | Fixed — both handlers now clear it; 15 new tests added, mutation-verified; orchestrator re-confirmed live (`33985ca`). |
| 2 | Medium | Stop can get stuck permanently disabled after a refused `cancel` while the turn is still active — the code's own comment claims to prevent this but doesn't for the realistic case. | Fixed — `_dashStopInProgress` now clears unconditionally on any `error` frame; orchestrator re-confirmed live (`33985ca`). |
| 3 | Medium | Plan-file bookkeeping (Status/Progress Tracker/Divergences) not updated for Phase 3 — 3rd consecutive phase this recurred. | Fixed — all sections populated; see also the Harness Improvement entry below on this recurring pattern. |
| 4 | Low | `error` handler restores `_dashSteerPending` before checking the frame's own sid — real gap was sid-less `error_frame()` calls slipping past the existing mismatched-sid guard. | Fixed — restore now requires the frame's `sessionId` to match `_viewingSid` (`33985ca`). |
| 5 | Low | Stale `steer_ack`/`steer_sent` could act on an abandoned attempt after reattach to the same session id. | Fixed — `steer_ack` (private reply) now no-ops with no pending steer; `steer_sent`'s broadcast rendering stays unconditional, only its unsafe composer-mutating branch removed (`33985ca`). |
| 6 | Low | `dashCloseIfAbandoned()` doesn't reset `dashPromptInput.disabled` (currently harmless — composer hidden right after). | Fixed — reset added for consistency with the other release points (`33985ca`). |
| 7 | Low | Enter-handler send path missing `!e.ctrlKey && !e.altKey` guards `acp.html` has (pre-existing gap in a function this phase edited). | Fixed — guards added, matching `acp.html` (`33985ca`). |
| 8 | Low | Stop/Steer `send()` failures were silent (debug-log only, no toast) — matches `acp.html`'s existing behavior exactly, not a phase-3 regression. | Fixed anyway (bias toward fixing, no design trade-off) — same toast `dashSendPrompt()` already uses (`33985ca`). |

### 2026-09-22 — Implementation Review (after Phase 4, persona: Senior engineer, Reliability engineer)

Implementation health: Green (post-fix). 6 findings (1 High, 3 Medium, 2 Low). The Reliability engineer's High finding was empirically reproduced (not just inferred) via a temporary, fully-reverted test probe, and every fix in the cycle was individually mutation-tested by the fixing sub-agent. Both reviewers independently re-ran both test suites and confirmed pass claims; both independently confirmed `qvalidate --phase 4 --expect-ticked 5` PASS. Per the user's "1 qreview cycle per phase" instruction, findings were auto-fixed once (code commit `8e6ef70`) and independently re-verified by the orchestrator (tests + live re-verification of the High finding specifically); no second persona re-review cycle was run.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | High | In-flight image encode has no staleness check — a session switch mid-encode silently repopulates the newly-viewed session's attachments and inserts a stray `[Image N]` marker into its prompt text. Empirically reproduced. | Fixed — `dashStageOne` now re-checks the target sid before its final mutation, discarding stale results; orchestrator re-confirmed live (`8e6ef70`). |
| 2 | Medium | Two concurrent `dashStageFiles()` calls aren't serialized — both can pass the count/byte-budget check before either pushes, exceeding the configured max. | Fixed — a persistent `_dashStageChain` now serializes staging across separate calls (`8e6ef70`). |
| 3 | Medium | `dashPendingAttachments`'s object URLs aren't revoked when an `error` frame has no `sessionId` (transient leak). | Fixed — a sid-less-error fallback branch now revokes/clears them (`8e6ef70`). |
| 4 | Medium | An image dropped during the lazy-attach window is staged/shown but silently excluded from the actual send, later revoked with no note — dashboard-specific, no `/acp` precedent. | Fixed — drop is now rejected with a clear note during that window (`8e6ef70`). |
| 5 | Low | A failed file in a multi-file paste silently aborts the rest of the batch with no per-file feedback. | Fixed — each file now gets its own `.catch()` naming the failed file (`8e6ef70`). |
| 6 | Low | `_dashPendingImages` isn't reset by the lazy-attach abandonment paths, unlike its sibling `_dashPendingSend` (bounded/self-correcting, both reviewers confirmed no real risk). | Fixed anyway (bias toward fixing, no design trade-off) — reset added at both sites for consistency (`8e6ef70`). |

### 2026-09-22 — Implementation Review (after Phase 5, persona: Security auditor, Senior engineer, Reliability engineer)

Implementation health: Green (post-fix). The most significant review cycle in this plan so far: 8 findings (2 High, 1 Medium, 5 Low), plus a 9th, separately-tracked exit-criteria bookkeeping correction (see below — not a code defect). All three reviewers independently re-ran both test suites and confirmed pass claims. Every High/Medium finding was empirically reproduced (not just inferred) — a temporary probe test for the `agent_died` regression, a scratch reproduction of the double-message sequence, direct tracing of the new-session-creation code path against `acp.py`'s own frame-routing source. Per the user's "1 qreview cycle per phase" instruction, findings were auto-fixed once (code commit `78a6b04`) and independently re-verified by the orchestrator (tests + live re-verification of all 3 highest-severity fixes); no second persona re-review cycle was run.

**Exit-criteria bookkeeping note**: all three reviewers independently ran `qvalidate --phase 5 --expect-ticked 6` and got a FAIL (`7 ticked, expected 6`) — the orchestrator's own dispatch briefs propagated the implementer's inaccurate self-report (`exit_criteria_ticked: 6`) rather than the actual file state. The underlying work was never in question — all 7 pre-Live-QA criteria were genuinely, correctly ticked with cited evidence; only the implementer's own count of its work was wrong (Phase 5 has 8 criteria total, not 7). Corrected directly from ground truth at Step 7; no code change involved. See the Harness Improvement entry below.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | High | `agent_died`'s sub-agent-panel teardown call unconditionally hid the composer, breaking the established Phase 1/3 "stays visible, disabled, with a note" design — reproduced with a probe test. | Fixed — composer visibility explicitly restored after teardown, before the existing disable-and-note tail; new permanent regression test (`78a6b04`). |
| 2 | High | Creating a new session without closing the current one bypassed all Phase 5 teardown — a stale sub-agent panel, open socket, and orphaned crew slot survived into the new session. | Fixed — the `created:true` session-frame branch now tears down panel/crew/socket; orchestrator re-confirmed live (`78a6b04`). |
| 3 | Medium | A real `too_many_connections` rejection sequence (error frame, then close) double-messaged the panel — EC5 wasn't genuinely complete for the case it names. | Fixed — a flag now prevents the `onclose` fallback from adding a second note once an error-derived one has shown; orchestrator re-confirmed live (`78a6b04`). |
| 4 | Low | `dashCrews` keyed directly by wire-controlled `toolCallId` — a `"__proto__"` key could reassign the object's own local prototype (confirmed not global pollution, still worth hardening). | Fixed — `Object.create(null)`, matching the sibling `dashSubToolRows` pattern (`78a6b04`). |
| 5 | Low | `dashSubAddToolCall`'s retitle-an-existing-row branch had no dedicated textContent-only test. | Fixed — new test added, including an injection-safety variant (`78a6b04`). |
| 6 | Low | `dashHandleSub` had no staleness guard — a frame for a previously-viewed sub-agent could render into a just-opened different one via the reused socket. | Fixed — frames are now gated on the currently-viewed sub-agent sid, verified safe against every real frame shape `acp.py` sends (`78a6b04`). |
| 7 | Low | No `logLine` tracing on `dashSubWs`'s lifecycle (open/error/close/parse-failure), unlike the main socket. | Fixed — tracing added, matching Phase 1's established pattern (`78a6b04`). |
| 8 | Low | `dashSubWs` was never explicitly closed on `session_closed`/`agent_died` teardown, only hidden — leaked a `MAX_CONNECTIONS` slot until page reload. | Fixed — new `dashCloseSubWs()` helper called at all three teardown sites; also positions Phase 6 to reuse it directly (`78a6b04`). |

### 2026-09-22 — Implementation Review (after Phase 6, persona: Senior engineer, Reliability engineer)

Implementation health: Green (post-fix). The final phase's review: 3 findings requiring code changes (1 High, 2 Low) plus 2 informational findings confirmed as inherited `acp.html` behavior (not introduced or worsened by this plan, left unfixed per the "bugs outside the task" principle — see Follow-up Work) and 2 process-only observations (a self-report test-count miscount with no functional gap, and transient test flakiness independently traced by the Reliability engineer to concurrent mutation-testing by the two review personas sharing this working tree — not a Phase 6 code defect). Both reviewers independently re-ran both test suites and confirmed pass claims; both independently confirmed `qvalidate --phase 6 --expect-ticked 5` PASS with an accurate implementer self-report — unlike Phase 5, no propagation error this time. Per the user's "1 qreview cycle per phase" instruction, findings were auto-fixed once (code commit `56179b0`) and independently re-verified by the orchestrator (tests + a genuine real-WebSocket Live QA pass); no second persona re-review cycle was run.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | High | A drop during unconfirmed lazy-attach left the composer permanently stuck disabled-and-hidden, with no recovery path even after a successful reconnect — violates the plan's own explicit 6b text. | Fixed — composer re-enabled/cleared when a lazy-attach was in flight at drop time; `dashCloseSubagentView()` no longer unconditionally hides the composer; orchestrator re-confirmed against a real socket close (`56179b0`). |
| 2 | Low | "Reload page" stayed visible after the user switched to and successfully loaded a different session. | Fixed — now clears on any confirmed session attach, not only a fresh socket open (`56179b0`). |
| 3 | Low | Close button silently no-opped (no toast) if clicked during the reconnect/backoff window — same class Phase 3 fixed for Stop/Steer. | Fixed — same "Not connected — nothing sent" toast applied (`56179b0`). |
| 4 | Medium | "21 new tests" claimed (plan text and commit message); actual count 25 — self-report miscount, no functional gap, more coverage not less. | Corrected in the record — orchestrator recorded 25 directly from the file, matching Phase 5's identical recurring pattern. |
| 5 | Medium | `dashDiagnoseRejectedHandshake()`/`dashReportStaleToken()`'s heuristic can misdiagnose a transient network blip as a stale token — confirmed byte-identical to `acp.html`'s own pre-existing logic. | Inherited, out of scope — not introduced or worsened by this phase; documented in Follow-up Work. |
| 6 | Low | No identity/staleness guard on `onclose` against a superseded `_dashWs` — theoretical, unconfirmed race, identical pattern inherited from `acp.html`'s own `connect()`. | Inherited, out of scope — no live reproduction found; documented in Follow-up Work. |
| 7 | Low | Dead-session resubscribe (`unknown_session`) leaves `_dashAttachedSid`/Close pointing at a dead session — bounded, self-corrects on the next explicit navigation via `dashCloseIfAbandoned()`. | No action needed — reviewer's own characterization confirms this is self-healing. |
| 8 | Low | Phase 6's own exit-criteria text, unlike Phases 3-5, documented no mutation-testing evidence for its reliability-critical timer/queue-restore tests. | Addressed here — both reviewers independently mutation-tested the timer-replaces-not-stacks and `turnActive`-gate tests themselves and confirmed they're load-bearing. |

### 2026-09-22 — Post-Implementation Review (Step 9, Full effort, 4 personas: Senior engineer, Security auditor, Architect, Reliability engineer)

Implementation health: Green (post-fix). The mandatory final holistic review across all 6 completed phases together — the first pass able to see cross-phase interactions no single phase's review could. 15 raw findings merged (deduping overlap across personas) to 10 distinct findings (0 High, 6 Medium, 4 Low). Full-effort verification: a dedicated REFUTE-stance verifier ran against every one of the 10 findings — 9 CONFIRMED, 1 PLAUSIBLE, 0 REFUTED. A Completeness critic pass followed (informational gaps only, never adding findings) and flagged three under-examined files (`style.css`, `AGENTS.md`, `README.md`) plus two genuine informational gaps (accessibility/ARIA never examined; `logLine()`'s unbounded log growth) — the orchestrator personally spot-checked all three flagged files and found nothing wrong (pre-existing quirks predating this plan, not introduced/worsened; see below), and recorded the two informational gaps in Follow-up Work above.

**Two findings required a genuine design trade-off and were escalated to the user rather than auto-resolved** (F2, F10 below) — both now resolved per the user's explicit direction, applied in the fix commit.

Dispatch methodology note: unlike every prior per-phase review in this plan, all 4 Step 9 personas and all 10 REFUTE verifiers were dispatched strictly read-only (no working-tree mutation, citing existing Review Log mutation-test evidence instead of re-running live mutation tests) — correcting a process error where every prior per-phase review violated `/qreview`'s own "dispatch mutation-testing personas sequentially, not in parallel" rule (see the Harness Improvement entry below dated for Phase 6). Findings were auto-fixed once (code commit `75f113d`, on top of `2750acf`) and independently re-verified by the orchestrator: diff-stat scope matched the sub-agent's claim exactly, both test suites (`node tests/acp_page.test.mjs`: 653/653; `pytest tests/test_web.py --timeout=300`: 1449 passed/1 skipped) were independently re-run and matched the sub-agent's reported counts exactly (not just trusted), `_check_test_names.py` stayed clean, the commit message was independently read and confirmed free of any AI-attribution trailer, and Fix 2's actual diff (the highest-risk change, touching the shared `transcript-renderer.js`) was read in full and confirmed to match its described reasoning and scope. No second persona re-review cycle was run, per the user's original "1 qreview cycle per phase" instruction extended to this final cycle.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | Medium | `dashHandle()`'s `session` case never cleared a stale steer-status line on reattach, unlike `acp.html`'s own handler. | Fixed — `setDashSteerStatus('')` added to both the normal reattach and `payload.created` paths (`75f113d`). |
| 2 | Medium | `steer_sent` had no `transcript-renderer.js` case (silently dropped on dashboard reconnect replay) and the dashboard's reconnect-restore couldn't detect an already-landed steer — a genuine duplicate-send risk. | Fixed per the user's explicit choice ("fix properly in the shared renderer") — `steer_sent` case added to `renderTranscriptFrame`; duplicate-send detection added to `dashHandle()`'s `history` case, mirroring `acp.html`'s own two-branch logic. Verified `acp.html` unaffected (`75f113d`). |
| 3 | Medium | `dashCloseIfAbandoned()`'s other 2 call sites (`openSessionTranscript()`'s fetch-catch, `dashRailForgetSession()`) skipped crew/sub-agent teardown. | Fixed — the established 3-call teardown pattern added at both sites (`75f113d`). |
| 4 | Medium | `close_in_progress` early-return in `dashHandle()`'s `error` case stranded the composer during lazy-attach (never reached the reset/re-enable block). | Fixed — hoisted unconditionally ahead of all three early-return codes, mirroring Phase 4's precedent; verified safe for the other two codes (`75f113d`). |
| 5 | Medium | `acp.html`'s `refreshComposerControls`/`connect()`/`openSubagent`/`closeSubagentView`/`setCrew`/`removeAllCrewPanels` carried no cross-reference comment pointing at their `dash`-prefixed twins, unlike the systematic reverse direction. | Fixed — one-line "mirrors index.html's dashXxx()" comments added to all 6 (`75f113d`). |
| 6 | Medium | `resetCommandPalette()` was never added to `acp.html`'s own `agent_died` handler — a real behavioral asymmetry against SC1's "identical on both pages" text. | Fixed — one-line call added, matching the dashboard's position (`75f113d`). |
| 7 | Low | A Phase 5 comment on `index.html`'s `agent_died` case described `dashCloseSubagentView()`'s composer-hide as unconditional; Phase 6's own Fix 1 later gated it. | Fixed — comment corrected to describe current (gated) behavior; no functional change (`75f113d`). |
| 8 | Low | Crew-slot timers only cleared on a successful reconnect's `session`/`history` frame — an indefinitely failed reconnect left them running forever. | Fixed — `dashRemoveAllCrewPanels()` now called directly in `onclose`. Surfaced a matching latent gap in `acp.html` itself, not fixed (out of scope; see Follow-up Work) (`75f113d`). |
| 9 | Low | `dashPendingAttachments`' object URLs leaked if a send raced a genuine WS drop before any server response. | Fixed — `onclose` now restores pending attachments to the tray for retry (or discards-with-note if superseded) (`75f113d`). |
| 10 | Low (Plausible, not Confirmed) | `confirmCommandSelection()` never checked `cmdSend()`'s return value — a failed palette send during a disconnect produced no specific feedback. | Fixed per the user's explicit choice ("strengthen `logLine` on both pages") — return-value check added, with a command-specific `logLine` failure message; no new toast UI (`75f113d`). |

**Completeness critic — informational findings (no severity, not counted above)**: (1) `style.css` declares `.acp-context {}` three times (lines 943/944/1023, non-overlapping properties) — orchestrator verified via `git blame`/`git log -S` that all three predate this plan (August 2026 commits), not introduced or worsened. No action needed. (2) `AGENTS.md`'s `## Doc & Test Guidelines` first bullet was spot-checked end-to-end against the final 6-phase state and confirmed fully accurate. No action needed. (3) `README.md`'s Phase 6 Features-section addition was checked against its actual diff (`d2be4c0`) and confirmed accurate and appropriately non-specific. No action needed. (4) Accessibility/ARIA and (5) `logLine()`'s unbounded log growth — both recorded in Follow-up Work above as genuine, honest gaps rather than silently passed over.

### 2026-09-22 — Step 9b Exhaustive Live QA

Full-probe-mandate live QA pass against a genuinely running PowerAtlas instance and a dedicated, disposable test session (`sess_37b3c3f1-...`, created in the agent's own scratch workspace, never touching a real user session; closed cleanly afterward). No PowerAtlas restart needed or performed. No source file modified — verification only. All 10 items from the brief covered; 9 PASS, SC5 and SC7 PASS (within the merged item 10), SC8 SKIP with a specific, disclosed reason (no genuine kiro-cli native sub-agent fan-out occurred despite a direct on-point attempt — the test session's own "delegate to sub-agents" request invoked an ordinary named tool, not kiro-cli's native multi-session RPC the crew panel's `subagents` frame depends on; confirmed against `acp.py`'s actual frame-emission call sites). Zero FAIL, zero BLOCKED, zero new defects found. All 10 Step 9 fixes (F1-F10) confirmed working live, most via fully real interaction (real steer sends acknowledged and visibly incorporated into agent responses, a real forced WS drop with a real ~2s automatic backoff reconnect, a real image staged/sent/dropped/restored-to-tray, a real command palette selection). Where synthetic frame injection was used (F3's crew-teardown setup, F4's `close_in_progress` race, F6's `agent_died` trigger), this was explicitly disclosed and matches the brief's own stated allowance for races impractical to reproduce live.

**`/acp` received its first live QA of this plan** at this step — every prior phase's Live QA had reported `/acp` unreachable ("403 Forbidden") and skipped it; this pass traced the cause to `web.py`'s `_acp_navigation_ok()` `Sec-Fetch-Site` cross-origin-navigation guard (a deliberate CSRF protection for a route that spawns an agent process on load, not an auth failure) and found the correct access path is a real in-page `<a href="/acp">` link click, not direct tool-driven URL navigation. `/acp` smoke test (palette selection, sid/copy widget confirming SC3's bug fix holds there too, debug log panel, real prompt send, real steer during an active turn) — all PASS.

**Two non-blocking observations recorded, not fixed** (informational, not findings): (1) the `/acp` reachability note above, worth keeping in mind for any future QA pass on this app; (2) `confirmCommandSelection()` silently no-ops with no `logLine` entry when `_dashAttachedSid` is null from never having attached at all (as opposed to attached-then-dropped, which logs correctly) — pre-existing, consistent with the function's own design (nothing to execute a command against), not a regression, and not the scenario F10 targets.

## Harness Improvement Opportunities

<None encountered during planning>

- A "1 qreview cycle per phase" cap suppresses `/qdev` Step 6's normal cycle-2 regression re-review, leaving only the orchestrator's own test re-run and diff read to catch a fix-introduced regression — cost: reduced independent-review assurance on every auto-fixed phase for the rest of this plan, versus the skill's default 2-cycle design — suggested change: document whether an explicit cycle-cap override is meant to skip only the *re-review*, or also the lighter self-verification substitute, so this judgment call doesn't have to be improvised per phase.
- A review finding whose fix falls outside the just-completed phase's declared file scope (here: a CSS fix needing `style.css`, not touched until Phase 2) has no named `/qdev` routing mechanism — Step 7's "Deferred exit criteria" is scoped to criteria the phase itself declared, not new findings discovered during its own review — cost: had to improvise a `(deferred from Phase 1)`-shaped bullet on the next phase rather than follow a documented path — suggested change: extend Step 6/7 with an explicit "cross-phase-scoped finding" routing rule alongside the existing deferred-exit-criteria one.
- A Phase 2 implementation sub-agent's session hit an account-level rate limit mid-phase (HTTP 429, "resets 7:20pm") with a stated wall-clock reset time — cost: ~15 minutes of otherwise-avoidable coordination overhead confirming the working tree was still clean and deciding whether/when to retry — suggested change: `/qdev`'s Interruption-and-recovery reference could name "resume via SendMessage once past a stated reset time" as the sanctioned response for this specific failure shape, distinct from the general sub-agent-failure escalation gate (which assumes an indefinite, not time-boxed, failure).
- The Chrome browser extension (`claude-in-chrome`) disconnected mid-plan and did not reconnect across ~5 retries spanning two phases, on both the orchestrator's and a sub-agent's independent sessions — cost: Phase 2's Live QA exit criterion could not be performed at all (not degraded, fully blocked), and the same blocker will likely recur for Phases 3-6 unless resolved — suggested change: `/qdev` Step 5b could name a "browser tool unavailable" outcome distinct from PASS/FAIL/BLOCKED/SKIP (or explicitly route to BLOCKED) with guidance to batch-defer Live QA across phases rather than re-attempt and re-document the same failure per phase. (Resolved for this plan: root cause was Chrome not running at all, not just the extension; fixed by restarting Chrome.)
- The same defect class — Progress Tracker / Status line / Implementation Divergences not updated for a just-completed phase — was independently caught by review 3 times running (Phase 1's review, Phase 2's review, and now Phase 3's, by 5 different reviewer instances across the three cycles) — cost: a recurring, fully-predictable review finding that never needed a fresh discovery, since it's identical every time (Step 7 items 1-4 are the orchestrator's own job, not something implementation sub-agents are asked to do) — suggested change: this is arguably not a "finding" at all but a Step 7 sequencing question — consider whether `/qdev` should explicitly forbid dispatching Step 5 review before Step 7's plan-file bookkeeping is done for the *previous* phase, or note in the Spawn brief contract that reviewers should skip this check entirely since it's mechanically guaranteed by process order, not a property of the implementation being reviewed.
- An implementation sub-agent's own self-reported `exit_criteria_ticked` count was wrong (Phase 5: reported 6, actual 7 of 8 total) and the orchestrator propagated that wrong number into three separate review dispatch briefs (`--expect-ticked 6`) without first counting the plan file's own checkboxes directly — cost: all three Phase 5 reviewers independently hit and had to report a `qvalidate` FAIL that traced back to the orchestrator's own unverified input, not a real implementation gap, adding three redundant "is this a real bug" investigations for a number the orchestrator could have gotten right by running `grep -c '^- \[x\]'` on the phase's own exit-criteria block before dispatching — suggested change: `/qdev` Step 5's Spawn brief contract could require the orchestrator to independently count ticked boxes from the plan file itself (not just relay the sub-agent's self-reported `exit_criteria_ticked` field) before passing `--expect-ticked N` to reviewers, catching a self-report/reality mismatch at dispatch time instead of three times downstream. (Partially self-correcting by Phase 6: that phase's own implementer independently recounted before reporting, after this exact item was recorded here, and got it right — the briefed instruction alone was enough without a skill change.)
- **Orchestrator process error, not a skill gap**: `/qreview` § Sub-agent execution already states "when a review whose brief invites live mutation testing... dispatches 2+ personas, dispatch them sequentially, not in parallel" — every per-phase review in this plan (Phases 1-6) invited mutation testing in the brief AND dispatched 2-3 personas in parallel, violating this existing rule. The predictable consequence surfaced in Phase 6: two parallel personas' concurrent edit-run-revert cycles against the same shared working tree caused transient, non-reproducible test failures for one of them (a `TEMP-VERIFY` test phantom-appearing/disappearing, the total oscillating between runs) — correctly self-diagnosed by the affected reviewer as interference, not a real defect (confirmed via clean `git status`/`git diff` before/after), but it cost reviewer time and could have misled a less careful one. Cost: the rule already existed and would have prevented this; the orchestrator simply didn't follow it. Corrected starting at Step 9 (dispatched read-only, no mutation testing, per the advisor's review of this exact plan). No skill change needed — this is a "read your own briefs against the cited rule before dispatching" discipline gap, worth naming so a future session doesn't repeat it.
- `/qqa`'s Browser-surface verification gate mandates citing a `browser_*` Playwright tool call, but this environment's browser automation surface is `mcp__claude-in-chrome__*`, not Playwright — cost: had to interpret the gate loosely (accepting `claude-in-chrome` tool calls as satisfying its intent) and dispatched Step 9b directly to a general-purpose sub-agent rather than routing through the named leaf skill `/qbrowser-test` as `/qqa` itself directs, an improvised deviation rather than a documented one — suggested change: either generalize the gate's wording to name the environment's actual browser-tool family instead of hardcoding Playwright, or clarify whether `/qbrowser-test` itself already abstracts over both tool families (in which case the gate's wording is accurate and the deviation was avoidable by reading further). The Step 9b QA outcome itself was rigorous (10 items, real interaction throughout, zero defects, one honest SKIP) — this is a bookkeeping gap, not a quality one.
