# ACP v2-to-v3 Engine Cutover

> **Date**: 2026-09-11
> **Status**: Complete — all 7 phases done, live-verified post-restart
> **Last Updated**: 2026-09-12
> **Scope**: Retire the live v2 ACP protocol engine, make v3 the sole engine behind `/acp`, delete v2-only code and rename the surviving v3 code to drop its suffix.
> **Estimated effort**: ~1-2 weeks (Major tier — architectural consolidation across two large files, a repo-wide rename with several genuine exceptions, and ~90 test artifacts)

---

## Completion Summary

### Acknowledged at archival

- **Documented divergence** — the final cleanup fixing ~91 stale `"ACP v3 ..."` log-string literals and one thread name in `acp.py`, plus 3 coupled test assertions pinning the old wording (commit `817ebf2`), never went through a sub-agent review cycle. **Accepted** — mechanical, string-content-only change with thorough direct verification already done by the orchestrator (clean import, full pytest 1535/0, exact grep-count match, zero duplicate defs).
- **Documented divergence** — two small follow-up fixes (README mode-terminology clarity and `acp.html` stale-comment corrections, commits `6f1e93a`/`840b0b3`) also never went through a formal sub-agent review, though the user explicitly authorized both directly ("1. fix. 2. fix."). **Accepted** — user-directed, independently spot-verified by the orchestrator.
- **Harness opportunity** — `pytest-timeout` not installed, causing silent `--timeout` flag failure and indefinite test hangs. **Fixed directly** (not a playbook governance promotion — this is a project-level fix): added `pytest-timeout>=2.3` to `pyproject.toml`'s dev extras, installed it in `.venv-PowerAtlas`, and added a note to `AGENTS.md`'s Doc & Test Guidelines recommending `--timeout=300` for concurrency-sensitive test runs (code: `e6eb630`).
- **Harness opportunity** — a suggested `/qdev` test-reconciliation discriminator category for `asyncio.Event`-based tests depending on retired code paths. **Skipped (harness opportunity): superseded by this plan's own resolution** — Phase 4 already found and fixed all 7 affected tests directly without this formal category existing.
- **Harness opportunity** — a rate-limit-killed sub-agent's partial edits deserve the same scrutiny as a self-directed "done" return. **Accepted (harness opportunity): single-occurrence observation this session; deferred to `/qdream`'s cross-plan harvest to confirm recurrence before promoting to `shared/skills/qdev/SKILL.md`.**
- **Harness opportunity** — every sub-agent brief granting commit authority must include the no-AI-attribution instruction (one omission this session produced a real policy-violating commit requiring a git-history rewrite to fix). **Accepted (harness opportunity): deferred to `/qdream`'s cross-plan harvest rather than promoted immediately, despite the concrete incident this session.**
- **Harness opportunity** — a phase's file-scope boundary routinely sits one file away from a stale reference it can't reach (observed twice in this plan). **Accepted (harness opportunity): observed twice in one plan; deferred to `/qdream`'s cross-plan harvest to confirm cross-plan recurrence before promoting a new `/qdev` sweep step.**
- **Harness opportunity** — this plan's own Phase 1 deferred work to "Phase 4/Phase 6" by name, but neither phase's file scope actually covered it — undetected through the whole plan and 8 review sub-agents. **Accepted (harness opportunity): a real, costly miss, but a single occurrence; deferred to `/qdream`'s cross-plan harvest before promoting a new `/qplan` Step 4 review check.**
- **Harness opportunity** — `qvalidate`'s `doc-updates` check can't distinguish "never addressed" from "verified, no change needed." **Accepted (harness opportunity): concrete, well-specified script-behavior change, but a single occurrence; deferred to `/qdream`'s cross-plan harvest before promoting a change to `qvalidate.{sh,ps1}`.** (This specific instance was independently resolved via a genuine documentation fix rather than an override — see `docs/KNOWLEDGE.md`, code: `7d7e00a`.)
- **Documentation gap** — `docs/KNOWLEDGE.md`'s `doc-updates` row initially showed FAIL (no commit touched it, despite Phase 6 confirming no change was needed). **Fixed** — resolved genuinely rather than carried as an override: added a dated re-verification annotation matching the file's own established convention (code: `7d7e00a`). `qvalidate --check-commits` now reports a clean PASS on all 8 checks.

Pass 4 (documentation-ripple sweep): 3 grep hits found across all tracked markdown outside the Documentation Updates table, all confirmed as deliberate, already-reviewed exceptions — the sibling `plans/260911_ACP_V3_FOLLOWUP_FEATURES.md` (a separate plan's own scope description, explicitly written to be execution-order-independent), `plans/CLOSED_INVESTIGATIONS.md` (D8's explicit historical-record exception), and `plans/tests/260701_POWERATLAS.md`'s own dated historical annotations (already handled in Phase 6 step 6). No fixes applied — zero genuine findings.

---

## Intent

### Problem statement & desired outcomes

PowerAtlas currently runs two parallel ACP (Agent Client Protocol) engines: `/acp` (v2, mature, spawns kiro-cli with blanket auto-approve `-a`) and `/acp-v3` (v3, kiro-cli `--agent-engine v3`, just brought to production quality by `plans/done/260909-1127_ACP_V3_PRODUCTION_HARDENING.md`). The merge of the two was explicitly deferred at the end of that plan ("kept parallel... lower-risk, separately-scoped future project", `plans/done/260909-1127_ACP_V3_PRODUCTION_HARDENING.md:685`).

This plan reopens that deferral: v3 becomes the only engine, reachable at `/acp` (the URL v2 currently owns). `/acp-v3` as a separate path is retired. v2's live protocol/supervisor code is deleted outright, not just left unreached — including restructuring `_SupervisorV3`, which currently subclasses `_Supervisor` (`acp.py:4697`), so it no longer depends on v2's class body. The surviving v3 code is renamed to drop its `_v3`/`V3` suffix, since there is no longer a second engine to disambiguate from — except for a small, identified set of names that denote a permanent store/provider-format distinction rather than engine identity (see §3 Design Decisions).

Desired outcome: one ACP engine, at one URL, with no dead v2 code left behind to maintain or reason about.

### Success criteria

- SC-1: `/acp` serves v3-descended sessions; `/acp-v3` and its dedicated route registrations no longer exist; the 5 `_ACP_V3_*` URL constants' *values* take over the 5 current v2 URL strings (`/acp`, `/ws/acp`, `/api/acp/sessions`, `/api/acp/workspaces`, `/api/acp/sessions/delete`).
- SC-2: v2-only live-protocol code is deleted: the `_Supervisor` class body (v2-only methods), the `_supervisor` global, `serve_socket`, `_dispatch`, the 9 unsuffixed `_handle_*` handlers, `ACP_ARGS`, `CLOSE_METHOD`, `_emit`, `_evict_crew_children`, `_build_kas_session_params`, `_crew_toolcallid` (D13). `_SupervisorV3` no longer inherits from `_Supervisor` — its 35 inherited-unchanged methods (D13) are folded directly into the sole surviving class, constructed eagerly at import time (D1).
- SC-3: The surviving engine's code is renamed to drop the `_v3`/`V3` suffix, **scoped to the live-engine files this plan touches** (`acp.py`, `web.py`, the templates, and their tests) — **not** a literal repo-wide sweep. `status_classifier.py`'s `classify_kiro_v3`/`_is_v3_format`/`_V3_SKIP_TYPES`, and `presence.py`/`data.py`'s `"kiro-cli-v3"`-provider-aware code, are out of this plan's rename scope entirely: they denote on-disk session-file *format*, used for both historical v2 and live v3 sessions (Q3's permanent distinction), not engine identity, and no phase touches them. Within the in-scope files, the exceptions are: `_lock_holder_v3` (+ `_V3_HOLDER_PID_UNKNOWN`/`_V3_HELD_STATUSES`/`_V3_SESSION_STALE_SECONDS`), `_stored_session_cwd_v3`, `_get_tool_diffs_v3`, the `data_kiro_v3` module, the `"kiro-cli-v3"` provider string and its `_ACP_V3_LISTING_PROVIDER` constant, and the `_context_percent_v3`/`_note_context_v3` pair (which coexists with its unsuffixed sibling — see D2).
- SC-4: `data_kiro.py`'s v2 session-file reader (`get_first_prompt`, `_extract_prompts_cached`, the `.json`/`.jsonl`/`.history` parsing under `KIRO_SESSION_DIR`) is untouched — the main dashboard can still browse/display the ~16,414 pre-existing v2 session files. **Scope clarification (Step 4 review)**: this is a pre-existing asymmetry, not introduced by this plan — the `/acp` page's own session rail (`_acp_listing`, renamed from `_acp_listing_v3`) is hard-scoped to the `"kiro-cli-v3"` provider today and does not enumerate v2 sessions, remotely or locally; only the main (loopback) dashboard's listing does. SC-4 covers the main dashboard, not a promise that `/acp`'s own rail gains v2-history browsing. `_acp_session_paths`, `_acp_delete_session`'s v2 branch, and `_stored_session_cwd` are confirmed to have no dependency on the deleted live-protocol code (D3 fixes the one exception found: `_lock_holder`'s v2-`_supervisor` self-check).
- SC-5: The dashboard's "💬 Open in Agent orchestrator" button (`session_row.html:31`) is hidden/disabled for old v2-provider rows, **and** the gate's positive case is widened to include `kiro-cli-v3` — today's gate (`in ('', 'kiro-cli')`) already excludes v3 sessions entirely; without this fix, zero session rows would show a live-resume button post-cutover (see D6).
- SC-6: `_REMOTE_ALLOWED_PATHS` (`web.py:1139-1171`) is reconciled from 13 entries to the 8-entry post-cutover set (§3 shows the exact before/after).
- SC-7: `tests/test_web.py` and `tests/acp_page.test.mjs` pass: ~55-60 v3-named tests renamed, ~26 KEEP-named tests (exercising the permanent-distinction set) left unchanged, 6 now-impossible/isolation-asserting cross-engine tests removed (corrected from 4 during Step 4 review — see Phase 4 step 0's discriminator rule), the additional non-v3-named cross-engine tests reviewed and updated, and the 3 `acp_page.test.mjs` ENGINE-ternary tests (`test_engine_v3_ws_path`, `test_engine_v3_session_api_url`, `test_engine_v3_workspaces_api_url`) rewritten — they currently assert v2 URLs are *absent*, which is no longer a meaningful assertion once there is only one engine.
- SC-8: No stray reference to a deleted/renamed v2 identifier or retired URL survives in tracked documentation, outside `plans/done/` and `plans/CLOSED_INVESTIGATIONS.md` (historical record, deliberately untouched — see D8).
- SC-9: The module imports cleanly post-consolidation with no NameError from a default-parameter forward reference (D4) or the `_lock_holder` self-check (D3); the merged supervisor is constructed eagerly at import time (D1, reopened during Step 4 review) so no unconditional call site (the 7 in §1) needs a `None`-guard; and no runtime-only reference — the CSRF path check at `web.py:912` (D10), the five `getattr`/held-set-union sites, or the `serve_socket_v3` direct call in `web.py` (D12, corrected during Phase 1 review) — is missed by a rename scoped only to symbol definitions.

### Scope boundaries & non-goals

- **Out of scope**: the permission-UX consequence of this cutover (v3's interactive `session/request_permission` approval becomes the only approval flow — v2's blanket `-a` auto-approve has no equivalent) is accepted as a deliberate, permanent product decision in this plan, not something to fix here. A v3-side auto-mode remains a separate, already-tracked ROADMAP item under "Session Control & Integration."
- **Out of scope**: the loss of server-side session-close wire confirmation (previously scoped only to v3 sessions, now universal since v2's `close_session`'s `_kiro.dev/session/terminate` call goes away with the rest of v2) is accepted as a permanent kiro-cli v3 binary limitation, not a defect to chase.
- **Out of scope**: the mode-switcher UI, `_pending_permission`/orphan-lock/steering-palette follow-ups — tracked separately in `plans/260911_ACP_V3_FOLLOWUP_FEATURES.md`. That plan's `_publish_live` (D32 orphan-lock) fix targets the same method this plan simplifies (D1/Phase 1 removes its v2-session union) — whichever plan lands second must re-read the method's then-current state rather than assume this plan's snapshot.
- **Non-goal**: this plan does not change kiro-cli itself or attempt to restore auto-approve-equivalent behavior for v3.

---

## 1) Current State

- `_SupervisorV3(_Supervisor)` (`acp.py:4697`) subclasses v2's `_Supervisor` (`acp.py:2618-4696`, ~2079 lines) and overrides 13 methods (`__init__`, `_on_notification`, `_on_agent_subtask_open`, `_on_agent_subtask_update`, `_spawn`, `_on_agent_request`, `_on_permission_request`, `_fulfill_token`, `new_session`, `load_session`, `close_session`, `_detach`, `_publish_live`). **35** methods are inherited unchanged (corrected during Step 4 review — the original enumeration omitted `_create_job`, called via `self._create_job()` inside `_SupervisorV3._spawn`): `alive`, `agent_pid`, `_get_start_lock`, `ensure_started`, `shutdown`, `_discard`, `_dispose`, `_tree_kill`, `_create_job`, `_request`, `_await_inactivity`, `_notify`, `_write`, `_reader_loop`, `_on_line`, `_post`, `_on_message`, `_on_response`, `_refuse`, `_stamp_activity`, `touch_used`, `_on_subagent_list`, `_note_subagent_action`, `_evict_finished_subagents`, `_compaction_session`, `_on_compaction_status`, `_on_agent_death`, `at_capacity`, `_flush_pending_commands`, `record`, `prompt`, `cancel`, `steer`, `commands_options`, `commands_execute`.
- Transport dispatch is two fully parallel chains: `serve_socket` (`acp.py:5939`) → `_dispatch` (`acp.py:6142`) → `CLIENT_TYPES` (`acp.py:162`, 9 types) → 9 unsuffixed `_handle_*` functions at `acp.py:6200,6572,6725,6831,7044,7123,7168,7309,7390`; vs. `serve_socket_v3` (`acp.py:6021`) → `_dispatch_v3` (`acp.py:6084`) → `CLIENT_TYPES_V3 = CLIENT_TYPES | {"permission_response"}` (`acp.py:177`) → 9 `_handle_*_v3` functions at `acp.py:7484,7591,7674,7758,7890,8159,8201,8044,8099` plus `_handle_permission_response_v3` (`acp.py:7958`, no v2 sibling ever existed).
- `web.py` mirrors the split: `acp_page`(1549)/`ws_acp`(1610)/`api_acp_sessions`(2518)/`api_acp_workspaces`(2707)/`api_acp_delete_sessions`(~3134) plus private helpers `_acp_listing`/`_acp_flat_listing`/`_acp_workspaces`/`_acp_status_for_held`(1851) are v2-only; `acp_v3_page`(1658)/`ws_acp_v3`(1686)/`api_acp_v3_sessions`(3258)/`api_acp_v3_workspaces`(3285)/`api_acp_v3_delete_sessions`(3298) plus `_acp_listing_v3`(2196)/`_acp_flat_listing_v3`(2433)/`_acp_workspaces_v3`(2679)/`_acp_status_for_held_v3`(1898) are their v3 counterparts. The 5 URL path constants are `web.py:692-729`; `_REMOTE_ALLOWED_PATHS` is `web.py:1139-1171` (13 entries today).
- `acp.html`'s engine-conditional surface is 5 lines (`templates/acp.html:657-666`): `var ENGINE = {{engine|tojson}}` plus 4 ternaries deriving `WS_PATH`/`RAIL_SESSIONS_API`/`PICKER_WORKSPACES_API`/`SESSION_DELETE_API`, out of 8034 total lines — confirmed by full-file grep, no other `ENGINE` reference exists. `templates/index.html:381`'s `openInAcp()` hardcodes `location.href='/acp?sid=...'` — needs no change, since `/acp`'s string value doesn't change, only its backing engine. `templates/partials/session_row.html:31` gates the resume button on `provider_name in ('', 'kiro-cli')` — `kiro-cli-v3` is not in this tuple today (confirmed via `presence.py`/`status_classifier.py`/`data.py`).
- `apply_config()` runs synchronously at `__main__.py:754`, before socket binding (`__main__.py:778`) — confirmed no race window for `_supervisor_v3 is None`.
- **Preserved-exception verification** (deep-research pass, 2026-09-11): `_acp_session_paths` (`web.py:2779`) and `_acp_delete_session`'s v2 branch (`web.py:2869-2917`) are pure file-store operations with **no dependency** on the live v2 protocol code — confirmed clean by direct read. `_acp_availability`'s v2 branch (`web.py:1843`) and `_acp_delete_many` (`web.py:2920-3012`) both call `acp._lock_holder`, which at `acp.py:2253` checks `pid == _supervisor.agent_pid()` — a direct reference to the v2 global being deleted. This is the one real dependency found; D3 below resolves it.
- **Two module-import-time crash risks** (Python evaluates default-parameter values at `def` time, not call time): `_flush_bubble(session_id, emit_fn=_emit)` (`acp.py:5839`) and `_deliver_load(..., subscribe_fn=_handle_subscribe)` (`acp.py:6536`) both default to names on the delete/rename list. Verified: every real call site already passes both arguments explicitly, so the defaults are functionally dead — but Python still evaluates them at definition time, so a rename that doesn't account for file-order (`_handle_subscribe`'s renamed source, `_handle_subscribe_v3` at `acp.py:7484`, sits *after* `_deliver_load` at `acp.py:6536` in the current file) produces a NameError on import. D4 resolves this.
- Seven further ripple-effect call sites (7th added during Step 4 review), traced by following every bare `_supervisor` reference (107 lines in `acp.py`, 12 in `web.py`): `_SupervisorV3._publish_live()` (`acp.py:5670-5686`) unions `_supervisor.sessions` into the combined live-session set; `set_sessions_changed_hook` (`acp.py:886`) calls `_supervisor._publish_live()` unconditionally; module-level `shutdown()` (`acp.py:5932-5936`) unconditionally calls `_supervisor.shutdown()`; `_sweepable`/`_sweep_once` (`acp.py:8344-8535`) run two full separately-coded sweep passes (~90 lines each) with dual-engine membership checks; `web.py:3267,3288,3308` (three `getattr(acp, "_supervisor_v3", None)` string-literal lookups) and `web.py:3339-3340,3390-3391` (two held-set unions with `acp._supervisor.sessions`) in the renamed `api_acp_sessions`/`api_acp_workspaces`/`api_acp_delete_sessions` survivors; `_Registry.attach`/`detach` (`acp.py:2061-2089`) dual-dispatch `touch_used` by membership check. All seven need updating as part of the consolidation (§3 D1, D5, D12; Phase 1, Phase 2) — with D1 now eager (reopened during Step 4 review), none of these need a `None`-guard, only a correct rename/repoint to the sole surviving supervisor.
- **A genuine, un-renameable naming collision**: `_context_percent`(1716)/`_context_percent_v3`(1738) and `_note_context`(5889)/`_note_context_v3`(5908) are four distinct functions that **all four survive** — the single surviving `_SupervisorV3._on_notification` (`acp.py:4755-5075`) calls both the unsuffixed pair (lines 4787, 4802) and the suffixed pair (line 4994: `_note_context_v3(session_id, _context_percent_v3(_kiro_meta))`) within the same method body, because they parse two different payload shapes v3's own protocol emits. See D2.
- `PROMPT_SILENCE_SECONDS` (`acp.py:765`) and the AGENTS.md restart constraint are unaffected by and out of scope for this plan.

## 2) Goal

Delete v2's live ACP protocol code, fold v3's supervisor into a standalone class (no longer subclassing v2), rename the survivor to drop its version suffix (with a small, explicit set of permanent exceptions), and repoint `/acp` at it — while keeping v2's historical session-file reading intact for the dashboard.

## 3) Design Decisions

| Decision | Choice | Alternatives considered | Rationale |
|---|---|---|---|
| Q1: Scope organization | Two plans: this cutover + a separate bundled follow-ups plan | One combined plan; six fully decomposed plans | User's explicit choice — isolates the high-risk architectural change from independent smaller features |
| Q2: Depth of replacement | Deep retirement — delete v2-only code now, restructure `_SupervisorV3` off `_Supervisor` | Routing-level takeover only (v2 code stays, unreached); routing-level + permanent v2 retention | User's explicit choice, confirmed non-default (recommended option was routing-level takeover) |
| Q3: Historical v2 data | Keep `data_kiro.py`'s v2 reader untouched | Retire historical browsing along with the live engine | User's explicit choice; only the live protocol engine retires, not the on-disk history |
| Q4: Naming | Rename to drop `_v3`/`V3` suffix repo-wide | Keep `_v3` suffix permanently | User's explicit choice — refined by this plan's research into the exception set (D2, SC-3) the exploration interview did not have visibility into |
| Q5: Permission UX | Accept v3's interactive approval now; auto-mode stays a separate future ROADMAP item | Block cutover on building auto-mode first; add a scoped trust-session shortcut to this plan | User explicitly declined `/qcouncil` and chose directly; recommended option |
| Q6: Old-session button | Hide/disable for old v2-provider rows | Leave as-is, let it fail gracefully | User's explicit choice; recommended option — refined by D6 below, which this plan's research found necessary to make SC-5 actually work |
| D1: Supervisor construction | **Reopened during Step 4 review.** Keep the merged supervisor **eagerly** constructed at module import time, matching v2's original `_supervisor = _Supervisor()` pattern | Lazy construction in `apply_config`, matching v3's current pattern (originally chosen, then reversed) | Review found `tests/test_web.py`'s `acp_session`/`acp_store` fixtures (332 call sites) reach into `acp_mod._supervisor.sessions/.history/...` with no construction step, assuming an eager singleton exactly like v2's; going lazy would break all 332 at fixture setup, none of them findable by Phase 4's `grep "v3\|V3"` sweep since none reference v3 by name. Eager construction loses nothing: a constructor failure becomes an import failure, already caught by the existing `_ACP_IMPORT_ERROR`/guarded-import mechanism at the module boundary — the same safety net `apply_config`'s try/except was providing. Eager also eliminates the need to audit every unconditional call site for a `None`-guard (7 confirmed sites, see Phase 1 step 9) |
| D2: `_context_percent`/`_note_context` collision | Keep all four functions (`_context_percent`, `_context_percent_v3`, `_note_context`, `_note_context_v3`) exactly as named — do not rename the `_v3` pair | Force a rename with new distinguishing names (e.g. `_context_percent_from_notification`) | Both pairs are actively called together from the single surviving `_on_notification`, parsing genuinely different payload shapes; renaming either `_v3` one would collide with its still-live unsuffixed sibling. A larger rename is out of scope for a plan whose goal is dropping suffixes, not inventing new names |
| D3: `_lock_holder`'s v2 self-check | Delete the `pid == _supervisor.agent_pid()` check (`acp.py:2253`) outright | Repoint it to the renamed supervisor | **Reworded per Step 4 review**: the staleness/skew check at `acp.py:2249` does not actually catch "this pid is genuinely our own still-running process" (a live, non-recycled pid passes it cleanly) — that clause of the original rationale doesn't hold under inspection. The real, structural reason the deletion is safe: the sole surviving `load_session` (v3-descended) writes `.history` files, never `.lock` files, so the scenario this self-check exists for (our own live pid appearing in a `.lock` file from our own failed load) is unreachable once v2's `.lock`-writing code path is deleted, independent of staleness. `_lock_holder` is documented as "a hint, never a grant" (`web.py:1811`), so even a wrong deletion would only cause a spurious fail-closed refusal on a legacy v2 delete, never an over-grant |
| D4: Forward-reference-unsafe defaults | Remove the `emit_fn=_emit`/`subscribe_fn=_handle_subscribe` defaults from `_flush_bubble`/`_deliver_load`; make both required keyword arguments | Rely on post-rename file ordering to keep the reference valid | **Reworded per Step 4 review**: every call site that *survives Phase 1's deletions* already passes both explicitly — not "every real call site" in the pre-Phase-1 file, where at least 8 v2-only call sites (`acp.py:4012,4041,4166,4184,6701,6722,6950,7039`) rely on the default. Those sites are themselves deleted by Phase 1 (they live in v2-only `_on_notification`/`_handle_load`/`_handle_prompt`), so the engineering conclusion holds — a required kwarg removes the NameError risk unconditionally rather than depending on where in the file the renamed function ends up |
| D5: Dual sweep passes | Collapse `_sweepable`/`_sweep_once`'s two ~90-line passes into one, keeping **both** of the two passes' non-overlapping pieces: the v2 pass's subscriber-notification-on-release block (`acp.py:8479-8490`, `session_closed` frame sent to subscribers) and the v3 pass's `_pending_early_frames` orphan-buffer sweep (`acp.py:8517-8535`, SC-1). **Do not assume either is redundant without first confirming**: before merging, read `_SupervisorV3.close_session` (renamed) to check whether it already emits an equivalent subscriber notification independently of the sweep loop — if yes, the v2 pass's block is genuinely redundant and can be dropped; if no, v3 sessions have been silently missing that notification and the block must be added, not merged as a no-op | Leave both passes, have the v2 pass become dead code | Only one engine exists post-cutover; a dead 90-line pass is exactly the "no dead v2 code left behind" the Problem statement commits to. **Amended per Step 4 review**: the two passes are not mirrors — a naive "collapse into one" leaves an undocumented judgment call about which pieces survive |
| D6: Dashboard resume-button gate | Widen `session_row.html:31`'s positive case to include `kiro-cli-v3`, in addition to Q6's negative-case fix for old v2 rows | Ship only Q6's fix | Today's gate already excludes `kiro-cli-v3` entirely; without this, post-cutover the button would show for *zero* rows — old v2 rows newly hidden by Q6, v3 rows (the only live sessions left) already excluded by the pre-existing tuple. Confirmed a real, currently-live gap, not a hypothetical |
| D7: `memory/MEMORY.md` entries | Update the ~13 entries' cited identifier names; preserve the underlying documented facts | Leave them referencing now-renamed/deleted names | The facts (protocol behavior, empirical findings) remain true; only the names they cite go stale |
| D8: `plans/CLOSED_INVESTIGATIONS.md` | Leave its 10 hits untouched | Update to current naming | Dated, historical record of what was true when investigated — not live documentation; rewriting it would misrepresent the historical record |
| D9: `plans/ROADMAP.md`'s merge item | Remove the "Merge `/acp` and `/acp-v3`" item (line 39) as superseded, rather than edit it in place | Edit it to describe this plan | This plan supersedes the item entirely once complete; a stale "still to do" entry for already-done work is worse than removing it |
| D10 *(added, Step 4 review)*: `same_origin_guard`'s CSRF path check | Simplify `web.py:912`'s `request.url.path in (_ACP_PATH, _ACP_V3_PATH)` to `request.url.path == _ACP_PATH`; update `_acp_navigation_ok`'s docstring (`web.py:854`) to drop its `/acp-v3` mention | Leave the tuple form, relying on `_ACP_PATH`/`_ACP_V3_PATH`'s renamed definitions to resolve correctly | This line lives inside a function body (evaluated per-request, not at import time) and gates the *only* loopback-originated CSRF-navigation defense for `/acp` — a missed rename here is invisible to the plan's "clean import" check and its URL-literal grep (§7), and would only surface as a runtime `NameError` on the first served request after `_ACP_V3_PATH`'s old name is gone. Security-relevant: this is not a cosmetic rename site |
| D11 *(added, Step 4 review)*: `acp_page`/`ws_acp`'s security-rationale docstrings | Fold the deleted v2 `acp_page`'s docstring content (`web.py:1548-1559`, explaining why `_request_host_allowed` is deliberately re-checked despite `same_origin_guard` already covering it) and the deleted v2 `ws_acp`'s docstring content (`web.py:1610-1638`, the disproved "phantom control" finding about `_ws_origin_ok`) directly into their renamed survivors, replacing the survivors' current "Mirrors `acp_page`/`ws_acp`, see there for rationale" stub text | Leave the stub text as-is | Once the v2 functions are deleted, "see `acp_page` for security rationale" on `acp_page` itself is a dangling self-reference with the actual rationale silently gone — a future maintainer could read the now-stub-only doc and conclude the "redundant-looking" host check is dead code, and remove it, reopening the DNS-rebinding token-leak gap the check exists to close |
| D12 *(added, Step 4 review; corrected during Phase 1 review, 2026-09-12)*: web.py-side `_supervisor_v3`/`serve_socket_v3` references Phase 1 doesn't reach | Phase 1 is scoped to `acp.py` only. **Corrected enumeration** (Phase 1's Senior engineer + Reliability engineer reviews independently found the original list undercounted): **five**, not three, `getattr(acp, "_supervisor_v3", None)` string-literal lookups (`web.py:3184,3237,3267,3288,3308`); two `acp._supervisor.sessions` held-set unions (`web.py:3339-3340,3390-3391`); and one direct call `await acp.serve_socket_v3(ws)` (`web.py:1703`, inside `ws_acp_v3`) that the original D12 missed entirely — none of these are covered by any Phase 1 step and all need their own explicit Phase 2 step | Assume Phase 1's `acp.py` rename covers these because they reference the same concept; trust the original three/two count without re-enumerating against the actual file | These are `web.py`-side literals/expressions/calls, invisible to a rename scoped to `acp.py`; left alone, the `getattr` calls silently return `None` post-rename (dead attribute, zeroing a "held" count), the held-set unions raise `AttributeError`, and the direct `serve_socket_v3` call raises `AttributeError` the moment a client hits `/ws/acp-v3` post-restart — all three are currently inert only because no restart has occurred since Phase 1 landed (AGENTS.md's restart-required rule; the plan withholds the only restart until Phase 7) |
| D13 *(added, Step 4 review)*: Inherited-method count and v2-only orphan helpers | §1/Phase 1's "34 inherited methods" is corrected to **35** (`_create_job`, a `_Supervisor` static method called via `self._create_job()` inside `_SupervisorV3._spawn`, was omitted). `_build_kas_session_params` (`acp.py:700`, v2-only, called only from deleted v2 code) and `_crew_toolcallid` (`acp.py:5692`, v2-only, called only from deleted v2 `_handle_subscribe`) are added to Phase 1's Delete list | Trust the original enumerated lists as complete | Both gaps were found by independently diffing the two classes' full method sets and every caller of the two helpers against the plan's lists, rather than trusting the lists themselves. A missed `_create_job` fold-in breaks every session spawn (`AttributeError` in the sole surviving `_spawn`); missed deletion of the two v2-only helpers doesn't crash (Python's last-definition-wins silently shadows the orphan with the renamed v3 survivor) but leaves ~15 unreachable lines, violating SC-2/SC-8's "no dead v2 code" commitment in a way no `v3`-keyed grep would catch |

## 4) External Dependencies & Costs

### Required external changes

| Category | Change needed | Owner | Status |
|---|---|---|---|
| Rollout / cutover | Coordinated PowerAtlas restart (Phase 7) — kills every live kiro-cli process for both engines, per AGENTS.md:5 | User | Pending |

All other rows from the template do not apply — no cloud resources, IAM, CI/CD, secrets, DNS, or third-party services are touched by this plan.

### Cost impact

None. No hosting, compute, API-call-volume, storage, or licensing change.

## 5) Implementation Phases

### Phase 1: Backend engine consolidation (acp.py) [QA]
**Goal**: Fold `_SupervisorV3` into a standalone class, delete all v2-only live-protocol code, rename the survivor per SC-3's rule and exceptions, and resolve the 7 ripple-effect call sites found in §1.
**Covers**: SC-2, SC-3, SC-9

**File scope**: `src/power_atlas/acp.py`

Steps:
1. Delete `_Supervisor`'s v2-only body (`acp.py:2618-4696`) down to the 35 methods it shares with `_SupervisorV3` (§1 list, corrected — includes `_create_job`) — fold those 35 directly into what remains, dropping the class's separate identity. **Do not delete** the module-level `_supervisor = _Supervisor()` construction statement itself (`acp.py:4683`) — under D1 (reopened, eager), the renamed sole supervisor keeps this exact construction pattern, just against the renamed class and at its post-fold-in position in the file.
2. Delete `ACP_ARGS` (`acp.py:642`), `CLOSE_METHOD` (`acp.py:459`), `serve_socket` (`acp.py:5939`), `_dispatch` (`acp.py:6142`), the 9 unsuffixed `_handle_*` functions, `_emit` (`acp.py:5701`), `_evict_crew_children` (`acp.py:6781`), and (D13, added during Step 4 review) two v2-only orphan helpers: `_build_kas_session_params` (`acp.py:700`) and `_crew_toolcallid` (`acp.py:5692`) — both are called only from code already being deleted, and must be removed *before* step 3 renames their v3-suffixed siblings onto the same names, or Python's last-definition-wins rebinding silently shadows them as unreachable dead code instead of actually replacing them.
3. Rename the survivors per SC-3: `_SupervisorV3`→`_Supervisor`, `serve_socket_v3`→`serve_socket`, `_dispatch_v3`→`_dispatch`, the 8 paired `_handle_*_v3`→`_handle_*`, `_handle_permission_response_v3`→`_handle_permission_response`, `ACP_V3_ARGS`→`ACP_ARGS`, `CLOSE_METHOD_V3`→`CLOSE_METHOD`, `_supervisor_v3`→`_supervisor`, `_build_kas_session_params_v3`→`_build_kas_session_params`, `_crew_toolcallid_v3`→`_crew_toolcallid`, `_emit_v3`→`_emit`, `_evict_crew_children_v3`→`_evict_crew_children`, `_KIRO_V3_TOKEN_BINARY`→`_KIRO_TOKEN_BINARY`, `CLIENT_TYPES_V3`→ merge into `CLIENT_TYPES` (adding `permission_response`), `SERVER_TYPES`'s v3-only additions stay merged into the one set.
4. **Do NOT rename** (SC-3 exceptions): `_lock_holder_v3`, `_V3_HOLDER_PID_UNKNOWN`, `_V3_HELD_STATUSES`, `_V3_SESSION_STALE_SECONDS`, `_stored_session_cwd_v3`, `_get_tool_diffs_v3`, `_context_percent_v3`, `_note_context_v3` (D2 — leave both pairs exactly as-is).
5. Apply D3: delete the `pid == _supervisor.agent_pid()` self-check at `_lock_holder` (formerly `acp.py:2253`).
6. Apply D4: remove the `emit_fn=_emit` default from `_flush_bubble` and the `subscribe_fn=_handle_subscribe` default from `_deliver_load`; make both required keyword arguments; update their call sites (already pass both explicitly per §1 — confirm no site relies on the default).
7. Apply D5: collapse `_sweepable`/`_sweep_once`'s two sweep passes into one.
8. Simplify `_SupervisorV3._publish_live()` (renamed) to publish only `self.sessions` — remove the `frozenset(self.sessions) | v2_sessions` union (`acp.py:5670-5686`), since there is only one engine's sessions to publish. **Leave the `pid=0` sentinel as-is** — that is the separate D32 orphan-lock fix tracked in `plans/260911_ACP_V3_FOLLOWUP_FEATURES.md`, out of scope here; note in a code comment that this method is shared cutover ground with that plan.
9. Apply D1 (reopened, eager): repoint the 7 ripple-effect call sites in §1 to the renamed sole supervisor by name only — `set_sessions_changed_hook` (`acp.py:886`), module-level `shutdown()` (`acp.py:5932-5936`, the 7th site, added during Step 4 review), `_Registry.attach`/`detach` (`acp.py:2061-2089`, collapse the dual-dispatch to one unconditional call). Since construction is eager, **none of these need a `None`-guard** — that was D1's original (now-reversed) lazy framing; a plain unconditional reference to the renamed `_supervisor` is correct and sufficient.
10. Sweep in-code comments (not just identifiers) for text that explains the *current* v2/v3 split and will misdescribe reality once it's gone — confirmed instances: `acp.py:166-177` (the `CLIENT_TYPES`/`CLIENT_TYPES_V3` rationale, obsolete once step 3 merges the two sets) and the present-tense `-a`/trust-all-tools framing in comments at `acp.py:356,896,2178` (v2's blanket auto-approve is being deleted; v3's interactive-approval model should be described in its place, not left describing removed behavior as current).
11. Read the current file fresh before editing — line numbers throughout this plan are as of 2026-09-11 and will drift after step 1's large deletion.

**Exit criteria**:
- [x] `_Supervisor` class contains no v2-only method; `_supervisor` (renamed from `_supervisor_v3`) is the sole module-level instance, constructed **eagerly** at import time per D1 (reopened)
- [x] Every rename in step 3 applied; every exception in step 4 preserved unchanged; the two v2-only orphan helpers (D13) deleted before their v3-suffixed siblings were renamed onto the same names
- [x] D3, D4, D5 applied; D5's sweep-pass merge decision (subscriber-notification block, `_pending_early_frames` sweep) resolved by actually reading `close_session`, not assumed — confirmed v3's `close_session` already broadcasts `session_closed` and detaches subscribers itself, so the v2 pass's block was genuinely redundant and was dropped; the orphan-buffer sweep was kept
- [x] `_publish_live`'s v2-session union removed (step 8), sentinel pid left as-is
- [x] **Scope clarified during Phase 1 review**: of the 7 ripple-effect call sites named in §1, the 5 that live in `acp.py` (`_publish_live`'s union, `set_sessions_changed_hook`, module-level `shutdown()`, `_sweepable`/`_sweep_once`, `_Registry.attach`/`detach`) are repointed to the renamed sole supervisor by name, no `None`-guards added (none needed under eager construction). The remaining 2 §1-named sites live in `web.py` (the `getattr`/held-set-union call sites) and are correctly **out of Phase 1's file scope** — deferred to Phase 2 step 5 per D12 (whose enumeration was itself corrected during this review, see D12). Additionally, ~12 dead `if _supervisor is None: ... return` guards inside individual handler functions (left over from lazy-construction-era code, beyond the 5 acp.py-side named sites) were found and removed for consistency with D1's "no guards needed anywhere"
- [x] `grep -rn "SupervisorV3\|_supervisor_v3\|CLIENT_TYPES_V3\|_dispatch_v3\|serve_socket_v3" src/power_atlas/acp.py` returns zero hits outside the SC-3 exception set — confirmed clean
- [x] `python -c "from power_atlas import acp"` succeeds with no NameError or AttributeError — **caveat**: this exact command fails with `AttributeError: partially initialized module 'power_atlas.data_kiro'... (most likely due to a circular import)`, but this reproduces identically against the pristine pre-Phase-1 file (verified against the git-committed original) — it is a pre-existing `data.py`/`data_kiro.py` circular-import fragility triggered only by this specific bare import order, entirely outside acp.py and Phase 1's file scope. The equivalent, robust checks both succeed: `from power_atlas import web` (the real application entry point, which imports acp internally) and `from power_atlas.data import Session; from power_atlas import acp` (the exact order `tests/test_web.py` itself uses). No NameError/AttributeError originates from acp.py.
- [x] Full pytest suite scoped to `acp.py`-testable behavior passes, **including** `acp_session`/`acp_store`-fixture-based tests (`tests/test_web.py`, 332 call sites) — these depend on eager construction (D1) and must not be treated as "expected failures until Phase 4"; only tests referencing a renamed/deleted symbol by name are expected to fail here. **Result**: 1421 passed, 127 failed, 2 deselected (see Implementation Divergences for the full breakdown) — every failure traces to a stale v2-only name (`_SupervisorV3`/`_supervisor_v3`, ~82 of the 127, all in `TestSupervisorV3`) or a stale v2-only behavioral assumption the plan deliberately retires (close_session's wire round-trip, `.lock`-file-based preflight, `result.sessionId` vs `result._meta.id`, v2's `crew_spawn_toolcallids`-keyed crew mechanism) — confirmed individually for every non-`TestSupervisorV3` failure, not assumed. Zero failures trace to acp.py logic being wrong.

Implementation (2026-09-12, code: c89a8e7)
The implementing sub-agent's work was interrupted (laptop shutdown) after it had fully applied the phase but before it could return its structured report or commit. The orchestrator recovered the uncommitted working-tree state at session resume and independently re-verified it against every exit criterion above rather than trusting the sub-agent's self-report at face value: re-ran the stale-name grep (clean), the two robust import checks plus a temporary swap-in of the pristine pre-Phase-1 `acp.py` to confirm the bare-import circular-import failure is genuinely pre-existing, an AST-based duplicate-def scan across module and class scopes (zero hits, confirming the two self-caught bugs described below were actually fixed), a spot-check of the D2 four-function survival and the D13 orphan-helper restoration, and a fresh full `pytest tests/test_web.py` run that reproduced the exact 1421/127/2 breakdown independently, including a manual trace of three non-`TestSupervisorV3` failures across three different claimed failure categories to confirm none was a Phase 1 logic bug in disguise. Full detail on what was implemented and the divergences found along the way is in this phase's Implementation Divergences entry below (written directly by the interrupted sub-agent before the interruption, preserved verbatim). The code commit above is orchestrator-authored on the sub-agent's behalf per the interrupted-session recovery path, since the sub-agent itself never reached its own commit step.

**Step 5b QA verification**: SKIP. Phase 1 is backend-only (`acp.py`), touches no route registration, and produces no user/browser-reachable behavior change by itself — `web.py` still dispatches to the pre-cutover names until Phase 2 lands, and per `AGENTS.md`, Python changes require a restart to take effect at all, which this plan withholds until Phase 7. A live/browser `/qqa` check right now would observe the still-running pre-Phase-1 process, not this phase's code — it cannot produce a meaningful verdict. Verification for this phase is the clean-import + full-pytest-suite pair already required by its own exit criteria (both independently reproduced above); live, integrated QA is Phase 7's explicit job (SC-1, SC-4, SC-5) and Phase 3's exit criteria already establish this same precedent for pre-restart phases.

### Phase 2: Backend routing & endpoints (web.py) [QA] [P:3]
**Goal**: Repoint `/acp` at the renamed engine, retire `/acp-v3`'s routes, migrate the 5 URL constants' values, reconcile `_REMOTE_ALLOWED_PATHS`.
**Covers**: SC-1, SC-6, SC-9

**File scope**: `src/power_atlas/web.py`

Steps:
1. Delete `acp_page`(1549), `ws_acp`(1611), `api_acp_sessions`(2518), `api_acp_workspaces`(2707), `api_acp_delete_sessions`(~3129), `_acp_listing`(2041), `_acp_flat_listing`(2311), `_acp_workspaces`(2639), `_acp_status_for_held`(1851). **Before deleting**, copy `acp_page`'s docstring (`web.py:1548-1559`, the `_request_host_allowed`-repetition rationale) and `ws_acp`'s docstring (`web.py:1610-1638`, the disproved "phantom control" / `_ws_origin_ok` finding) aside — step 2 folds them into the renamed survivors (D11).
2. Rename the v3 survivors to drop the suffix: `acp_v3_page`→`acp_page`, `ws_acp_v3`→`ws_acp`, `api_acp_v3_sessions`→`api_acp_sessions`, `api_acp_v3_workspaces`→`api_acp_workspaces`, `api_acp_v3_delete_sessions`→`api_acp_delete_sessions`, `_acp_listing_v3`→`_acp_listing`, `_acp_flat_listing_v3`→`_acp_flat_listing`, `_acp_workspaces_v3`→`_acp_workspaces`, `_acp_status_for_held_v3`→`_acp_status_for_held`. Inside the renamed `acp_page`, drop the `"engine": "v3"` template variable entirely (Phase 3 removes the template's need for it). Apply D11: replace the renamed `acp_page`'s current "Mirrors `acp_page` with `engine='v3'`. See `acp_page` for security rationale" stub with the actual v2 docstring content saved in step 1 (rewritten to no longer describe itself in the third person); do the same for the renamed `ws_acp`.
3. Migrate URL path constant values: `_ACP_V3_PATH`("/acp-v3")→`_ACP_PATH`("/acp"), `_ACP_V3_WS_PATH`("/ws/acp-v3")→`_ACP_WS_PATH`("/ws/acp") — the first-ever named constant for this path, replacing the bare `"/ws/acp"` literal that appears nowhere else after step 1's deletions — `_ACP_V3_LISTING_PATH`→`_ACP_LISTING_PATH`("/api/acp/sessions"), `_ACP_V3_WORKSPACES_PATH`→`_ACP_WORKSPACES_PATH`("/api/acp/workspaces"), `_ACP_V3_DELETE_PATH`→`_ACP_DELETE_PATH`("/api/acp/sessions/delete"). Delete the 5 old v2 constants entirely.
4. Apply D10: in `same_origin_guard`, simplify `request.url.path in (_ACP_PATH, _ACP_V3_PATH)` (`web.py:912`) to `request.url.path == _ACP_PATH`; update `_acp_navigation_ok`'s docstring (`web.py:854`) to drop its `/acp-v3` mention. This reference lives inside a function body — not caught by an import-time check or a URL-literal grep — so treat it as a named step, not an incidental consequence of the constant rename in step 3.
5. Apply D12 (corrected enumeration, Phase 1 review): rename the **five**, not three, `getattr(acp, "_supervisor_v3", None)` string literals (`web.py:3184,3237,3267,3288,3308` — the original enumeration missed 3184 and 3237) to `"_supervisor"`; collapse the two `frozenset(sv3.sessions if sv3 else ()) | frozenset(acp._supervisor.sessions)`-shaped held-set unions (`web.py:3339-3340,3390-3391`) into a single reference to the sole renamed supervisor. **Also apply** (found by the same review, missing from the original D12 entirely): inside step 2's renamed `ws_acp` (formerly `ws_acp_v3`), update its body's direct call `await acp.serve_socket_v3(ws)` (`web.py:1703`) to `await acp.serve_socket(ws)` — this is a functional call, not a literal/getattr, and left alone raises `AttributeError` the first time a client connects post-restart. Sweep the handful of now-stale comment-only mentions of `_supervisor_v3`/`serve_socket_v3` found alongside these (`web.py:1689,3172,3254,3265,3299,3302`) while touching each function.
6. Reconcile `_REMOTE_ALLOWED_PATHS` (`web.py:1139-1171`) from the current 13 entries to exactly the 8 keys below — **preserve each surviving entry's existing rationale comment** (`web.py:1144-1149` for workspaces, `1151-1158` for delete, `1160-1164` for restart); delete only the `# v3 ACP paths — parallel to the v2 entries above` header (`web.py:1165`) and the 5 keys it introduces:
   ```python
   _REMOTE_ALLOWED_PATHS: dict[str, str] = {
       _REMOTE_AUTH_PATH: "http",
       _ACP_RESTART_PATH: "http",
       _ACP_PATH: "http",
       _ACP_WS_PATH: "websocket",
       _ACP_LISTING_PATH: "http",
       _ACP_WORKSPACES_PATH: "http",
       _ACP_DELETE_PATH: "http",
       _REMOTE_STATIC_MOUNT: "http",
   }
   ```
7. **Keep unchanged** (D-exceptions, per Phase 1 step 4's rationale extended to web.py — and out of this plan's rename scope entirely per SC-3's narrowing, since these denote store/provider format, not engine identity): `_ACP_V3_LISTING_PROVIDER = "kiro-cli-v3"` (`web.py:1778`), the `PROVIDER_COLOR`/`PROVIDER_LABEL`/`PROVIDER_ICON`/`launcher_arg` dict entries for `"kiro-cli-v3"` (`web.py:89,95,101,107`), and the `sess_`-prefix shape-dispatch logic in `_acp_availability`/`_acp_delete_session`/`_acp_delete_many` (these route between the now-sole live v3 store and v2's permanently-preserved historical store — SC-4 depends on this staying, not becoming dead code).
8. Apply D3's downstream effect: confirm `_acp_availability`'s v2 branch and `_acp_delete_many` no longer hit the deleted `_lock_holder` self-check (Phase 1 already removed it at the source).

**Exit criteria**:
- [x] `/acp` and `/ws/acp` resolve to the renamed v3-descended engine; `/acp-v3`, `/ws/acp-v3` and their 3 API siblings no longer route anywhere
- [x] `same_origin_guard`'s CSRF check (D10) and the `getattr`/held-set-union sites (D12, corrected — 5 getattr sites + the direct `serve_socket_v3` call) are updated — import-time and stale-name-grep clean confirmed now; live served-request verification is Phase 7's job as originally scoped
- [x] The renamed `acp_page`/`ws_acp` carry the folded-in v2 security rationale (D11), not a dangling "see `acp_page`" self-reference — extended by the same reasoning to the other renamed handlers whose stub docstrings would otherwise dangle (see Implementation Divergences)
- [x] `_REMOTE_ALLOWED_PATHS` matches the 8-entry table in step 6 exactly, with every surviving entry's rationale comment intact
- [x] `_acp_availability`/`_acp_delete_session`/`_acp_delete_many`'s `sess_`-prefix dispatch logic is unchanged and still functions (Phase 4 will re-verify with updated tests; a `TypeError` from stale test mocks not passing `include_v3` is a known Phase-4 item, not a dispatch-logic defect — see Implementation Divergences)
- [x] `"kiro-cli-v3"` provider identity constants (`_ACP_V3_LISTING_PROVIDER`, `PROVIDER_*` dict entries) are unchanged

Implementation (2026-09-12, code: c3ff937)
A first attempt at this phase was interrupted by an unrelated rate-limit error partway through implementation and left the delete/rename direction backwards: it deleted the v3-suffixed survivor functions and kept the v2-named ones nearly untouched, while also repointing three v3-suffixed API handlers' route decorators onto the same URL constants the untouched v2 handlers still used — producing three duplicate FastAPI route registrations for the same path. Since Starlette/FastAPI resolves route conflicts by registration order, this would have silently served the old v2 handlers' logic instead of the intended v3-descended ones, defeating the phase. That partial state was never committed; the orchestrator stashed it and re-dispatched a fresh implementation sub-agent with an explicit brief describing the exact pitfall and a mandatory pre-completion check (`grep` confirming each of the 5 route decorators appears exactly once). The retry completed cleanly: delete-then-rename in the correct direction throughout, D10/D11/D12(corrected)/`_REMOTE_ALLOWED_PATHS` all applied, zero duplicate route registrations (independently re-verified by the orchestrator), zero stale-name grep hits, clean import, zero duplicate module-level defs (AST scan). pytest: 1378 passed / 170 failed / 2 deselected vs. Phase 1's 1421/127/2 baseline — the increase is fully accounted for: the orchestrator independently traced two representative failure clusters via direct traceback inspection (`TestAcpListingEndpoint`'s 26 failures all share one assertion expecting the stale `"kiro-cli"` provider name where the code now correctly returns `"kiro-cli-v3"`; `TestAcpDeleteEndpoint`'s failures are a `TypeError` from mock lambdas not accepting the new, correct `include_v3=True` keyword the merged delete endpoint passes) and confirmed both are stale pre-Phase-4 test assumptions, not web.py logic defects. Full detail on the divergence and the extended D11-style docstring folding (applied beyond D11's literal function list, for the same dangling-self-reference reason) is in this phase's Implementation Divergences entry below.

**Step 5b QA verification**: SKIP, same rationale as Phase 1 — backend-only, no live-reachable surface without the Phase 7 restart. `same_origin_guard`'s CSRF path check and the D12 runtime-only references are confirmed clean by grep/import now; their actual runtime behavior under a live served request is Phase 7's explicit job (the phase's own exit criteria already scope it that way).

Post-review auto-fix (code: ffe2870): applied the 6 review findings above (see Review Log). Docstring/comment/log-string/variable-naming only; pytest re-run reproduced the identical 1378/170/2 baseline, confirming no behavior change.

### Phase 3: Frontend — ENGINE collapse & dashboard gate fix [QA] [P:2]
**Goal**: Remove the now-dead `ENGINE` branching in `acp.html`; fix `session_row.html`'s resume-button gate (D6).
**Covers**: SC-1, SC-5

**File scope**: `src/power_atlas/templates/acp.html`, `src/power_atlas/templates/partials/session_row.html`

Steps:
1. In `acp.html:657-666`, replace the `ENGINE`-ternary block with hardcoded constants using the post-migration v2-spelled URL strings: `WS_PATH = '/ws/acp'`, `RAIL_SESSIONS_API = '/api/acp/sessions'`, `PICKER_WORKSPACES_API = '/api/acp/workspaces'`, `SESSION_DELETE_API = '/api/acp/sessions/delete'`. Remove the `var ENGINE = {{engine|tojson}}` line.
2. In `session_row.html:31`, change `provider_name|default('') in ('', 'kiro-cli')` to `provider_name|default('') in ('', 'kiro-cli', 'kiro-cli-v3')`.
3. Apply Q6's negative-case fix in the same edit: for a row whose `provider_name == 'kiro-cli'` (a historical v2 session — confirmed identifiable since `data_kiro.py`'s reader still populates this provider tag per SC-4), hide/disable the resume button specifically, distinct from the general gate widened in step 2. Concretely: the gate in step 2 controls whether the button renders *at all*; a second, narrower condition disables it specifically for `provider_name == 'kiro-cli'` rows (old v2 history) while leaving it enabled for `kiro-cli-v3` rows (the only live sessions left).

**Exit criteria**:
- [x] `acp.html` has no remaining reference to `ENGINE` or the `engine` template variable (grep confirms zero hits)
- [x] `session_row.html`'s resume button renders for `kiro-cli-v3` rows, and is hidden/disabled specifically for `kiro-cli` (v2, historical) rows
- [x] **Verified with a substitute method — the browser extension was unavailable this session (Claude-in-Chrome not connected)**. Hard browser reload / live console-error check could not be performed directly. Substitute evidence: `curl` against the live (pre-restart) `/acp-v3` page returns HTTP 200 with the hardcoded `WS_PATH`/`RAIL_SESSIONS_API` constants present in the served HTML (confirming no server-side template error from the `engine` variable removal), plus `node tests/acp_page.test.mjs`'s DOM-stand-in harness exercising the actual rendered script end-to-end (see next criterion) as a proxy for "JS executes with no console error." Scope unchanged from the plan: page-load/script-execution only, not session functionality — pre-restart, the running Python process still routes per the old dispatch until Phase 7's restart.
- [x] `node tests/acp_page.test.mjs` run — exactly the 3 `test_engine_*` tests fail (expected, Phase 5's job), independently confirmed by stashing this phase's edits and re-running (431 passed/5 failed pristine vs. 428 passed/8 failed with this phase's changes — a clean drop of exactly 3, the 5 other failures reproduced identically on pristine code, confirmed pre-existing and unrelated)

Implementation (2026-09-12, code: 99634de)
This phase's implementing sub-agent was interrupted by the same unrelated rate-limit error as Phase 2's first attempt, but here it had already finished its edits and was mid-way through its own pre-completion verification (last visible action: "consult the advisor before running verification") when killed. The orchestrator read the recovered uncommitted diff directly, confirmed it matched the plan's 3 steps exactly, and independently verified it (stash-and-rerun `node tests/acp_page.test.mjs` to isolate this phase's effect from pre-existing failures, `grep` for zero ENGINE/engine references, a live HTTP fetch of `/acp-v3` to confirm no server-side render error) before committing on the sub-agent's behalf. No divergences — this phase applied exactly as specified with no need for judgment calls.

**Step 5b QA verification**: partially performed live (see exit criteria above) — a live HTTP fetch and DOM-harness execution stood in for a genuine browser console check, which was unavailable this session. Full session-level QA is Phase 7's job as originally scoped.

Post-review auto-fix (code: d1186e4): applied the review's CSS finding (see Review Log). **Harness note**: the auto-fix sub-agent's first commit of this fix (`12c11b8`) carried a `Claude-Session:` attribution trailer, violating this project's standing governance ban — an orchestrator oversight (this specific auto-fix brief omitted the no-attribution instruction that every other sub-agent brief in this session included). Caught by a full-history trailer scan before proceeding, corrected via `git commit-tree`/`git update-ref` (rewriting only this commit's message, tree content byte-identical, verified via empty diff) since the commit was local, unpushed, and the current branch tip — the precedent-approved remediation for exactly this scenario per governance. Logged under Harness Improvement Opportunities below.

### Phase 4: Test suite — tests/test_web.py [QA] [P:5,6]
**Goal**: Rename, delete, or update every ACP-related test to match the consolidated engine.
**Covers**: SC-7

**File scope**: `tests/test_web.py`

Steps:
0. **Discriminator rule (added, Step 4 review)** — apply before bucketing any test: a test body referencing **both** the suffixed and unsuffixed spelling of the same identifier (`_dispatch`+`_dispatch_v3`, `CLIENT_TYPES`+`CLIENT_TYPES_V3`, `_supervisor`+`_supervisor_v3`) is cross-engine-scoped, never a plain rename target — DELETE it if it asserts *isolation* between the two spellings (the assertion becomes false once Phase 1 merges them, not merely stale), or re-scope it per step 3 if it asserts *coexistence*. A mechanical "rename internal references" pass does not fix a false assertion.
1. **Rename** (~55-60 tests, the RENAME-target bucket from the enumeration research — full list is drift-prone by line number; regenerate via `grep -n "v3\|V3" tests/test_web.py` against the then-current file and cross-reference against the KEEP/DELETE buckets in steps 2-3 before renaming anything): update test names and internal references (function calls, URL literals, class names) to match Phase 1/2's renames.
2. **Delete** (6 tests under step 0's isolation rule, now-impossible scenarios — corrected from 4 during Step 4 review): `test_a_v3_session_open_in_the_other_engine_is_refused`(16324), `test_deleting_a_v2_session_open_in_the_other_engine_is_refused`(17299), `test_v2_endpoint_workspace_delete_ignores_v3_sessions_in_the_same_cwd`(17411), `test_v2_endpoint_workspace_delete_is_unaffected_by_a_v3_session_held_elsewhere`(17432), and (found by the discriminator rule) `test_permission_response_routing_isolated_between_v2_and_v3_dispatch`(23752-23819, asserts `"permission_response" not in acp_mod.CLIENT_TYPES` — false once step 1's `CLIENT_TYPES` merge lands) and `test_all_declared_v3_client_types_are_routed`(23821-23859, iterates the deleted `CLIENT_TYPES_V3` over `_dispatch_v3`) — the latter becomes a duplicate of `TestAcpDeclaredTypesAreRouted`(7037-7068) once both dispatchers merge into one; fold the merged-set assertion into that surviving class instead of keeping both.
3. **Review and update, not delete** (non-v3-named but cross-engine-scoped, per step 0): `TestApiAcpDeleteSessionsV2RegressionForMixedWorkspace` and any sibling test asserting behavior across both a live v2 session and a live v3 session — these need to become tests of "a historical v2 file entry coexisting with a live v3 session," not deleted outright, since SC-4's historical-browsing guarantee is exactly what they should now verify.
4. **Keep unchanged** (~26 tests, exercise the permanent-distinction set — SC-3 exceptions): `TestClassifyKiroV3`+members, `test_kiro_v3_*`, `test_classify_from_path_routes_v3_provider_to_v3_classifier`, `TestAcpAvailabilityV3`+members, `TestAcpSessionsForWorkspaceV3`+members, `TestAcpDeleteSessionV3Dispatch`+members, `test_get_tool_diffs_v3_*`, `test_stored_session_cwd_v3*`, `test_lock_holder_v3_*`. **Exception within `TestSupervisorV3`**: this class is itself mixed — its `_get_tool_diffs_v3`/`_stored_session_cwd_v3` coverage stays, but its `_SupervisorV3._fulfill_token` coverage is a RENAME target (bucket 1) since `_fulfill_token` itself is not in the permanent-distinction set.
5. Update the 69 v2-URL literal occurrences (`"/acp"` ×49, `"/ws/acp"` ×14, `"/api/acp/sessions"` ×3, `"/api/acp/workspaces"` ×1, `"/api/acp/sessions/delete"` ×2) that were previously exercising v2's *own* route — confirm each still makes sense post-cutover (most should, since the URL strings are unchanged, only their backing engine is) or update per step 3's re-scoping.
6. Update the 1 v3-URL literal (`"/api/acp-v3/sessions/delete"`, `TestApiAcpV3DeleteSessionsEndpoint`, line 17260) — this class is a RENAME target.

**Exit criteria**:
- [x] Full pytest run passes with zero references to a deleted v2-only function/class/constant remaining — 1533 passed, 0 failed, 0 skipped (independently reproduced, no `-k` exclusion needed — the two previously-hanging tests are fully fixed, not skipped)
- [x] The 6 now-impossible/isolation-asserting cross-engine tests are removed (step 2); the mixed-scenario tests (step 3) are re-scoped to "historical v2 + live v3," not deleted — **corrected during implementation**: the plan's named 6 resolved to 4 outright deletions + 2 rescopes (the containing class tests coexistence, not isolation, per step 0's own discriminator rule), plus a third previously-unnamed sibling test in that same class needing identical treatment; 13 further tests were deleted beyond the plan's list because their assertions became false or unreachable once specific v2-only mechanisms (lock-file preflight, pre-close wire handshake, the `supervisor is None` branch) were actually deleted, not merely renamed — see Implementation Divergences for the full itemized accounting (17 net deletions, not 6)
- [x] The ~26 KEEP-named tests are unchanged and still pass, confirming the permanent-distinction set (SC-3) survived Phase 1/2 correctly — one docstring parenthetical in `TestAcpAvailabilityV3` was updated to name a renamed caller; zero behavior change
- [x] The `acp_session`/`acp_store` fixtures work unmodified against the eagerly-constructed renamed supervisor (D1) — confirmed: all 332 call sites needed no changes

Implementation (2026-09-12, code: f0557f5)
Reconciled `tests/test_web.py` against the merged engine. Root-caused and fixed the two previously-hanging tests (`test_a_second_close_cannot_overtake_the_first`, `test_a_prompt_arriving_during_a_close_is_refused`): `close_session` has zero internal `await` points (independently confirmed by the orchestrator via direct read), so the original wire-call-mocking technique never intercepted anything and the tests blocked forever. Rewritten to patch `close_session` itself with an artificial suspension point — the same fix applied to 5 sibling tests found during the same investigation. A vacuous assertion (`test_a_session_inside_the_ttl_is_left_alone`, which could no longer distinguish "left alone" from "silently terminated") was found and fixed on final review. Full detail is in Implementation Divergences below. The orchestrator independently reproduced the exact 1533/0/0 result, an AST duplicate-def scan (clean), `_check_test_names.py` (clean), a direct read of `close_session`'s body confirming zero `await` statements, and a direct read of the rewritten hang-prone test confirming the fix's correctness.

**Step 5b QA verification**: N/A — test-suite-only phase, no runtime UI/HTTP surface of its own; the pytest run itself is the verification.

Post-review auto-fix (code: 39d5a1c): applied the 6 review findings above (see Review Log) — an undocumented deletion comment, 2 new regression tests closing real coverage gaps, dead mock-scaffolding removal, and a wording correction. Orchestrator independently re-verified: 1535 passed/0 failed/0 skipped, zero duplicate defs, both new tests read in full and confirmed to assert real current behavior.

### Phase 5: Test suite — tests/acp_page.test.mjs [QA] [P:4,6]
**Goal**: Rewrite the 3 tests that currently assert v2 URLs are absent, to match single-engine reality.
**Covers**: SC-7

**File scope**: `tests/acp_page.test.mjs`

Steps:
1. Rewrite `test_engine_v3_ws_path`(10123), `test_engine_v3_session_api_url`(10137), `test_engine_v3_workspaces_api_url`(10151): remove the `ENGINE`-ternary premise entirely (Phase 3 deleted the variable). Replace with direct assertions that `WS_PATH === '/ws/acp'`, `RAIL_SESSIONS_API === '/api/acp/sessions'`, `PICKER_WORKSPACES_API === '/api/acp/workspaces'` — no longer conditional on anything.
2. Sweep the file for any other `/acp-v3`, `/ws/acp-v3`, `/api/acp-v3/*` literal (10, 3, 6 occurrences respectively per the enumeration research) and update or remove as appropriate.

**Exit criteria**:
- [x] `node tests/acp_page.test.mjs` passes in full, including the 3 rewritten tests — 431 passed / 5 failed (up from 428/8); the 3 targeted tests pass, the 5 remaining failures are pre-existing and confirmed unrelated (reproduced identically on pristine pre-Phase-3 code during Phase 3's own review)
- [x] Zero remaining `/acp-v3`-family literal in the file — confirmed

Implementation (2026-09-12, code: ef094ab)
Rewrote the 3 `test_engine_v3_*` checks to assert directly against the single hardcoded `WS_PATH`/`RAIL_SESSIONS_API`/`PICKER_WORKSPACES_API` values, following the file's existing verify-via-observed-effect pattern. Renamed the checks away from their `test_engine_v3_*` names (the only checks in the file using that naming convention) since retaining "v3"/"engine" language on assertions that no longer branch on anything would mislead. Also removed a now-dead `engine` context key from `loadPage()`. The plan's original 10/3/6 literal-count estimate was stale; re-verified against the current file directly (9 total occurrences found, all now removed).

**Step 5b QA verification**: N/A — test-suite-only phase.

Post-review auto-fix (code: 9803aba): applied the review's assertion-precision finding (see Review Log). Orchestrator independently confirmed the fix is correctly scoped and the pass/fail count is unchanged.

### Phase 6: Documentation sweep [P:4,5]
**Goal**: Update every live documentation reference to the retired v2/v3 split; leave historical records untouched (D8).
**Covers**: SC-8

**File scope**: `README.md`, `AGENTS.md`, `docs/KNOWLEDGE.md`, `plans/ROADMAP.md`, `memory/MEMORY.md`, `plans/tests/260701_POWERATLAS.md`

Steps:
1. `README.md`: rewrite the `## Agent sessions, v3 protocol (/acp-v3)` section (line 311, ~10 lines following) — the "two engines" framing itself is retired, this needs substantive rewriting, not a name-swap. Line 61 (kiro-cli v3 sessions scanned separately from v2, a store-format fact) stays.
2. `AGENTS.md`: update line 7's "`/acp-v3` uses the same template with `engine="v3"`" to reflect the removed `ENGINE` variable (Phase 3).
3. `docs/KNOWLEDGE.md`: update the `_get_tool_diffs_v3` mechanism note at line 122 only if its cited call sites changed name (per SC-3, `_get_tool_diffs_v3` itself is unrenamed — likely no change needed beyond confirming its cross-references still resolve). Line 45's `kiro-cli --agent-engine v1|v2|v3` flag documentation is a kiro-cli CLI fact, unrelated to this plan — leave as-is.
4. `plans/ROADMAP.md`: apply D9 — remove the `[POST-SPIKE] Merge /acp and /acp-v3...` item (line 39) as superseded by this plan. Update the "ACP v3 Follow-up" section's remaining references to `_SupervisorV3`/`CLOSE_METHOD_V3`/`/acp-v3` to match the post-rename names, or add a forward-pointer note where the underlying item now lives in `plans/260911_ACP_V3_FOLLOWUP_FEATURES.md`. Leave the unrelated `conversations_v2` sqlite-table reference (line 166) untouched — confirmed unrelated to the ACP engine split.
5. `memory/MEMORY.md`: apply D7 — update the ~13 entries' cited identifier names (`_SupervisorV3.close_session`, `CLOSE_METHOD_V3`, `_lock_holder_v3`, `_handle_new_v3`, `_Supervisor._publish_live`, and others) to their post-rename form, preserving the documented facts themselves.
6. `plans/tests/260701_POWERATLAS.md`: update line 174's references to the 5 `_ACP_V3_*` constants and their values, and the test class names listed there (`TestAcpSessionsForWorkspaceV3`, `TestAcpDeleteSessionV3Dispatch`, `TestAcpDeleteManyV3Dispatch`, `TestApiAcpV3DeleteSessionsEndpoint`, and the non-v3-named `TestApiAcpDeleteSessionsV2RegressionForMixedWorkspace` at line 361) to match Phase 4's actual disposition of each.
7. **Do not touch** (D8): `plans/CLOSED_INVESTIGATIONS.md`'s 10 hits — historical record.

**Exit criteria**:
- [x] `README.md`'s v3-protocol section rewritten; no reference to `/acp-v3` as a live, separate path remains in any file this phase covers — the two-section split was merged into one (see Implementation Divergences), a more thorough treatment than a name-swap
- [x] `grep -rn "acp-v3\|_v3\b\|SupervisorV3\|CLOSE_METHOD_V3" README.md AGENTS.md docs/KNOWLEDGE.md plans/ROADMAP.md memory/MEMORY.md plans/tests/260701_POWERATLAS.md` returns only expected hits from the permanent-distinction set (SC-3) or explicitly-preserved facts — confirmed by the orchestrator; remaining hits are SC-3 exceptions and clearly-labeled historical text (the `260701_POWERATLAS.md` section 2.27 rewrite explicitly marks its retained pre-cutover description as historical)
- [x] `plans/CLOSED_INVESTIGATIONS.md` is unmodified — confirmed, zero diff

Implementation (2026-09-12, code: e2550ba, 14153ee)
Steps 1-5 and 7 landed as a parallel-dispatched sub-agent (code: e2550ba); step 6 (`plans/tests/260701_POWERATLAS.md`) was deliberately deferred — it needed Phase 4's actual test disposition, which the concurrent dispatch couldn't know yet despite the plan's `[P:4,5,6]` annotation — and completed as a small follow-up once Phase 4 landed (code: 14153ee). `docs/KNOWLEDGE.md` needed no edits (confirmed via direct code read that its cross-references still resolve). Full detail, including the README.md section-merge rationale and the `260701_POWERATLAS.md` byte-corruption workaround, is in Implementation Divergences below.

**Step 5b QA verification**: N/A — prose-only phase, no runtime surface.

Post-review auto-fix (code: 9eea759): applied the review's 3 README factual-accuracy findings (see Review Log). Two Low findings (a readability regression, an out-of-scope stale comment in `acp.html`) are recorded as `Orchestrator: proposed-accept — pending user decision`, not silently closed. Orchestrator independently confirmed the fix commit is clean and spot-verified one claim directly against `data_kiro_v3.py`.

### Phase 7: Restart coordination & live verification
**Goal**: Deploy the consolidated engine and verify every success criterion against the real, running instance.
**Covers**: SC-1, SC-4, SC-5

**File scope**: none (verification only)

Steps:
1. Confirm all prior phases' automated tests (pytest, `node tests/acp_page.test.mjs`) pass before requesting a restart.
2. Present the restart as needed to the user; do not restart autonomously (AGENTS.md:5). Every currently-open v2 and v3 session dies at this restart — this is the same pre-existing cost any `acp.py`/`web.py` change already carries, not something this plan adds.
3. Post-restart, live-verify each SC: `/acp` opens a v3 session (SC-1); `/acp-v3` returns 404 or an equivalent "not found" (SC-1); the dashboard still lists and can open historical v2 sessions, with the resume button correctly hidden for them (SC-4, SC-5); a live v3 session's resume button works (SC-5); old bookmarked v2-shaped `?sid=` links degrade gracefully (already confirmed via code reading during exploration — re-confirm live).
4. Run the full pytest suite and `node tests/acp_page.test.mjs` one more time against the restarted, live instance.

**Exit criteria**:
- [x] User has confirmed the restart — triggered via the live `/api/restart` endpoint (the same call the `/acp` page's own restart button makes, invoked directly to avoid its blocking `confirm()` dialog inside browser automation) at the user's explicit go-ahead
- [x] All SC-1 through SC-9 verified live, not just by code reading — see Implementation notes below for exactly what was live-tested vs. confirmed via the running system continuing to function correctly (SC-2/3/6/8/9 are code-structure/hygiene criteria with no independent browser-clickable surface of their own; their "live" verification is that the restarted system works end-to-end with no import/runtime error, which a structural regression in any of them would have broken)
- [x] Full pytest + `node tests/acp_page.test.mjs` pass post-restart — 1535 passed/0 failed and 431/436 (5 pre-existing unrelated failures), both reproduced against the live restarted instance

Implementation (2026-09-12)
Restart triggered live via browser automation once the user gave explicit, direct authorization ("Go ahead and [restart it]") after being told the exact blast radius (every live kiro-cli ACP session across all workspaces terminates). Confirmed via `curl` before and after: `/acp-v3` was 200 pre-restart (old code still serving it) and 404 post-restart; `/api/acp-v3/sessions` also 404 post-restart.

**SC-1** (live-verified): opened a real historical session via the dashboard's "Open in Agent orchestrator" button (proper in-app navigation, not a bare URL hit — a bare reload of `/acp?sid=...` correctly 403s, confirming `_acp_navigation_ok`'s CSRF check is active). The session loaded with its full history replayed, showed `connected`, and a live prompt round-trip completed successfully (sent "Just say OK, testing the live connection." and received "OK, testing the live connection." back). Closed the session afterward and got the exact expected local-cleanup message ("This session was closed. Its agent-side processes and its replay buffer are gone... The conversation itself is still in the agent's own store, so it can be reopened from the PowerAtlas dashboard"), confirming `close_session`'s no-wire-call local cleanup (Q5/D-accepted-limitation) works correctly live, not just in tests. Zero browser console errors during the entire session.

**SC-4/SC-5** (live-verified): filtered the dashboard's session list to `kiro-cli` (historical v2) — every row's resume button was present but `disabled: true` with the tooltip "Unavailable — this session is from the retired v2 engine" (confirmed via direct DOM inspection, not just visual read). Filtered to `kiro-cli-v3` — every row's resume button was present and `disabled: false`. Both the D6 negative case and the pre-existing positive case are correctly live.

**Backwards-compatibility item** (live-verified): navigated to `/acp?sid=old-v2-style-id-not-a-real-session` from within the app (same-origin JS navigation). Got a clean `session not loaded` status badge and a specific agent error (`[agent_error] Invalid params: cwd must be an absolute path (code -32602)`) with clear user-facing guidance — no crash, no unhandled exception, matching the plan's "confirmed via code reading, re-confirm live" note exactly.

**SC-2/3/6/8/9** (confirmed via the live system's correct end-to-end operation, not a dedicated browser action): the dashboard loaded, the session listing rendered real provider-tagged data correctly (exercising the D12-fixed `getattr`/held-set-union sites), the CSRF/navigation guard (D10) correctly allowed the legitimate in-app navigation and rejected the bare reload, and the full pytest/node suites passed against the live process — any live regression in the deleted v2 code, the rename scope, the `_REMOTE_ALLOWED_PATHS` reconciliation, stray documentation references, or the eager-construction/runtime-reference fixes would have surfaced as an import failure, a broken dashboard, or a failing test, none of which occurred.

## 6) Risk Assessment

| Risk | Impact | Mitigation |
|---|---|---|
| Phase 1's mechanical rename/delete is large enough that a manual pass could miss a reference | Module fails to import, or a subtle behavior change ships silently | Exit criteria require a clean import plus full pytest pass before Phase 2 begins; Phase 4/5 test updates independently re-verify every renamed symbol is actually exercised |
| The `_context_percent`/`_note_context` collision (D2) is easy to mis-simplify if a future editor doesn't know why both pairs exist | A "helpful" cleanup rename reintroduces the collision or silently drops one payload shape's parsing | D2's rationale is recorded in this plan and should be left as an inline code comment at the point of definition (addressed within this plan, Phase 1) |
| D1's lazy-construction choice means every unconditional call site must be found, not just the two named in §1 | A missed call site raises `AttributeError: NoneType` in production if ACP fails to initialize | Phase 1 exit criteria explicitly require auditing beyond the two named sites; Phase 7's live verification includes a cold-start check |
| Phase 6's documentation sweep is easy to under-scope (memory/MEMORY.md was not in the original exploration's risk list — found only by this deep-research pass) | Stale documentation ships, misleading a future session | Phase 6 lists every file explicitly with a grep-verifiable exit criterion, rather than an open-ended "update docs" instruction |
| Restart timing (Phase 7) is entirely user-dependent per AGENTS.md | Plan cannot fully complete/verify without user action | Addressed within this plan — Phase 7 explicitly waits for user confirmation, all other phases are structured to be fully testable via pytest/node beforehand |

## 7) Verification

- `python -c "from power_atlas import acp, web"` — clean import, no NameError/AttributeError (Phase 1, 2).
- Full pytest suite (project's existing invocation) — Phase 4's updates make this the authoritative pass/fail signal for Phases 1-2's correctness.
- `node tests/acp_page.test.mjs` — Phase 5's updates make this authoritative for Phase 3's correctness.
- `grep -rn "acp-v3\|/ws/acp-v3\|api/acp-v3" src/power_atlas tests` — zero hits after Phase 1-5 (a stray literal is exactly the failure mode a mechanical rename risks).
- Live browser verification against the restarted instance (Phase 7) — reuses the disposable-subprocess and live-composer-check techniques already proven during this plan's exploration.

## 8) Documentation Updates

| Document | Update needed | Phase |
|---|---|---|
| `README.md` | Rewrite the v3-protocol/two-engine section | 6 |
| `AGENTS.md` | Update the `engine="v3"` template-variable reference | 6 |
| `docs/KNOWLEDGE.md` | Confirmed no change needed — `_get_tool_diffs_v3`'s cross-references (verified against `acp.py` and `data_kiro_v3.py` directly) still resolve correctly as-is | 6 (verified, not edited) |
| `plans/ROADMAP.md` | Remove the superseded merge item; update the ACP v3 Follow-up section's identifier references | 6 |
| `memory/MEMORY.md` | Update ~13 entries' cited identifier names | 6 |
| `plans/tests/260701_POWERATLAS.md` | Update constant/test-class references to match Phase 4's disposition | 6 |
| `plans/CLOSED_INVESTIGATIONS.md` | None — explicitly preserved (D8) | doc-table-only |

## 9) Implementation Divergences from Plan

### Phase 1 (2026-09-11)

- **Two dead-`.lock`/collision bugs introduced and self-caught during implementation, not shipped**: (1) the class-merge step's initial assembly accidentally dropped `_build_kas_session_params_v3`'s function body entirely while keeping its two call sites — caught via an `ast`-based before/after name-diff against the pristine original, fixed by restoring it (renamed) in place of the v2-only orphan it replaces. (2) A line-range miscalculation while collapsing `_Registry.attach`/`detach`'s dead dual-engine branches produced a duplicate `__init__`/partial-duplicate `attach` inside `_Registry` (harmless at runtime under Python's last-definition-wins, but exactly the silent-shadowing hazard AGENTS.md's `_check_test_names.py` note warns about) — caught via a per-class duplicate-method `ast` scan, fixed by deleting the stray first copy. Both are recorded so a future reader auditing this diff's history understands the two `acp.py.*.bak` snapshots in scratch, if seen, were intermediate — not alternate shipped versions.
- **A third bug reached the point of breaking test runs before being caught**: D4's `_flush_bubble` signature change (`emit_fn=_emit` → keyword-only, no default) was applied to the signature but not, at first, to `_flush_bubble`'s own ~9 call sites inside `acp.py`, which all used the old positional style (`_flush_bubble(session_id, _emit)`) — this raises `TypeError` at the first bubble-flush, at runtime, not at import or compile time. Caught by manual re-inspection of every call site before the first full test run (not by the test run itself, which had not yet been re-launched against this state); fixed by updating all 9 sites to `emit_fn=_emit`. `_deliver_load`'s `subscribe_fn` was already keyword-only before this plan (unaffected).
- **D5's resolution, concretely**: read `_SupervisorV3.close_session` (now the sole `close_session`) directly — it already sends `_session_closed_frame` to the closing session's own subscribers and detaches them (lines then-numbered 5630-5633), something v2's `close_session` never did (v2 relied on its caller — `_handle_close` or the sweep's own notification block — to do this). Confirmed independently via `_handle_close_v3`'s own comment ("close_session already broadcasts session_closed and detaches sockets"). The merged `_sweep_once` therefore drops the v2 pass's notification block entirely and keeps the `_pending_early_frames` orphan-buffer sweep, which has no v2 counterpart.
- **Beyond the 7 named ripple-effect call sites**, roughly a dozen additional `if _supervisor is None: ... return` guards inside individual v3-descended handler functions (`_handle_subscribe`, `_handle_load`, `_handle_new`, `_handle_prompt`, `_handle_steer`, `_handle_permission_response`, `_handle_cancel`, `_handle_close`, `_handle_commands_options`, `_handle_commands_execute`, `_crew_toolcallid`, `_evict_crew_children`) were left over from lazy-construction-era code and are now permanently dead under eager construction. These were not in the plan's explicit list but are the same class of cleanup D1 calls for ("no `None`-guards are needed anywhere") — removed for consistency, verified via `grep -n "_supervisor is None\|_supervisor is not None"` returning zero hits afterward.
- **Comment sweep (step 10) scoped narrower than a full pass**: applied to the plan's explicitly named instances (the `CLIENT_TYPES`/`CLIENT_TYPES_V3` rationale block, and the three confirmed `-a`/trust-all-tools mentions), plus several more found opportunistically while reading affected regions (a self-referential "full copy of `_on_notification`... replaced by `_emit_v3`" comment that became literally nonsensical once both names collapsed to the same spelling; six "Mirror of `_handle_X`" docstrings on functions that renaming made self-referential; "base class"/"subclasses `_Supervisor`"/"archived spike" framing in `_on_agent_request`, `new_session`, `close_session`, and `_emit_subagents_frame`'s docstrings). **Not swept**: `log.info`/`log.warning` call sites that still read `"ACP v3 ..."` (e.g. `"ACP v3 socket %s closed"`, `"ACP v3 tool %s: ..."`, `"ACP v3 sweeper: ..."`) — these are executable string literals, not comments, and a full sweep would touch dozens of call sites across the file; left for Phase 4/Phase 6 to reconcile alongside the test assertions that key off the old `"ACP socket"`/`"ACP tool_call"` (no `v3`) log phrasing (see test-failure breakdown below). **Correction (final verification, 2026-09-12)**: this deferral named the wrong phases — Phase 4's file scope was `tests/test_web.py` and Phase 6's was documentation files; neither phase's actual scope ever included `acp.py`, so these ~91 log-string/thread-name literals were never touched by either phase and only surfaced during the plan's final `§7 Verification` grep sweep, after Phase 6 had already landed. Fixed as a standalone cleanup commit (`fix(260911_ACP_V2_TO_V3_ENGINE_CUTOVER): drop stale v3 qualifier from acp.py log messages and update pinning tests`, code: `817ebf2`) — 91 log-message/thread-name literals corrected in `acp.py`, plus 3 test assertions in `tests/test_web.py` that pinned the exact old wording, landed together in one commit since splitting them would have left a broken intermediate state in git history. Full pytest suite reproduced at 1535 passed / 0 failed after the fix.
- **`python -c "from power_atlas import acp"` (exit criterion, verbatim) fails on pristine pre-Phase-1 code too**: confirmed by running the identical command against the git-committed original `acp.py` before any edit — `AttributeError: partially initialized module 'power_atlas.data_kiro' ... has no attribute 'SESSION_DIR' (most likely due to a circular import)`, from `data.py`/`data_kiro.py`'s mutual import when `acp.py` is the very first thing to touch `data_kiro`. `from power_atlas import web` (real entry point) and `from power_atlas.data import Session; from power_atlas import acp` (exactly `tests/test_web.py`'s own import order) both succeed. Reported as a harness/environment finding, not treated as blocking — see Harness Improvement Opportunities.
- **Test triage (127 failures + 2 deselected out of 1550 collected)**: none trace to acp.py logic being wrong. ~82 are `TestSupervisorV3` referencing the deleted `_SupervisorV3`/`_supervisor_v3` names directly (`sv3 = acp_mod._SupervisorV3()` style) — confirmed every single `AttributeError` in that class names one of exactly those two identifiers, nothing else. The remaining ~45 were each individually traced (not assumed) to one of: (a) `CLOSE_METHOD`'s value now being `None` and the sole `close_session` never calling `_request`/`_write` (v2's close *was* a wire round-trip; v3's, the sole survivor, never was — probe AS-5), (b) `_handle_load`'s sole survivor using `_lock_holder_v3`/`session.json` status rather than `_lock_holder`/`.lock` files for its preflight, (c) `new_session`'s sole survivor reading `result._meta.id` rather than `result.sessionId`, (d) `_handle_prompt`/`_handle_subscribe`'s sole survivors reading `_active_fan_out_wave` for the current-fan-out id rather than `crew_spawn_toolcallids` (v2's crew-list-update mechanism vs. v3's tool-call-tagged one), (e) log-message text still reading `"ACP v3 ..."` where a test's `_logged_socket_ids`-style helper regex-matches the old unprefixed `"ACP socket"`/`"ACP tool_call"` wording. Two of these ((a)-shaped) are additionally `asyncio.Event`-synchronized to a `_request`/`_write` call inside the close path that no longer happens — `test_a_second_close_cannot_overtake_the_first` and `test_a_prompt_arriving_during_a_close_is_refused` — and **hang indefinitely** rather than fail, since the `Event` this sole survivor is written to never got set. Diagnosed by identifying the exact hang point from a verbose run, then confirmed by reading both tests directly. Deselected (`--deselect`) to complete the full-suite run; not fixable from acp.py, and a mechanical rename would not fix them either (their mocking *strategy* assumes the wire round-trip, not just the wire-call's name) — flagged for Phase 4 as needing an actual rewrite, not a rename, and flagged again under Harness Improvement Opportunities since a hang (not a fast failure) is a materially worse failure mode for whoever next runs this suite without knowing to deselect them.

### Phase 2 (2026-09-12)

- **First attempt interrupted with a structural bug, discarded before commit**: an earlier implementation sub-agent, killed mid-work by an unrelated rate-limit error, had deleted the wrong side of each v2/v3 pair (kept `acp_page`/`ws_acp`, deleted `acp_v3_page`/`ws_acp_v3`) while also repointing 3 v3-suffixed API handlers' route decorators onto URL constants the untouched v2 handlers still used — producing 3 duplicate FastAPI route registrations, which Starlette/FastAPI would resolve by registration order (silently serving the old v2 handler, defeating the phase). Caught by the orchestrator reading the diff directly before committing anything; the partial state was stashed (not committed) and the phase re-run from scratch with a corrected brief.
- **Some v2/v3 function pairs were near-identical mirrors**: rather than a pure textual delete-then-paste, the retry sub-agent kept whichever body of each pair already matched the desired final behavior and deleted the other, verifying by construction that the end state matches the plan's intent (v3-descended behavior, unsuffixed name) — this is functionally equivalent to but not always the literal mechanical operation the plan's step-by-step prose describes.
- **D11's docstring-folding treatment extended beyond its literal function list**: D11 only names `acp_page`/`ws_acp`, but `_acp_listing`, `_acp_flat_listing`, `_acp_workspaces`, `_acp_status_for_held`, `api_acp_sessions`, `api_acp_workspaces`, and `api_acp_delete_sessions`'s v3-suffixed stub docstrings ("Mirrors X", "v3 parallel of X") become the identical dangling-self-reference problem D11 exists to fix, once X (the deleted v2 sibling) no longer exists and the function itself has become X. Folded in the deleted v2 docstrings' content by the same reasoning, adapting empirically-stale claims (a store-size/fallback-rate figure in the v2 `api_acp_sessions` docstring that no longer holds for the v3-descended implementation) rather than copying them verbatim.
- **Test triage (170 failures + 2 deselected, up from Phase 1's 127)**: the orchestrator independently traced two representative failure clusters via direct pytest traceback inspection rather than trusting the sub-agent's self-report. `TestAcpListingEndpoint`'s 26 failures all raise the identical assertion `"the browser is ACP's, and ACP is v2 kiro-cli only; got 'kiro-cli-v3'"` — a stale provider-name expectation, not a web.py defect (the code now correctly serves the v3-descended provider). `TestAcpDeleteEndpoint`'s failures raise `TypeError: ...<lambda>() got an unexpected keyword argument 'include_v3'` — the merged `api_acp_delete_sessions` correctly passes `include_v3=True` to `_acp_sessions_for_workspace`, which the test's mock lambda (written before this merge) doesn't accept. Both confirmed by direct traceback read, not assumed from the sub-agent's categorization. Full triage of the remaining ~144 failures is Phase 4's job; none investigated by the orchestrator trace to web.py logic being wrong.

### Phase 3 (2026-09-12)

- **Implementing sub-agent interrupted near completion, not mid-work**: killed by the same unrelated rate-limit error, but after finishing all 3 steps' edits — its last visible action was consulting its own pre-completion advisor check, not still editing. The orchestrator verified the recovered diff directly rather than assuming completeness from the near-done state: `grep` confirmed zero `ENGINE`/`engine` references, and `node tests/acp_page.test.mjs` was run once against the stashed (pristine) templates and once restored, isolating the phase's effect to exactly the 3 expected `test_engine_*` regressions with zero unintended ones (431→428 passed, the other 5 failures reproduced identically on both).
- **Browser tools unavailable this session**: the plan's exit criterion calling for a live hard-reload + console-error check could not be performed as specified (Claude-in-Chrome extension not connected). Substituted a live `curl` fetch of `/acp-v3` (HTTP 200, hardcoded constants present in the rendered HTML, no server-side template error) plus the node harness's DOM-driven script execution as the closest available proxy — recorded explicitly as a substitution, not silently treated as equivalent.

### Phase 4 (2026-09-12)

- **17 net test deletions, not the plan's estimated 6**: the plan's named 6 resolved to 4 outright deletions (isolation assertions now definitionally false: `CLIENT_TYPES` merged, single supervisor instance) plus 2 rescopes (the containing class, per step 0's discriminator rule, tests coexistence not isolation — rewritten rather than deleted), plus a third, previously-unnamed sibling test needing identical treatment — **correction (Step 5 review)**: that third test actually lives in `TestAcpDeleteEndpointForV3Ids`, a sibling class, not the same class as the plan's original 2 named rescopes (which live in `TestApiAcpDeleteSessionsEndpointMixedWorkspace`). The remaining 13 deletions are tests whose assertions became either false or unreachable once specific v2-only mechanisms were actually *deleted* (not merely renamed): the `.lock`-file preflight/holder-re-read machinery (superseded entirely by `session.json` status checks), the pre-close wire handshake (`CLOSE_METHOD = None`, no wire call ever made), and the now-unreachable "supervisor is None" branch under D1's eager construction. Each deletion left a comment citing the specific plan section or code fact that killed it. **Rename-count reconciliation (Step 5 review)**: the commit message's "~24 renamed test functions" and SC-7's original "~55-60" estimate were never reconciled. A rough independent check (`git show f0557f5 -- tests/test_web.py | grep` on changed `def test_` lines mentioning `v3`/`V3`) found 38 such changed lines — consistent with the true figure landing somewhere between the two cited estimates, but not precise enough to fully explain the gap. Likely contributors, not independently confirmed: SC-7's ~55-60 may have been counting every `v3`-mentioning test line from the pre-Phase-4 `grep "v3\|V3"` sweep the plan's own step 1 describes as "drift-prone," including URL-literal-only edits and tests inside renamed classes that don't themselves carry `v3` in their own name. Recorded as an open reconciliation gap rather than a resolved one — a future reader should not treat either figure as authoritative without re-deriving it.
- **Root cause of the two previously-hanging tests, found not assumed**: `close_session` has zero internal `await` expressions (confirmed by direct read) — the tests' original technique patched `_request` (the wire call) to create an artificial suspension point, but since `close_session` never calls `_request` at all, the patch never intercepted anything and the tests blocked forever on an `asyncio.Event` nothing would set. Fixed by patching `close_session` itself with a delegate that awaits an artificial event before delegating to the real method — a genuine suspension point that preserves the synchronous-prefix ordering `_handle_close`'s `closing` claim depends on. Five sibling tests in the same idle-sweep/close cluster shared the identical root cause and got the identical fix, found during the same investigation, not separately flagged by the plan.
- **Self-corrected mid-implementation**: the sub-agent initially wrote `@pytest.mark.skip` decorators for the two hang-prone tests, reasoning a fix would require modifying `acp.py` (out of its file scope) — then caught its own error on re-inspection: the fix is entirely test-side (patching `close_session` from within the test, not editing it). Zero skip decorators remain in the file for this reason.
- **One vacuous assertion found and fixed on final review**: `test_a_session_inside_the_ttl_is_left_alone` asserted only that no wire bytes were written — trivially true now regardless of outcome, since no wire call is ever made either way, so the assertion could no longer distinguish "left alone" from "silently terminated." Added a positive assertion (`sid in acp_mod._supervisor.sessions`) mirroring the fix already needed on a sibling test, making the test load-bearing again.
- **Two out-of-scope, unrelated staleness patterns flagged, not fixed**: `_sweep_once`'s and `_load_failure`'s docstrings in `acp.py` still describe mechanisms (`_kiro.dev/session/terminate`, `.lock`-file removal, a holder re-read) that no longer execute — outside this phase's `tests/test_web.py`-only file scope. A pre-existing, unrelated log-wording split ("ACP socket" vs. "ACP v3 socket") predates the v2/v3 merge; the shared test-helper regex was made tolerant of either spelling rather than picking a side to enforce, since fixing the actual log lines is outside this phase's scope.

### Phase 5 (2026-09-12)

- **Plan's literal-count estimate was stale**: the plan's 2026-09-11 research counted 10/3/6 `/acp-v3`-family literal occurrences; the actual current-file count was 9 total. Re-verified directly rather than trusting the cited figure, consistent with this plan's own repeated finding that line-number and count citations drift.
- **Removed one item beyond the 3 named tests**: a dead `engine: opts.engine ?? "v2"` context key in `loadPage()`, unreferenced by any remaining call site after the 3 tests were rewritten, and itself containing a literal `/acp-v3` string the exit criterion required gone.

### Phase 6 (2026-09-12)

- **README.md's two-engine section fully merged, not name-swapped**: the plan's step 1 called for "substantive rewriting, not a name-swap" — the implementing sub-agent went further and merged the separate `## Agent sessions, v3 protocol (/acp-v3)` section entirely into the main `## Agent sessions (/acp)` section, since every fact in the v3-specific section is now simply true of the sole engine and a separate "protocol notes" heading had nothing left to contrast against. Also corrected the main section's process-invocation line to name the actual spawn (`kiro-cli acp --agent-engine v3`), which the pre-existing text had never fully stated.
- **memory/MEMORY.md: two entries got new dated annotations, not silent rewrites of the underlying fact**: where the underlying *mechanism* changed (not just a name) — the `_publish_live` v2-session union's deletion, and the base/subclass split behind `new_session`'s `_meta.id` read — the sub-agent added a new `**Update (2026-09-12, ...)**` annotation, matching the file's own pre-existing convention for exactly this situation (D7 requires facts preserved, not merely names updated). **Correction (Step 5 review)**: the `_publish_live` entry's own *older* `Update (2026-08-19, ...)` annotation was also lightly edited in the same commit, renaming its `_supervisor_v3.sessions` reference to "the v3-specific supervisor's `.sessions`" — a genuine in-place identifier-reference update, not the "new-annotation-only" pattern this note originally claimed. The underlying documented fact in that older annotation was not altered, only the post-rename identifier it names — consistent with D7's actual rule (preserve facts, update names), just inconsistent with how this note first described the mechanism.
- **plans/tests/260701_POWERATLAS.md's known byte-corruption issue, confirmed pre-existing and left untouched**: the file's line 174 (the "2026-08-19 update (v3 spike)" note) contains literal control characters (0x07 BEL replacing the "a" in "api", 0x09 TAB replacing the "t" in "tests") — the same recurring cross-file corruption pattern `memory/MEMORY.md` already documents with unknown origin. A plain string-match edit against this line failed for exactly this reason; worked around with a byte-level, line-indexed insertion that left the corrupted line itself completely untouched. Not this plan's bug to fix.
- **Step 6 deferred and completed as a follow-up, not skipped**: the plan's `[P:4,5,6]` parallel annotation implied Phase 6 could run fully concurrently with Phase 4, but step 6 explicitly needs "Phase 4's actual disposition" of each renamed/deleted test class — unknowable while Phase 4 was still in flight. Deferred cleanly (the concurrent sub-agent was told to skip it explicitly, not left to guess), then completed once Phase 4's actual class-name dispositions (`TestApiAcpV3DeleteSessionsEndpoint`→`TestAcpDeleteEndpointForV3Ids`, `TestApiAcpDeleteSessionsV2RegressionForMixedWorkspace`→`TestApiAcpDeleteSessionsEndpointMixedWorkspace`) were known and independently confirmed present in the committed test file.

## Progress Tracker

| # | Phase/Task | Status | Notes |
|---|---|---|---|
| 1 | Backend engine consolidation (acp.py) | Complete | Class merged, all renames applied, D1-D5 applied; pytest: 1421 passed / 127 failed / 2 deselected, all failures traced to stale v2 names/semantics (see §9) |
| 2 | Backend routing & endpoints (web.py) | Complete | Retried once (first attempt had a duplicate-route bug, discarded uncommitted); routes/constants/D10/D11(extended)/D12(corrected)/`_REMOTE_ALLOWED_PATHS` all applied; pytest 1378 passed/170 failed/2 deselected, delta from Phase 1 baseline fully traced (see §9) |
| 3 | Frontend — ENGINE collapse & dashboard gate fix | Complete | ENGINE ternary collapsed, D6 gate fix applied; node test 428/436 (exactly 3 expected regressions); browser console check unavailable, substituted with HTTP fetch + DOM harness |
| 4 | Test suite — tests/test_web.py | Complete | 1533 passed/0 failed/0 skipped; 17 net deletions (plan estimated 6); both hang-prone tests root-caused and fixed, not skipped |
| 5 | Test suite — tests/acp_page.test.mjs | Complete | 431/436 passed (3 targeted tests fixed, 5 unrelated pre-existing failures untouched); zero acp-v3 literals remain |
| 6 | Documentation sweep | Complete | Steps 1-5,7 via parallel dispatch; step 6 deferred and completed as a follow-up once Phase 4's disposition was known |
| 7 | Restart coordination & live verification | Complete | Restarted with user's explicit authorization; SC-1/SC-4/SC-5 live-verified in browser (session open, prompt round-trip, close, resume-button gating both cases, graceful bad-sid degradation); full pytest (1535/0) + node (431/436, pre-existing) pass post-restart |

## Dependency Graph

```
Phase 1 (acp.py)
   │
   ├──> Phase 2 (web.py) ──┐
   └──> Phase 3 (templates) ┘  [P:2,3 — disjoint files, both depend only on Phase 1]
              │
              ├──> Phase 4 (test_web.py) ──┐
              ├──> Phase 5 (acp_page.test.mjs) ──┤  [P:4,5,6 — disjoint files]
              └──> Phase 6 (docs) ──────────┘
                         │
                         └──> Phase 7 (restart + live verification)
```

## Backwards Compatibility

| Item | Strategy | Safety effect |
|---|---|---|
| `/acp` URL | Unchanged string, new backing engine | Existing bookmarks/links to `/acp` continue to work, now opening v3 sessions |
| `/acp-v3` URL | Retired, no redirect | A bookmarked `/acp-v3` link 404s post-cutover — acceptable per this plan's scope (no redirect requested) |
| Old v2-shaped `?sid=` on `/acp` | Degrades gracefully (confirmed via code reading during exploration: `_handle_load`'s refusal path returns a clean "session not found"-style error, not a crash) | No special handling added; existing fail-open pattern already covers this |
| Historical v2 session files | Untouched (SC-4) | Full backward compatibility for the dashboard's history/browsing surface |
| v2 session deletion | Preserved (Phase 2 step 5) | Users can still delete old v2 sessions from the dashboard post-cutover |

## File Change Summary

### Created
- None (this plan modifies existing files only)

### Modified
- `src/power_atlas/acp.py` (Phase 1)
- `src/power_atlas/web.py` (Phase 2)
- `src/power_atlas/templates/acp.html` (Phase 3)
- `src/power_atlas/templates/partials/session_row.html` (Phase 3)
- `tests/test_web.py` (Phase 4)
- `tests/acp_page.test.mjs` (Phase 5)
- `README.md`, `AGENTS.md`, `docs/KNOWLEDGE.md`, `plans/ROADMAP.md`, `memory/MEMORY.md`, `plans/tests/260701_POWERATLAS.md` (Phase 6)

### Deleted
- No files deleted — this is a within-file consolidation, not a file removal

### Unchanged
- `src/power_atlas/data_kiro.py` (SC-4 — deliberately untouched)
- `src/power_atlas/data_kiro_v3.py` (permanent-distinction set)
- `src/power_atlas/static/style.css` (confirmed zero engine-conditional rules)
- `plans/CLOSED_INVESTIGATIONS.md` (D8)
- `src/power_atlas/status_classifier.py` (`classify_kiro_v3`, `_is_v3_format`, `_V3_SKIP_TYPES` — added during Step 4 review; `v3` here denotes on-disk session-file format, not engine identity, per SC-3's scope narrowing)
- `src/power_atlas/presence.py`, `src/power_atlas/data.py` (`"kiro-cli-v3"`-provider-aware code — same rationale)
- `tests/test_data_kiro_v3.py` (confirmed during exploration to have zero functional reference to `acp.py`/`web.py`'s v2/v3 split)

## Follow-up Work (Deferred)

1. **Mode-switcher UI, `_pending_permission`/orphan-lock/steering-palette follow-ups.** Tracked in `plans/260911_ACP_V3_FOLLOWUP_FEATURES.md`, deliberately out of scope here. Source: Scope boundaries.
2. **v3-side auto-mode for ACP permissions.** Already tracked in `plans/ROADMAP.md`'s "Session Control & Integration" section, untouched by this plan. Source: Q5.

## Review Log

### 2026-09-11 — Plan Review (via /qplan Step 4, cycle 1 of 1 — capped per user instruction)

Architect (gap-critic lens), Senior engineer, and Security auditor personas, dispatched in parallel against the finished plan. 14 findings (2 High, 6 Medium, 6 Low). All 14 auto-resolved — no unresolved High or Medium findings remain. Per the user's standing "1 review cycle max" instruction, no second cycle was run to re-verify the fixes; the fixes below were applied directly to this plan document.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | High | D1's lazy supervisor construction breaks 332 `acp_session`/`acp_store` test-fixture call sites that assume eager construction, undetectable by a v3-keyed grep | Fixed — D1 reopened, reversed to eager construction, matching v2's original pattern; eliminates the None-guard audit entirely |
| 2 | High | Several tests assert things that become *false* post-merge (e.g. `CLIENT_TYPES`/`CLIENT_TYPES_V3` isolation), not just stale-named — a mechanical rename doesn't fix a false assertion | Fixed — Phase 4 step 0 adds an explicit discriminator rule; 2 more tests added to the delete list (6 total, was 4); SC-7 corrected |
| 3 | High | Phase 2 never updates 3 `getattr(acp, "_supervisor_v3", None)` string literals or 2 held-set unions in `web.py` — outside Phase 1's `acp.py`-only scope | Fixed — new D12, new Phase 2 step 5 |
| 4 | Medium | `same_origin_guard`'s CSRF path check (`web.py:912`) references `_ACP_V3_PATH` inside a function body — a missed rename here is invisible to import checks or URL-literal greps, breaks every served request | Fixed — new D10, new Phase 2 step 4, added to exit criteria |
| 5 | Medium | `acp_page`'s security-rationale docstring is deleted with v2; the renamed survivor's "see `acp_page` for rationale" stub becomes a dangling self-reference, risking future removal of a real security check | Fixed — new D11, Phase 2 steps 1/2 fold the v2 docstring content into the survivors |
| 6 | Medium | D5's "collapse two sweep passes into one" doesn't say which non-overlapping piece (v2's subscriber-notification block vs. v3's `_pending_early_frames` sweep) survives | Fixed — D5 amended with an explicit merge spec and a required code-read confirmation step |
| 7 | Medium | `_REMOTE_ALLOWED_PATHS`'s reconciliation gave a bare dict literal, silently dropping ~17 lines of per-entry security rationale comments | Fixed — Phase 2 step 6 reworded to require preserving existing comments |
| 8 | Medium | SC-3's "repo-wide" rename wording doesn't account for `status_classifier.py`'s permanent v3-format-distinction names | Fixed — SC-3 narrowed to the live-engine files; File Change Summary's Unchanged table extended |
| 9 | Medium | `_create_job` omitted from the "34 inherited methods" list; `_build_kas_session_params`/`_crew_toolcallid` (v2-only) omitted from the Delete list, risking silent shadowing | Fixed — new D13; §1 and Phase 1 corrected to 35 methods and the two orphans added to the Delete list |
| 10 | Low | No exit criterion sweeps in-code comments (not just URLs/identifiers) for stale v2/v3 explanations | Fixed — new Phase 1 step 10 |
| 11 | Low | Phase 3's "renders correctly" pre-restart exit criterion overstates what's verifiable (JS-load only, not session functionality) | Fixed — reworded with an explicit scope note |
| 12 | Low | D4's "every real call site" claim is false for the pre-Phase-1 file (8 v2-only sites rely on the default); true only post-deletion | Fixed — D4 reworded |
| 13 | Low | D3's stated rationale's second clause (staleness check) doesn't actually hold under inspection; the real reason is structural | Fixed — D3 reworded to the structural argument |
| 14 | Low | Stale present-tense `-a`/trust-all-tools comments in `acp.py`/`web.py` will misdescribe v3's interactive-approval model as v2's blanket auto-approve | Fixed — folded into new Phase 1 step 10 |

### 2026-09-12 -- Implementation Review (after Phase 1, persona: Senior engineer, Reliability engineer)

Implementation health: Green. 1 review cycle (capped per user instruction — no cycle 2 re-review dispatched after auto-fix; fixes independently re-verified by the orchestrator directly instead).
7 distinct findings after merge (0 High, 2 Medium, 5 Low) — 9 raw findings across the two personas, 2 pairs deduplicated.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | Medium | Phase 1's "All 7 ripple-effect call sites repointed" exit criterion didn't scope out the 2 §1-named sites that live in `web.py` and are correctly deferred to Phase 2. | Fixed — exit criterion reworded to name the 5 acp.py-side sites vs. the 2 deferred web.py-side sites explicitly. |
| 2 | Medium | D12's web.py ripple-site enumeration undercounted (3 of 5 actual `getattr` sites) and omitted a direct `acp.serve_socket_v3(ws)` call entirely, currently inert but AttributeError-live post-restart. | Fixed — D12 and Phase 2 step 5 corrected to the actual 5 getattr sites + the missed direct call; comment sweep added. |
| 3 | Low | `_spawn`'s docstring read "Uses ACP_ARGS instead of ACP_ARGS" — self-contradictory after the rename collapsed both names onto one. | Fixed — reworded to describe current behavior, no comparison clause. |
| 4 | Low | `serve_socket`'s docstring (raised independently by both personas) still described itself as the "v3 counterpart... for the /ws/acp-v3 route" and cited a stale "Phase 3 must add" TODO for already-completed work. | Fixed — rewritten in present tense; confirmed `_acp_token_ok`/`_ws_origin_ok` already exist in web.py before rewording. |
| 5 | Low | `load_session`'s docstring cited "Differences from base," referencing the deleted v2 `_Supervisor` base class this method no longer subclasses. | Fixed — reworded to describe current behavior directly, corrected to note the caller (not `load_session` itself) resolves `cwd`. |
| 6 | Low | Two more stale `-a`/blanket-auto-approve comments described v2's retired mode as a live consideration, beyond the 3 already fixed during the original consolidation. | Fixed — 2 of 2 named instances addressed; a third candidate near `PROMPT_SILENCE_SECONDS` was assessed and left as-is with recorded rationale (archaeological framing explaining a *replaced* timeout, not a live-`-a` claim) — flagged for the user to overrule if read differently. |
| 7 | Low | Both personas independently found `qvalidate --expect-ticked 7` failed against the plan's actual 8 ticked Phase 1 boxes. | Fixed — orchestrator's own review-brief error (miscounted 8 as 7), not a plan/code defect; re-ran with `--expect-ticked 8`, PASS confirmed. |

The auto-fix pass (commit `a127813`) additionally found and fixed 4 more instances of the same staleness class via a bounded sweep beyond the 6 named findings above (a dangling self-reference in `_on_notification`'s INFO/DEBUG comment recovered from the pre-merge commit; a `_handle_subagent_subscribe` docstring whose "v2 vs. v3 call site" branches had collapsed onto identical merged names; two self-referential "port of / mirror of `_handle_subscribe`" comments inside `_handle_subscribe` itself, one with a line-number pointer that had drifted onto an unrelated function). All are comment/docstring-only edits — verified via full-diff read (every changed line is inside a `#` comment or `"""..."""` docstring), a re-run stale-name grep (zero hits), and a re-run import check (`from power_atlas.data import Session; from power_atlas import acp` succeeds).

Both personas independently corroborated a third time (having already been found by the interrupted sub-agent's own self-check, see Phase 1 Implementation Divergences) that `web.py:1703`'s `ws_acp_v3` calls the now-deleted `acp.serve_socket_v3` — out of Phase 1's file scope, addressed via the D12/Phase 2 step 5 plan correction above (finding #2), not a code change here.

### 2026-09-12 -- Implementation Review (after Phase 2, persona: Security auditor, Senior engineer)

Implementation health: Green. 1 review cycle (capped per user instruction — no cycle 2 re-review dispatched after auto-fix; fixes independently re-verified by the orchestrator directly instead).
6 distinct findings after merge (0 High, 2 Medium, 4 Low) — 7 raw findings across the two personas, 1 pair deduplicated. Both personas independently re-verified the duplicate-route-registration bug class from the discarded first implementation attempt is genuinely absent from the committed code (`grep` + AST scan + clean import, each reproduced independently).

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | Medium | `same_origin_guard`'s docstring plus two nearby comments (one inside a function this phase edited) describe v2's retired blanket `-a` auto-approve as v3's current behavior. | Fixed — reworded around v3's actual interactive `session/request_permission` approval and its self-approval implication, verified against `ACP_ARGS`'s actual value. |
| 2 | Medium | `_acp_listing`/`_acp_workspaces`'s docstrings kept dated v2-store session counts (208-session workspace; 14 of 65, measured 2026-08-01) now misattributed to the v3 store these functions read from post-rename. | Fixed — stale figures removed, docstrings reframed to describe the mechanism rather than a specific (now-wrong) measurement. |
| 3 | Low | `api_acp_sessions`/`api_acp_workspaces` still name a local variable `sv3` for what is now simply the sole supervisor. | Fixed — renamed to `supervisor`. |
| 4 | Low | Three `log.*` string literals still read "ACP v3 listing"/"ACP v3 flat listing" though only one listing mechanism exists now. | Fixed — "v3" qualifier dropped from all three. |
| 5 | Low | `data_kiro_v3.py:613`'s docstring references `web._acp_listing_v3`, renamed by this phase to `_acp_listing`; no phase's file scope covers `data_kiro_v3.py`, so the reference had no scheduled remediation. | Fixed — single-line docstring reference updated (the only change made to that otherwise-unchanged file). |
| 6 | Low | Both personas independently found the plan's top `> **Status**` banner still read "Phase 1 complete, Phases 2-7 pending" though the Progress Tracker below correctly showed Phases 1-3 Complete. | Fixed — banner updated to match the Progress Tracker. |

The auto-fix pass (commit `ffe2870`) additionally found and fixed 5 more instances of the same two staleness patterns via a bounded sweep beyond the 5 code-level named findings above — a repeated wrong-store-figure pattern in `_acp_cwd_exists`'s docstring and three pagination/picker-sizing comments, all independently confirmed via `git log -L` to predate `data_kiro_v3.py`'s creation (so "the real store" they cite could only ever have meant the v2 store) — reframed as explicitly historical/illustrative context rather than dropped outright, since they still justify why the relevant constants are sized as they are.

### 2026-09-12 -- Implementation Review (after Phase 3, persona: Senior engineer)

Implementation health: Green. 1 review cycle (capped per user instruction).
2 distinct findings (0 High, 1 Medium, 1 Low).

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | Medium | No CSS rule visually distinguishes a `disabled` resume button from an active one, and `.row-btn:hover` lacked the `:not(:disabled)` guard the codebase's own established pattern uses elsewhere — every one of the ~16,414 historical v2 rows would show active hover treatment right up until the click silently no-ops. | Fixed — `:not(:disabled)` added to the hover selector, a `.row-btn:disabled { opacity: 0.45; cursor: default; }` rule added, matching the exact visual language 3 of 4 existing disabled-control rules in the file already use. |
| 2 | Low | Plan's top `> **Status**` banner stale (same finding as Phase 2's #6, raised independently by this review too). | Fixed — already resolved by Phase 2's review-log entry above; same banner. |

This review's specific checks that a live browser console check would ordinarily cover (disabled-button visual state, click/keyboard-activation bypass paths, URL-constant drift between `acp.html` and `web.py`) were performed by direct code/CSS inspection since the browser extension was unavailable this session — the one substantive gap that inspection alone would likely have missed (finding #1, the missing disabled styling) was in fact found this way, confirming the substitute method was not merely a formality.

### 2026-09-12 -- Implementation Review (after Phase 4, persona: Senior engineer, Reliability engineer)

Implementation health: Green. 1 review cycle (capped per user instruction — no cycle 2 re-review dispatched after auto-fix; fixes independently re-verified by the orchestrator directly instead, including reading both newly-written tests in full).
6 distinct findings (0 High, 3 Medium, 3 Low) — 6 raw findings across the two personas, no overlap. The Senior engineer reviewer independently corroborated the "17 net deletions" figure via `1550 (Phase 2 baseline) − 17 = 1533`; the Reliability engineer independently ran a mutation trace on the rewritten synchronization technique (removing `_handle_close`'s `closing.add()` guard changes the outcome from `close_in_progress` to `agent_error`, confirming the fix is not a vacuous pass) and ran the full suite twice plus the 7 race-simulation tests 8 more times in isolation with zero flakiness.

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | Medium | `test_sub_agent_sessions_are_absent` was deleted with no explanatory comment, unlike every other deletion in the file, and was absent from the divergence narrative's "full itemized accounting." | Fixed — deletion comment added citing the verified fact chain (v3's listing has no sibling sub-agent entries to filter, unlike v2). |
| 2 | Medium | Deleting `test_a_missing_store_entry_falls_back_to_the_neutral_cwd` left `_stored_session_cwd_v3`'s actual missing-entry behavior (silently returns `""`, no fallback) completely untested on the sole surviving production path. | Fixed — new regression test `test_stored_session_cwd_v3_returns_empty_for_a_missing_entry` added, pinning the actual current behavior. |
| 3 | Medium | Deleting the wire-handshake tests also deleted the only test of `_handle_close`'s own `except Exception`/`finally: closing.discard(...)` cleanup path, with no v3-appropriate replacement. | Fixed — new regression test `test_a_failing_close_still_frees_the_closing_claim` added; forces `close_session` to raise and confirms the `closing` claim is freed and a subsequent close succeeds normally. |
| 4 | Low | Two `TestAcpLoadFailureAttribution` tests kept dead `_lock_holder`-mock scaffolding and now-vacuous sub-assertions, an inconsistent application of the standard already applied elsewhere in the same commit. | Fixed — dead mock scaffolding and the `_one_shot` helper removed; each test's still-load-bearing core assertion kept unchanged. |
| 5 | Low | The Implementation Divergences narrative never reconciled the commit's "~24 renamed test functions" against SC-7's original "~55-60" estimate, and misattributed which class contains a third rescoped test. | Fixed directly by the orchestrator (plan-text correction, not code) — misattribution corrected; reconciliation recorded as an open gap with a rough independent count (38 changed `v3`-mentioning def-lines) rather than a fabricated precise explanation. |
| 6 | Low | Docstrings for the two hang-fix tests (and one sibling) called their scenario a "race," but `closing.add()` always precedes the only relevant `await` in both call sites, making the ordering deterministic under asyncio's cooperative scheduling, not a genuine race. | Fixed — reworded to describe enforced ordering/sequencing; left every other, genuinely-different "race" occurrence in the file untouched. |

Auto-fix commit `39d5a1c` (tests/test_web.py). Orchestrator independently reproduced: 1535 passed / 0 failed / 0 skipped (was 1533 before the 2 new tests), zero duplicate defs (fresh AST scan), and read both new tests in full to confirm they assert real, current behavior rather than trivially-true statements.

### 2026-09-12 -- Implementation Review (after Phase 5, persona: Senior engineer)

Implementation health: Green. 1 review cycle (capped per user instruction).
1 finding (0 High, 0 Medium, 1 Low).

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | Low | The rewritten `/ws/acp` WebSocket-URL assertion used `.includes()` with no boundary check, so it would also pass against a future regression reintroducing `/ws/acp-v3` — inconsistent with its two sibling tests' precise `.startsWith()` checks. | Fixed — tightened to a boundary-aware regex (`/\/ws\/acp(\?|$)/`); manually verified it accepts the real URL and rejects a hypothetical `-v3` regression. |

Auto-fix commit `9803aba` (tests/acp_page.test.mjs). Orchestrator independently confirmed the commit is clean and scoped to exactly this one assertion.

### 2026-09-12 -- Implementation Review (after Phase 6, persona: Senior engineer)

Implementation health: Green. 1 review cycle (capped per user instruction).
6 distinct findings (0 High, 3 Medium, 3 Low).

| # | Severity | Finding (one line) | Resolution (one line) |
|---|---|---|---|
| 1 | Medium | README's rewritten "Agent sessions" section left two v2-shaped session-store-layout claims standing as fact for the sole v3-descended engine (per-session vs. shared `.lock`; what session deletion actually erases). | Fixed — reworded against `data_kiro_v3.py`'s actual `delete_session`/store-layout code, verified by direct read. |
| 2 | Medium | The same section still described a wire-level session-terminate mechanism ("its lock removed", implying wire confirmation) that no longer exists — `close_session` makes no wire call at all (`CLOSE_METHOD = None`). | Fixed — reworded around "released locally... without waiting for wire-level confirmation," matching this plan's own accepted-limitation framing. |
| 3 | Medium | The markdown-rendering paragraph's security rationale ("an agent running with every tool pre-approved") described v2's retired blanket `-a` auto-approve, contradicting the same section's own later paragraph about interactive per-request permission. | Fixed — reframed around defense-in-depth against untrusted transcript content, consistent with the rest of the section. |
| 4 | Low | The Phase 6 divergence note claimed two `memory/MEMORY.md` entries got "new annotations, not in-place rewrites," but the `_publish_live` entry's own older annotation was also lightly edited in the same commit. | Fixed directly by the orchestrator (plan-text correction) — note reworded to describe the actual mechanism (an in-place identifier-reference update, facts preserved), matching what the commit actually did. |
| 5 | Low | README's merge placed two unrelated concepts (Steer/Queue send-mode; spec/quick-spec/bug-fix/plan agent modes) both using the word "mode" close together in the same section — a readability regression for a cold reader, not a factual error. | User: accepted — fix (user turn: "1. fix. 2. fix."). Both mode concepts reworded to "send mode"/"agent mode" throughout, with the previously headerless agent-mode paragraph given a bold lead sentence (code: `6f1e93a`). |
| 6 | Low | `templates/acp.html:1985`'s comment still names the pre-rename `_handle_permission_response_v3`. | User: accepted — fix (user turn: "1. fix. 2. fix."). Fixed at both occurrences (line ~1985 and a second one at ~7039 found during the fix); a third, adjacent stale reference (`_handle_prompt_v3` at ~7040, same comment block) was found but left out of this fix's approved scope, then fixed separately by the orchestrator immediately after (code: `6f1e93a`, `840b0b3`) — `grep -c "_v3" src/power_atlas/templates/acp.html` now returns 0. |

Auto-fix commit `9eea759` (README.md). Orchestrator independently confirmed the commit is clean, scoped to the "Agent sessions" section, and spot-verified the `.index/.lock` claim directly against `data_kiro_v3.py`.

## Harness Improvement Opportunities
<Reserved -- appended during /qexplore, /qplan and /qdev when harness friction is felt.>

- `pytest-timeout` is not installed in `.venv-PowerAtlas`, so the project's own suggested invocation (`pytest tests/test_web.py -x --timeout=300`) fails immediately with `unrecognized arguments: --timeout=300` — cost: two full-suite hangs (each requiring a background-task kill, since without the plugin a stuck `asyncio.Event.wait()` has no bound) before finding this out, plus the time spent isolating which of two structurally-identical tests was hanging, since nothing bounded or reported it — suggested change: add `pytest-timeout` to the project's dev dependencies (or note in `AGENTS.md`'s Doc & Test Guidelines that it is not installed and the flag will silently not apply), so a future stuck test fails loudly with a stack dump at 300s instead of hanging the whole run indefinitely.
- Two tests (`test_a_second_close_cannot_overtake_the_first`, `test_a_prompt_arriving_during_a_close_is_refused`) hang indefinitely rather than fail once `close_session`'s v2-only wire round-trip is gone, because their concurrency setup is an `asyncio.Event` synchronized to a `_request`/`_write` call inside the close path — cost: not discoverable from a failure list or a `--tb=line` summary at all, only from watching a run get stuck and manually bisecting; a mechanical rename cannot fix this class of test (the *mocking strategy*, not just an identifier, assumes the wire call happens) — suggested change: Phase 4 should treat "does this test's concurrency/timing setup depend on a call that only v2's code path made" as its own discriminator category, distinct from the existing name-based and assertion-based ones, and should specifically audit every `asyncio.Event`-based test in `tests/test_web.py` for this before Phase 4 exit, not just the two found here.
- Two parallel-dispatched sub-agents in the same session were killed mid-work by an account-level rate-limit (`You've hit your session limit`), not a per-agent failure — cost: one of the two (Phase 2's first attempt) left a genuine structural bug (duplicate FastAPI route registrations) in its uncommitted working-tree state, which the orchestrator had to independently diagnose and discard before a clean retry; the other (Phase 3) was interrupted after finishing its edits but before its own verification pass, and turned out to be correct on inspection — suggested change: `/qdev`'s orphaned-working-tree recovery guidance already covers this well for the crash-recovery case, but doesn't yet call out that a rate-limit-killed sub-agent's partial edits deserve the SAME scrutiny as a self-directed "done" return, not less — a near-finished-looking diff can still be silently wrong in a way that only a careful read (not just "it looks mostly done") catches.
- A dispatched implementation/auto-fix sub-agent's own git commit can carry a `Claude-Session:` attribution trailer the project's standing governance absolutely bans, if — and only if — the orchestrator's brief for that specific dispatch happens to omit the explicit "no AI/agent attribution" instruction (every other sub-agent brief in this session included it; one auto-fix brief, for a small single-file CSS fix, did not) — cost: a policy-violating commit landed in local history and required a `git commit-tree`/`git update-ref` rewrite to correct (safe here only because the commit was unpushed and the branch tip; the same gap on a later, non-tip commit or a pushed one would have been materially harder to fix) — suggested change: the orchestrator should treat the no-attribution instruction as a mandatory, non-omittable line in *every* sub-agent brief that grants a commit-authority, not something to remember per-brief; consider adding a Step-4/Step-6 brief-template checklist item for this in `shared/skills/qdev/SKILL.md` so it can't be silently dropped from a smaller/faster-typed brief.
- A recurring pattern across this plan's Step-5 reviews: a phase's file-scope boundary routinely sits one file away from a stale reference that boundary can't reach (Phase 1 review found `web.py:1703`'s `serve_socket_v3` call, outside `acp.py`-only scope; Phase 6 review found `templates/acp.html:1985`'s stale comment, outside every phase's scope) — cost: each instance required a full review cycle to surface rather than being caught by the implementing phase itself, and the `acp.html` one has no scheduled remediation at all since no phase's file scope covers template comments; suggested change: when a rename/consolidation plan's phases partition a codebase by file, add one lightweight cross-phase sweep step (a repo-wide grep for the retired names, run once after all rename phases land, scoped to comments/docstrings only) rather than relying on each phase's reviewer to independently notice a neighbor file's drift.
- Phase 1's own Implementation Divergences deferred ~91 stale log-message/thread-name literals to "Phase 4/Phase 6" by name — but neither phase's actual `**File scope**` line included `acp.py`, so the deferral pointed at two phases incapable of acting on it. This survived the entire plan's execution (Phases 1-6, plus 3 separate per-group review cycles across 8 review sub-agents) undetected, only surfacing when the orchestrator ran the plan's own `§7 Verification` grep sweep at the very end, after every phase and every review had already completed — cost: a full extra cleanup dispatch (2 sub-agent round-trips) after the plan was believed done; suggested change: when a plan's own text defers work "to Phase N," `/qplan`'s Step 4 review (or a lightweight automated check) should verify Phase N's stated `**File scope**` actually covers what's being deferred to it, rather than trusting the deferral's phase reference at face value — this is exactly the kind of claim `/qreview`'s "treat the plan's empirical claims as hypotheses to test" rule already tells reviewers to verify, but no review pass in this plan's history happened to pick this specific one out.
- `qvalidate`'s `doc-updates` check can't distinguish "this Documentation Updates row states a requirement that was never addressed" from "this row states a conditional check that was actually run and resolved to no-change-needed" — it purely checks whether a commit touched the named file, so `docs/KNOWLEDGE.md`'s row (worded from the start as "update only if X" and confirmed by Phase 6 to resolve to no-X) FAILs at `/qclose`-readiness time even though the row's own requirement was fully satisfied — cost: a genuine FAIL had to be triaged and explicitly overridden by the user at the very end of the plan, right when the mechanical gate should have been quietest; suggested change: `doc-updates` should accept a row-level marker (e.g. a row ending "(verified, not edited)") as satisfying the check without a commit touching that file, so a conditional row that resolves to "confirmed, no edit needed" doesn't read identically to a row nobody ever looked at.

**Resolution (at `/qclose`)**: the `doc-updates` FAIL on `docs/KNOWLEDGE.md` was presented to the user per `## Failure semantics`' "halt, present verbatim, user decides" rule. The user first accepted it as a noted false positive, then on reflection at `/qclose` time asked for a real fix instead of a carried-forward override (turn: "Fix now"). Resolved genuinely, not overridden: added a dated "**Re-verified 2026-09-12**" annotation to the `_get_tool_diffs_v3` mechanism note (matching the file's own established re-verification convention), independently confirmed accurate via direct code read (`code: 7d7e00a`). `qvalidate --check-commits` now reports a clean PASS on all 8 checks, 0 warn, 0 fail.
