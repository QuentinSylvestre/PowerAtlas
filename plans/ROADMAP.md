# PowerAtlas — Roadmap

> Non-executed ideas and future features, organized by theme. Shipped items are removed rather than
> struck through — `git log -- plans/ROADMAP.md` carries their history.
>
> **Paths already investigated and rejected live in `plans/CLOSED_INVESTIGATIONS.md`**, with the
> measurements that decided them and the condition that would reopen each one. Read it before
> proposing `kiro-cli serve`, `_kiro.dev/session/list`, or kiro-cli remote control again.

---

## Automation & Workflows

> Every item here needs one capability PowerAtlas does not have: sending a prompt to an agent
> without a terminal. That is why `260725_KIRO_CLI_ACP_CLIENT_PROTOTYPE` was built, and the findings
> are recorded per item below, dated 2026-07-26 and measured on kiro-cli 2.14.2. Read them with the
> prototype's boundary in mind: it proved a **chat surface driven by a human watching it**, which is
> what its `-a` posture assumed. Three of the six — fire-and-forget dispatch, scheduled tasks and
> chained launches — are *unattended*, which is the absence of that human, so for those the prototype
> prices the work without deciding it. Where it established nothing, that is said rather than left
> blank.

- **Dispatch no-interactive tasks** — launch kiro-cli with `--no-interactive` and a prompt, fire-and-forget from the UI
  - **The mechanism this item names persists nothing, which changes what the feature would deliver.** Four variants were run: `--no-interactive` piped, `--no-interactive` in a real console window, and an interactive session with piped stdio quit gracefully via `/quit` (exit 0) all wrote **zero files**; only a fully interactive TTY session persisted. It is the console, not the flag. So a fire-and-forget `--no-interactive` dispatch leaves no session file, no transcript and no lock — nothing for the dashboard that motivates the item to show, and nothing for `status_classifier.py` to classify. Recorded in full under *Accepted limitations* in `plans/CLOSED_INVESTIGATIONS.md`.
  - **ACP is the dispatch path that does leave a trail**, and the trade is the mirror image. `session/new` writes `<sid>.json`, `.jsonl` and `.lock` at creation before any prompt, and the turn lands in the `.jsonl` the existing pipeline already reads — but every dispatch is then a permanent artifact in a store already holding ~13,300, and the prototype had to enumerate the store by `cwd` before and after each phase to keep its own count of what it had created honest.
  - *Unanswered — the thing that decides the item*: whether dispatch is safe with nobody watching. See the permission experiment under `## Session Control & Integration`.
- **Open session with specific prompt/skill** — generic prompt input, local/user skill execution with params
  - **The prompt half is built and measured.** Prompting over ACP works end to end with incremental chunks, tool-call rendering and an authoritative `stopReason`. Budget for latency rather than assuming it: a trivial 20-line answer took ~24 s wall clock and a tool-using turn runs to minutes, which is why the prototype needed a 600 s prompt ceiling distinct from its 90 s ceiling for ordinary requests.
  - **Skills work, and they work in full.** A skill invoked over ACP loads the whole `SKILL.md` body, not just the frontmatter description — verified with tools forbidden, reproducing a script path, four flag names and a five-row taxonomy that exist only in the body. The advertised catalogue matches `~\.kiro\skills\` exactly: 25 each, set difference empty in both directions.
  - **`with params` is the open half, and it is this item's own wording.** `/qsummarize-chat 1` honoured the body's output format but emitted 4 pairs rather than 1, and nothing distinguishes "the argument never reached `$ARGUMENTS`" from "the model ignored it". Any surface that passes parameters into a skill needs this settled first, and settling it costs one prompt.
- **Template prompts** — save reusable prompt templates per workspace (e.g., "/qdev {latest plan file}" as one-click)
  - **Nothing established about templating itself.** Storage, per-workspace scoping and the UI were all out of scope; the prototype hardcoded agent, model and effort and added no config keys, deliberately.
  - **The substitution question is inherited, not new**: this item's own example is a slash command carrying an argument, so it is blocked behind the same inconclusive `$ARGUMENTS` result as the item above — the template mechanism is worthless if the parameter never arrives.
- **Scheduled tasks** — cron-like recurring kiro-cli launches (e.g., "/qdream every Sunday 10am in agent-playbook")
  - **This is the item the prototype was scoped never to test**, and the gap is structural rather than incidental: `-a` was accepted for "a prototype driven by a human watching it", and a schedule is precisely the absence of that human.
  - **Three measured facts price it even so.** An unprompted `shell` command ran under `-a` and wrote into the real session store, overriding that session's own recorded `allowed_write_paths: []`, on a trigger nobody identified — so plan on an ungated command running on any turn, not the first. A held-open session costs ~254 MB and 5 processes, and the only lever that frees them is `_kiro.dev/session/terminate`, a kiro-private extension; there is no idle sweeper anywhere. And creation is ~5.4 s cold / ~2.5 s warm, so a scheduler either pays that per run or holds processes between runs and pays the memory instead.
    - *All three re-measured 2026-07-31 on 2.16.0, and all three moved in the scheduler's favour.* The "trigger nobody identified" is identified: it is `~\.kiro\steering\session-tab-title.md`, PowerAtlas's own global steering, so the ungated command is **known and predictable** rather than arbitrary — it fires once on the first turn of every new CLI session and touches only that session's own store record. A held-open session now costs **~3 procs / ~150 MB** (not 5 / 254), `_kiro.dev/session/terminate` is confirmed working and confirmed to actually reclaim, and creation is **1.47 s cold / ~0.5 s warm**. The dilemma in the last sentence therefore softens a lot: sub-second warm creation makes *not* holding processes between runs the cheap option.
  - *Blocked on*: the permission experiment under `## Session Control & Integration`. Of the six items here this is the one that should not be built on the prototype's evidence alone.
- **Chained launches** — "when session X exits, auto-launch session Y in folder Z" (pipeline mode)
  - **The trigger this item needs exists, and polling cannot see it.** `stopReason` on the completing `session/prompt` is an authoritative turn-end signal — measured at ~0.11 s after a cancel notification — where a transcript tail can only infer one from the shape of the last record.
  - **But it is a trigger only for sessions PowerAtlas itself drives.** A session live in a terminal is unreachable, because `kiro-cli chat`'s own ACP grandchild holds the lock, so "when session X exits" holds for X's that PowerAtlas launched or that have already exited — not for the terminal work the dashboard mostly watches.
  - **Stage health is not readable from the process.** `_kiro.dev/commands/execute` killed the agent with exit code 0 and no stderr, and the prototype's agent wrote 0 stderr bytes across whole sessions; separately, `-32603 "Internal error"` is this build's generic failure and was returned once for a `session/prompt` that then succeeded on immediate retry. A pipeline must judge liveness from the JSON-RPC channel and must not read a single error frame as a failed stage.
- **Plan-file shortcuts** — detect plan files in `plans/` and offer one-click "/qdev on this plan" buttons
  - **The invocation path is proven and it is cheap.** Slash commands are ordinary prompt text intercepted agent-side — `/tools` returned a canned answer in **1 ms**, impossible for a model round trip — so no proprietary RPC is involved and a standards-only client sees every skill.
  - **The parameter is the whole feature, and it is the unresolved part**: "/qdev on this plan" is exactly an argument-carrying invocation, blocked on the same `$ARGUMENTS` question as the two items above.
  - **Like *Open session with specific prompt/skill*, this one keeps its human.** A one-click button is a user gesture with the user present, so it sits inside the posture the prototype actually validated and is blocked on the parameter question rather than on the permission experiment. That makes it, with its two siblings above, the part of this section that survives a bad result from that experiment.

## Workspace Intelligence

- **Session status extensions** — the base shipped across three plans (live dots and status filter, semantic Active/Needs-input/Idle/Errored, toast notifications, blocked-on-approval vs asked-a-question). Still open: "stale /qdev never completed" heuristics, sound/chime notifications, and detecting fresh (non-resumed) in-terminal sessions
- **Plan progress overlay** — parse plan files to show phase completion status on workspace cards (e.g., "Phase 3/5")
- **Sub-agent pipeline visibility** — surface `/qreview` and `/qdev` fan-outs, which are currently invisible. kiro-cli's `subagent` tool is a DAG: *"Spawn and coordinate multiple AI agents in a pipeline… Each stage runs as a persistent session. Stages with no `depends_on` start immediately in parallel"*, with `loop_to` / `trigger` / `max_iterations` for review cycles. The TUI already exposes this behind ctrl+g; PowerAtlas shows nothing. Two independent data sources, worth treating as separate increments:
  - **Completed pipelines, from disk — available today, no integration.** Sub-agent stages persist as ordinary sessions carrying `parent_session_id`. There are **4,532** of them in the local store (vs 1,120 user-facing sessions), deliberately filtered out by the four `parent_session_id` guards in `data_kiro.py` — correct for the session list, but it means the raw material for a pipeline/history view already exists on disk. Grouping stages under their parent needs no ACP, no hooks and no new dependency. Titles are self-describing (e.g. *"You are a Maintainability reviewer reviewing a governance/skill-file c…"*).
  - **Live pipeline state, over ACP — the stated blocker is gone.** `_kiro.dev/subagent/list_update` pushes `{subagents, pendingStages}` as a fan-out runs — the DAG's current state, free once an ACP client exists. Observed firing 23 times across captured traffic, though always with empty arrays, since no probe session ever dispatched a fan-out. That client now exists: `260725_KIRO_CLI_ACP_CLIENT_PROTOTYPE` shipped a working kiro-cli ACP client on 2026-07-26, so this needs only a notification handler and a rendering surface on top of it. Two caveats keep it from being free. The prototype is **throwaway**, so this rides the rebuild rather than the original build decision; and it only ever sees fan-outs dispatched from sessions PowerAtlas itself drives — a fan-out running in the user's terminal is not reachable this way, which is what makes the disk-based increment above the one that covers real work.
  - *Confirmed*: `subagent` is advertised over ACP as a built-in tool and `chat.enableSubagent = true` globally. Since `kiro-cli chat` is a wrapper around `kiro-cli acp` (process tree measured 2026-07-25: `kiro-cli acp` ← `bun.exe` ← `kiro-cli chat -a`), every sub-agent ever dispatched from a terminal already ran through ACP — there is no TUI-only path. **Not directly observed**: a fan-out running inside an ACP probe session.
- **kiro-cli usage stats** — dashboard with session counts, durations, tool usage patterns over time

## Platform

- **[HIGH] Secret-aware handling for custom-launcher env vars** — launcher environment variables are stored, served and rendered in cleartext everywhere, and at least one real launcher holds production credentials. *Scoped 2026-07-25.*
  - *The concrete case*: `%LOCALAPPDATA%\power-atlas\config.toml` (lines 57-61 as of 2026-07-25) carries `AUTH_TOKEN_PRODUCTION`, `AUTH_TOKEN_STAGING` and `MQTT_CERT_PATH` under a `[custom_launchers.env]` block. Three other launchers have empty env blocks. The file is outside the repo, so this is not a git-leak risk — it is a local-exposure one.
  - **The exposure is wider than the config file, and this is the part that makes it high priority.** `/api/launchers` (`web.py`) returns every launcher **including its env dict**, and the `_launchers` bootstrap in `index.html` (`var _launchers={{launchers|tojson}}`) injects the same list into the page. `same_origin_guard` (`web.py`) gates **POST only** — its first line is `if request.method == "POST":` — so both `GET /` and `GET /api/launchers` are unauthenticated. **Any local process can read the production tokens with a single HTTP GET**, with no ACP client, no agent, and no user interaction. *Verified by code reading 2026-07-25; not executed against a live server.*
  - *Nothing masks them on the way through*: `launcher_modal.html:10` is a plain `<textarea>` ("Env (KEY=VAL per line)"), `editLauncher` (`index.html`) renders stored values straight back into it, and `launcher_create` (`web.py`) stores `body.get("env", {})` with no validation. Values reach the child at `launcher.py:403` (`proc_env = {**os.environ, **env}`), which is the intended behaviour and not the problem.
  - *Why now*: `save_config` (`config.py:271-290`) rewrites the whole file on any settings change, so the values are re-serialised constantly; and the ACP prototype (`260725_KIRO_CLI_ACP_CLIENT_PROTOTYPE`) introduces an agent running with `-a` that has filesystem access — a second, weaker path to the same data. That plan added a WebSocket token partly for this reason, but the token is itself served over the same unauthenticated `GET`, so it does not close this.
  - *Shapes worth considering, none chosen*: (a) reference-by-name indirection — store `AUTH_TOKEN_PRODUCTION = "@keyring:poweratlas/prod"` and resolve at launch from Windows Credential Manager / libsecret; (b) keep values local but stop serving them — omit `env` from `/api/launchers` and the `|tojson` payload, fetching it only when the edit modal opens; (c) minimum viable — mask in the UI and redact from any export or log. Note (b) alone fixes the GET exposure and is small; (a) is the durable answer.
  - *Adjacent, same root cause*: the `GET`-side gap is general. `same_origin_guard` protecting only POST means every read endpoint is open to any local process. Worth deciding whether that is acceptable independently of this item.

- **Launched sessions inherit PowerAtlas's own environment — on one of three launch paths only** — a session's environment silently depends on how the tray app was last started, and the three spawn paths disagree about whether it does. *Scoped 2026-07-28.*
  - *The symptom that surfaced it*: Claude Code launched from the PowerAtlas UI opens with `Transcript saving is off — inherited CLAUDE_CODE_CHILD_SESSION marker`; the same binary typed by hand into a terminal PowerAtlas opened in the same folder does not. `CLAUDE_CODE_CHILD_SESSION` is set by Claude Code on processes it spawns so a nested `claude` suppresses transcript saving.
  - **Verified — the discriminator is the `wt` command shape, measured 2026-07-28** on Windows Terminal 1.24.11911.0. Probe: exact-argv `subprocess.Popen` (the same call shape `launcher.py` uses) from a caller carrying `PA_PROBE_MARKER=set-by-caller` and `CLAUDE_CODE_CHILD_SESSION=1`. `wt --title … -p … -d … -- pwsh …` → the spawned shell sees **neither**. `wt new-tab --title … -d … -- pwsh …` → it sees **both**. `PA_PROBE_MARKER` existed nowhere but in the caller, so what arrives is the *caller's* environment block, not the Terminal's. Bare `wt` resolves through the app-execution-alias activation path, so the window is created by the app-model infrastructure with a fresh environment from the user registry; `new-tab` is routed into an existing Terminal instance, which forwards the invoking `wt.exe`'s environment to that tab's shell.
  - **The asymmetry is PowerAtlas's own, and nothing in the code names it.** `launch_session` (`launcher.py:172`) passes the configured template straight to `detect_terminal`; `launch_terminal` (`launcher.py:556`) and `launch_custom` (`launcher.py:419`) **discard** it whenever it contains a `{` placeholder and fall back to auto-detected bare `wt`. The Windows default template (`config.py:35`, live at `config.toml:19`) begins `wt new-tab`. So the provider path forwards the environment and the two others drop it, decided implicitly by which command shape each code path happens to build.
  - *The marker is the visible instance, not the extent*: `launch_session` calls `Popen` with no `env=`, so **everything** PowerAtlas holds reaches the session. The tray app is long-lived, so one start from inside an agent session poisons every launch until restart — currently reproducible because PowerAtlas is developed with Claude Code and gets restarted from tool calls (live instance at the time of scoping: PID 33732, `python -m power_atlas --foreground`, parent already exited — the shape of a launch from a tool call that then returned).
  - *Same disease as the interpreter split, on a different resource*: `interpreter.py`'s module docstring already diagnoses it — "four entry points independently choose an interpreter … and each inherits whichever environment invoked it. Left alone they drift apart." That module made the interpreter a property of the source tree instead of the invoking context. The environment has the identical shape and no equivalent fix.
  - *Shapes worth considering, none chosen*: (a) deny-list — strip `CLAUDECODE`, the `CLAUDE_CODE_*` session markers, the kiro equivalents and `POWER_ATLAS_VENV_REEXEC` before spawning; small, but a list that needs maintaining as agents add variables; (b) construct the child environment explicitly rather than inheriting it, which is the durable answer and also the one that makes the three launch paths agree; (c) accept it and document the workaround — restart PowerAtlas from a shell that is not inside an agent session, or from its autostart shortcut. Note that `launch_custom` deliberately merges `os.environ` at `launcher.py:403` for user-supplied launcher env vars, so (b) must keep that path's intended inheritance while fixing its default.
  - *Unverified — the one gap*: PowerAtlas's own environment block was never read directly. A PEB reader written for the purpose returned 0 variables, so its "no marker present" results are meaningless and are not relied on here; the conclusion that the tray process carries the marker is deduction from the observed banner plus the proven forwarding. Cheapest confirmation, if this is ever picked up: print `$env:CLAUDE_CODE_CHILD_SESSION` in a session launched from the UI. Also unmeasured: whether Terminal's forwarding behaviour is version-stable, and what the non-`wt` terminals in `_LINUX_TERMINALS` do.

- **Unify the `/search` and `partials_workspaces` render pipelines** — `search` and `partials_workspaces` (`web.py`) implement substantially the same filter-and-render pipeline in parallel. Unify them behind one shared helper — but diff the two functions first: they are not faithful copies, and a helper generalised from either one alone will silently change the other route's behaviour. *Scoped 2026-07-25.*
  - **The duplication has already shipped a defect, which is what moves this off the tidiness pile.** `51fc500` dropped the `latest_updated` parameter from `_workspace_status` and updated the `partials_workspaces` copy of the call; the `search` copy kept passing it, so `/search` with a status filter raised a 500 until the `260725_KIRO_CLI_ACP_CLIENT_PROTOTYPE` work fixed it. Nothing structural stops the next signature change repeating that.
  - *Static analysis will not catch the repeat*: `pyproject.toml` declares `ruff>=0.3` as a dev extra, there is no `[tool.ruff]` block and no `ruff.toml` anywhere in the repo, and ruff's default rule set does not check call arity. Tests are the only guard.
  - *Shape*: one shared filter-and-render helper both routes call, parameterised on the search prefilter and on whichever behavioural differences survive the keep-drop-unify decision the diff forces. Contained to `web.py`. Sequence it after the ACP routes land — that plan inserts into the same file.

- **kiro-cli v3 session support** — scan `~/.kiro/sessions/<workspace-hash>/sess_*/` alongside v2 `cli/` directory; handle new message format, subagent detection via `sub-executions/` dir
  - *Sized 2026-07-24*: **23 v3 sessions currently invisible** across 3 workspace-hash dirs. A read path already exists (the v3 branch of `_resolve_jsonl_path_uncached` in `status_classifier.py`); what is missing is discovery — `data_kiro.py` globs only `sessions\cli\*.json`. `kiro-cli chat --list-sessions -f json` confirms them as `"source":"v3"`. A further **11 "classic" sqlite conversations** have no file on disk at all. See `## Session Control & Integration` → [P2b].
  - **Priority downgraded 2026-07-24 — the v3 store is dormant, not live.** All 23 sessions fall between **2026-06-18 and 2026-07-16**, nothing in the 8 days since, and the window opens exactly at the `260618_KIRO_CLI_V3_PLAYBOOK_MIGRATION` work. Current kiro-cli 2.14.x writes `kiro-cli chat` sessions to the **v2** `sessions\cli\` layout even under `--v3`. So this item surfaces 23 historical sessions from a closed four-week experiment, not ongoing work — worth doing for completeness, not for coverage of live activity. Re-raise if the store starts growing again.

## Session Control & Integration

> Candidate paths to move PowerAtlas from read-only observation toward control, spiked over two rounds on 2026-07-24. Raw findings, wire logs, captured payloads and benchmark scripts are archived outside the repo at `Downloads\260724_PowerAtlas-spikes\` (`REPORT.md` for round 1, `REPORT-ROUND2.md` for round 2). Claims are marked *verified* (observed on this machine, with logs) or *unverified* (inferred from shipped code or docs, not executed). Pinned to Claude Code `2.1.219` and kiro-cli `2.14.0`/`2.14.1` — the kiro KAS bundle self-updated mid-spike, so code offsets rot.
>
> **The two correctness defects the spikes found (transcript-tail size, provider matching) and both row-level identity gaps shipped on 2026-07-24 as local fixes, with no integration work at all** (`a973715`, `ee998c2`, `8d97162`, `abc28db`). What remains below is genuine integration, ordered by value per day rather than by ambition.

- **Revisit the `None` → `"working"` fallback at the end of `_session_status` (`web.py`)** — an unclassifiable session currently displays as working, on the reasoning that a running process is working. That was unsafe when `None` was 77% of Claude Code sessions; the tail-size fix took it to 8%, so the fallback is now defensible — but an explicit unknown state may be the better answer. Small and self-contained, and the last surviving follow-up from the transcript-tail fix.

- **Baseline — measured 2026-07-24, at real scale** (84 workspaces, 1,709 sessions, 1.9 GB transcripts, 500-process table). *The sweep and classification figures were superseded by `plans/done/260725-1542_PARSE_AND_POLL_PERFORMANCE.md`, which also delivered the negative-lookup cache this block recommended. The process-scan findings below were untouched by that work and still stand.*
  - *Within the scan, `cmdline()` is ~96% of the cost*: `process_iter(["name"])` = 2.66 ms, adding `cmdline` = 57.8 ms. 213 of 502 processes are access-denied and still pay a failed syscall. ~0.1 ms per process, roughly linear in process-table size.
  - *Already solved, do not spend effort here*: workspace-level fresh-session detection is **11 ms**; session-end detection is **0 ms** at the scan layer (≤8 s end-to-end through the TTL and poll interval).

- **Push-based session status via hooks (Claude Code + kiro-cli)** — replace polling with agent-emitted events. **Status: roadmap item, not scheduled. Mechanism fully proven over two rounds.** Row-level identity, the original motivation, was solved without hooks on 2026-07-24, so what remains is latency (~300 ms against a 5 s poll) and events polling cannot see at all: permission prompts, idle nudges, sub-agent boundaries.
  - *Problem it solves*: `status_classifier.py` derives Working/Waiting/Errored by tailing JSONL transcripts behind a 5-second cache, so the dashboard is always up to one poll stale and sees only what the transcript records. Row-level liveness, the other half of the original gap, is now covered by the per-process sidecars, so hooks are no longer the only path to it.
  - *Verified — mechanism works end to end*: a project-level `.claude/settings.json` drove 28 real hook events into a FastAPI endpoint across 4 sessions; 7 hook types fired, zero delivery failures.
  - *Verified — `SessionStart` fires for fresh sessions*, carrying `source: "startup"` (vs `"resume"`), a `session_id` byte-identical to the JSONL name the `claude-code` branch of `_resolve_jsonl_path_uncached` (`status_classifier.py`) keys on, plus `transcript_path` and `cwd` outright — which removes the need for `_get_project_folder()`'s cwd-mangling. Confirmed three independent ways.
  - *Verified — Working vs Waiting is derivable from `hook_event_name` alone*, no transcript read. `Stop.last_assistant_message` even carries the final text. **Trap**: `SubagentStop` fires ~5s before the parent `Stop`; a rule that does not check `hook_event_name` (and `agent_id`, present on `SubagentStop`, absent on `Stop`) will flip a session to Waiting mid-work.
  - *Verified — the catalogue is far larger than documented*: 31 hook event types have schemas in the 2.1.219 binary. Only 8 were wired. The unwired set includes `PermissionRequest`, `PermissionDenied`, `StopFailure`, `PostToolUseFailure`, `TeammateIdle`.
  - *Verified — kiro-cli HAS hooks*, resolving the previous unknown. Not in `kiro-cli settings` (no hook keys) and not in the empty `~/.kiro/hooks/`, but **per-agent in the agent JSON** — both `agent_config.json.example` and the live `kiro_default.json` carry a top-level `"hooks": {}`. Five triggers, extracted from the binary: `agentSpawn`, `userPromptSubmit`, `preToolUse`, `postToolUse`, `stop`. Per-hook fields: `command`, `matcher?`, `timeout_ms?` (default 10s), `max_output_size?` (default 10240), `cache_ttl_seconds?`.
  - *Verified — kiro-cli `agentSpawn` DOES yield a session id, in the environment rather than the payload.* The JSON body carries only `hook_event_name`, `cwd`, `prompt`. But the hook subprocess environment contains `KIRO_SESSION_ID` (e.g. `ef360af4-9374-4931-8e4c-ba67c87e898f`), matching `kiro-cli chat --list-sessions -f json` exactly for that cwd — **the same identifier namespace `_PROVIDER_SPECS` (`presence.py`) already correlates on via `--resume-id`**. This resolves the load-bearing kiro-cli question affirmatively. `agentSpawn` fires ~1.17 s *before* `userPromptSubmit` and carries the prompt, so it is a genuine fresh-session signal.
  - *Caveat on that*: no transcript for that id was found at either path `status_classifier.py` knows (v2 `~/.kiro/sessions/cli/<id>.jsonl`, v3 `sess_<id>/messages.jsonl`). Its `"source":"classic"` points at the sqlite store (`data_kiro.py:16-25`), unopened. So the **id** is confirmed; transcript-path resolution for a classic-source session is undetermined.
  - *Verified — reactivity, measured in-band* (the tool printed `time.time_ns()`; the sink stamped arrival on the same clock): tool result → `PostToolUse` at sink is **266-321 ms**. Against the incumbent's ~2.5 s expected median staleness that is **~9× at the median, ~19× at worst case**. But decompose it: **~180 ms of that is the Python forwarder** (56 ms interpreter startup + 122 ms script) and only ~90 ms is Claude Code's own detect-and-spawn. kiro-cli is ~4× slower — `postToolUse` arrived **1,189 ms** after the tool's timestamp, with comparable forwarder cost, so ~900 ms is kiro-side.
  - *Verified — hooks BLOCK the session, serially. This is the dominant implementation constraint.* Identical 4-turn workload: no hooks 12,184 ms; 31 events wired with a fast forwarder **16,676 ms (+4.5 s, ~280 ms per event)**; a `sleep 5` hook **49,931 ms (+37.7 s)**; a `sleep 15` hook under `timeout: 10` **84,231 ms (+72.0 s)**. `duration_api_ms` stayed flat at 6.3-7.4 s across all four, proving the delta is pure hook blocking, not model latency. The configured `timeout` caps the damage but does **not** make hooks asynchronous. **A shipped implementation must not spawn a Python interpreter per tool call** — a compiled binary or fire-and-forget socket write is the single highest-leverage engineering item here.
  - *Verified — no delivery guarantee.* A session with three simultaneous failure modes (exit 1, unreachable endpoint, uncaught exception) completed `is_error: false` and *faster* than the clean baseline. Non-zero exit is logged at DEBUG only; nothing reaches the model or stderr. **Exactly one delivery attempt per event across all 205 records — no retries.** Good: a dead PowerAtlas endpoint degrades silently. Bad: a missed event is simply gone.
  - *Verified — `SessionEnd` does NOT fire on abnormal termination.* `taskkill /F /T` on a live session (21 events already delivered) produced zero further events, sink count unchanged 8 s later. **`presence.py` cannot be retired**; a hook-only model pins a hard-killed session at "Working" forever, and the process scan must *win* against stale hook state.
  - *Verified — delivery quality is otherwise good*: zero duplicates across 205 events; causally correct ordering in all 38 complete tool cycles; two simultaneous sessions in the same project interleaved with the correct `session_id` on every event and no cross-contamination.
  - *Verified — subagent attribution trap*: a subagent's own tool events carry the **parent** `session_id`. Only `SubagentStart`/`SubagentStop` carry `agent_id`/`agent_type`, so events between them cannot be attributed by `session_id` alone — the consumer must track the open-subagent window.
  - *Verified — 12 of 31 event types fire* (round 2 added `PostToolBatch`, `MessageDisplay`, `InstructionsLoaded`, `SubagentStart`, `CwdChanged`). **No event name is rejected by the settings schema — a typo is silently accepted and simply never fires.**
  - *Verified — `PermissionRequest` and `Notification` DO fire, but only with a real TTY.* Four headless approaches produced nothing (two with genuine `permission_denials` recorded); an interactive session produced both immediately. `PermissionRequest` carries `tool_name` and `tool_input` and lands **1.2 s after `PreToolUse`**; `Notification` follows ~6 s later with `notification_type: "permission_prompt"`, message `"Claude needs your permission"`. Since PowerAtlas watches sessions users run in real terminals, this is the *available* path, not a theoretical one.
  - *Verified — `Notification` has at least two types.* `permission_prompt` (above) and `idle_prompt` (`"Claude is waiting for your input"`), the latter firing **60.3 s after `Stop`** and **exactly once** across ~660 s of idling — a one-shot, not a heartbeat. Useful shape: `Stop` is the immediate turn-ended transition; `idle_prompt` is the "has been unattended a while" nudge that suits the toast-notification item under Workspace Intelligence.
  - *Verified — `PermissionDenied` never fires*, on either denial path, despite being in the binary schema.
  - *Verified — the two denial paths are distinguishable without it.* An **interactive rejection** yields `PreToolUse` → `PermissionRequest` → `Notification(permission_prompt)` → nothing (no `PostToolUse`, no `PostToolBatch`). A **`deny`-rule refusal** yields `PreToolUse` → `PostToolBatch` with no `PermissionRequest`, and `PostToolBatch.tool_calls[].tool_response` carries the reason verbatim: `"Permission to use PowerShell with command systeminfo has been denied."`
  - *Fallback for headless sessions*: a `PreToolUse` with no matching `PostToolUse` still marks blocked/denied — it flagged both denial sessions and none of 38 successful tool calls.
  - *Limitations, verified*: hooks instrument **only sessions started after installation** — a settings file written mid-session is not picked up by the running session. kiro-cli config is **per agent**, so install means editing every `~/.kiro/agents/*.json` plus project-local ones, and silently misses agents added later. kiro-cli has **no `sessionEnd`, no notification and no permission trigger** at all.
  - *Gotchas for implementation*: **kiro-cli executes hook commands through PowerShell, not cmd** — the Claude-Code-style `"<python.exe>" "<script>" label` form is a PowerShell parse error and 0 of 5 hooks ran; it requires the call operator `& "<python.exe>" "<script>" label`. kiro's `tool_name` is `execute_cmd`, not the `execute_bash` name in the agent config's `tools` array; its `stop` carries `assistant_response` (final assistant text, directly usable). `config.py:53` defaults the port to 0 (OS-assigned), so a hook command cannot hardcode one; `same_origin_guard` (`web.py`) 403s any POST lacking `Origin`/`Referer`. A file-spool transport sidesteps both.
  - *Untested*: whether kiro-cli's 10 s default `timeout_ms` blocks the agent the way Claude Code's does. Likely — it prints `✗ 0 of 1 hooks finished in 1.13 s` synchronously in turn output — but unmeasured.
  - *Effort*: **3-5 days.** Ingest + state machine ~0.5 d; config deployment ~1 d (Claude Code is one global file; kiro-cli is the tax); a low-overhead forwarder ~0.5-1 d (**prerequisite, not follow-up**); liveness-backstop reconciliation ~1 d (the real design work — reconciling fast-but-lossy hooks against slow-but-authoritative process scan); "waiting on human" ~0.5 d plus unresolved risk.

- **ACP client (kiro-cli + Claude Code)** — structured agent control over JSON-RPC instead of terminal scraping. **The kiro-cli half was prototyped 2026-07-25/26** (`260725_KIRO_CLI_ACP_CLIENT_PROTOTYPE`) and works end to end from a `/acp` page — create, resume, prompt, stream, cancel, close, tool-call rendering and context-window telemetry — but deliberately as throwaway code, so what carries forward is the measurement below rather than the implementation. **The Claude Code half was never started**: its bridge needs a separate npm bootstrap, so the two gate-open findings below are still static analysis. **The rebuild answer is at the foot of this entry**: not this shape, not yet — ship the read-only half now, gate the chat client on the permission experiment below it.
  - *What it buys, verified on kiro-cli*: streaming `agent_message_chunk`s, `tool_call`/`tool_call_update` with real command and output, a clean `session/request_permission` round trip, `stopReason` as an authoritative idle signal, and `_kiro.dev/metadata` carrying live **context-window usage percentage** and turn duration — data unavailable from transcript tailing.
  - *Verified — `kiro-cli acp` works today with no auth at all* (`"authMethods":[]`). Framing is **NDJSON**, not LSP `Content-Length`. `session/new` takes **~5.4 s for the first session of a process**, spawn and handshake included, and **~2.5 s for each session after** — re-measured 2026-07-26 on 2.14.2, superseding the ~3.2 s single sample, which was neither the cold nor the warm case. MCP startup dominates the first one. Options: `--agent`, `--model`, `--effort`, `--trust-all-tools`, `--trust-tools`, `--agent-engine v1|v2|v3` (default v2).
  - *Verified — explicit slash-command invocation is ordinary prompt text*, not a proprietary RPC. `/tools` sent as prompt text returned in **1 millisecond** with a canned count — impossible for a model round-trip, proving agent-side interception. **This inverts the previous assumption**: `_kiro.dev/commands/available` is an agent→client *push notification* carrying the catalogue (24 built-ins, all 25 user skills as `prompts` with `serverName: "skill:config"`, 15 tools, live MCP server status); calling it as a client→agent request returns `-32601`. A standards-only client sees skills fine.
  - *Verified — `_kiro.dev/commands/execute` kills the agent process with exit code 0, no stderr, no JSON-RPC error.* Reproduced 2/2 across separate processes and two param shapes. Combined with the process writing nothing to stderr all session, a supervisor can trust **neither exit code nor stderr** for health. Also: one process is a **tree** (MCP servers are grandchildren — a plain kill leaks them).
  - *Verified — Claude Code ACP gate is OPEN.* The `@zed-industries/claude-code-acp` bridge hardcodes `settingSources: ["user","project","local"]` (`acp-agent.js:777`), which is the documented source loading `~/.claude/skills/`; prompt text passes through unrewritten (`:992-1006`); no `/q*` skill is on its `UNSUPPORTED_COMMANDS` deny-list. Static analysis only — never executed.
  - *Verified — two Claude Code SDK-path gaps*: `allowed-tools` in SKILL.md is **silently ignored** through the SDK, and the bridge **drops hook lifecycle events** (`hook_started`/`hook_progress`/`hook_response` fall through to `break;` under a `// Todo`) — precisely the data PowerAtlas wants. Statusline is absent from both packages.
  - *Verified — `session/load` attaches to pre-existing sessions with full context, including ones the user's own terminal kiro-cli created.* Loading a real 17 KB `agent-playbook` session and asking what it had been working on returned `Plan 260724_QCLOSE_DOCUMENTATION_RIPPLE_SWEEP_AND_QPLAN_DOC_IMPACT_WIDENING, Phase 4` — matching the stored `title` exactly, unguessable. Load takes ~3.8 s and **replays history as `session/update` events including `tool_call`/`tool_call_update` pairs**, so a client can rebuild the full timeline without tailing JSONL. This answers the scope-defining question: ACP is not limited to sessions PowerAtlas itself launched.
  - *Verified — concurrent attach is hard-refused, not silently shared.* Ownership is a lock file `~\.kiro\sessions\cli\<sid>.lock` containing `{"pid":…,"started_at":…}`, and **an ACP process takes the same lock a terminal session does**. Loading a busy session returns in 0.84 s: `-32603 … "Session is active in another process (PID 23316)"`. Lock not stolen; the owner's turn completed normally; the final `.jsonl` had zero unparseable lines and none of the intruder's content. **Stale locks are correctly ignored** — the guard is a pid liveness check, not file presence, so crashed sessions stay attachable while live ones are protected. Reading the lock file is a cheap pre-check PowerAtlas could do without connecting at all.
  - **Corrected 2026-07-26 on 2.14.2 — the typed refusal is gone, and the relationship is inverted.** Measured live against a real terminal session holding the lock, the agent returns a bare `-32603 "Internal error"`: no PID, no text, indistinguishable from any other internal failure. So the **lock file is now the only thing that can name the holder**, not the cheap pre-check it was described as. And a pid-liveness-only pre-check would have been wrong *every time it fired*: of this machine's 803 lock files, 22 name a currently-existing pid and **all 22 are recycled** (svchost, firefox, RuntimeBroker, pwsh), created days to weeks after their lock was written; a repeat census at 804 locks found 25, again all recycled. A usable pre-check additionally requires the holder's process to have started at or before the lock's own `started_at`. Also measured: **`session/load` takes the lock too**, and locks survive the agent's exit — so a timed-out load leaves a lock naming your own agent, which reads as "active in another process" unless you recognise your own pid.
  - *Now verified*: the refusal generalises from ACP-vs-ACP to a live *interactive terminal* owner — driven end to end from `/acp`, which surfaces it as an in-use message rather than hanging.
  - *Verified — skills load the full SKILL.md body* (round 1's ~50% is now high confidence). Discriminator: flag names appearing only in `qmemory-eval`'s body Flags table while the frontmatter advertises different ones, with the wire catalogue entry confirmed to carry only the `description` string. Invoked with tools forbidden: **0 tool calls**, and it reproduced the script path, all four flags, and the five-row label taxonomy. Advertised set matches `~\.kiro\skills\` exactly — 25 each, set difference empty both directions. **Arguments inconclusive**: `/qsummarize-chat 1` honoured the body's output format but emitted 4 pairs rather than 1; cannot distinguish "argument never reached `$ARGUMENTS`" from "model ignored it".
  - *Verified — process model*: one process holds **multiple concurrent sessions** across different workspaces with no cross-talk (two parallel prompts: 3.71 s wall, not 6.75 s), so one process for everything is viable. But MCP servers spawn **per session**: **~2 + 5N processes and ~283 + 254N MB**, re-measured 2026-07-26 against a real supervisor — three concurrent sessions were **17 processes / 1,045 MB** against a **283 MB** one-agent baseline. That corrects the round-2 model in both directions: the per-session memory figure was ~20% high at 306 MB, and the process count was one short, the extra being the parent's own `conhost`. Time-to-first-token warm 1.88-2.14 s.
  - *Re-measured 2026-07-31 on kiro-cli **2.16.0**, and the model got materially cheaper — plus the close method was finally exercised.* Walking the agent's own process tree and summing RSS: **2 procs / 79 MB** at N=0, then **+3 procs and +150 MB per session** after the first (the first costs +374 MB because it pays the shared MCP/node startup). Three concurrent sessions are **11 procs / 754 MB**, against the **17 procs / 1,045 MB** on record — so the marginal session went from ~5 procs / 254 MB to **~3 procs / 150 MB**. *Caveat on the baseline*: 79 MB here vs the 283 MB recorded above is too large a gap to be version drift alone, and the likely reason is that MCP servers spawn on `session/new` rather than at `initialize`, so a genuine zero-session baseline has none running — treat the two baselines as possibly not measuring the same thing, and trust the **marginal** figure over the absolute. Session creation also collapsed: **1.47 s / 0.55 s / 0.48 s** for the first three, i.e. warm creation is now sub-second against ~2.5 s on 2.14.2.
  - *Verified — `_kiro.dev/session/terminate` still exists on 2.16.0, works, and actually reclaims.* All four candidates were probed: `session/close`, `session/terminate` and `_kiro.dev/session/close` each answer **-32601 Method not found**; only **`_kiro.dev/session/terminate`** works, returning `{}` in ~2 ms. **No documented replacement has appeared**, so the private-extension dependency stands exactly as feared — but the extension itself is real rather than hopeful: terminating one session **freed 3 processes and 172.6 MB**, it is **per-session** (a sibling session prompted fine afterwards, `end_turn`), and the terminated handle is properly gone (prompting it returns `-32603` with a typed `data: "No session found with id"`). **Critically for the dispatch-and-resume shape: terminate removes the `.lock` and leaves the `.json` intact**, so a terminated session stays resumable later. That makes an idle sweeper a straightforward design rather than a gamble — the memory lever works, it is just undocumented.
  - *Verified — `--agent` binds to **every** session on the process, not just the first, and neither it nor `--trust-tools` validates its argument.* The flag's help text says "when starting the first session", which describes when it is *read*, not what it scopes to: three sessions created under `--agent kiro_planner` all returned `modes.currentModeId: "kiro_planner"`, against `kiro_default` for an unflagged control. So one agent choice covers a whole multi-session process. **But a name that does not exist is accepted silently** — `--agent definitely-not-an-agent-xyz` spawned normally and fell back to `kiro_default` with no error and nothing on stderr, exactly as `--trust-tools` accepted a non-existent tool name. Neither flag fails loudly, so a client that depends on either must **read the setting back and assert it** (`currentModeId` from the `session/new` response) rather than trusting that the flag took.
  - **Verified 2026-07-31 — MCP startup is ~97% of the marginal session cost, and agent choice controls it. This is the largest single lever on this entry.** Same measurement method, `-a`, three sessions per configuration:

    | configuration | procs @ N=3 | RSS @ N=3 | marginal/session | MCP init notifications | warm create |
    |---|---|---|---|---|---|
    | default agent | 11 | 758.5 MB | ~151 MB | 11 (2 distinct servers) | 0.49-0.79 s |
    | `--agent kiro_planner` | **2** | **291.0 MB** | **~4.6 MB** | **0** | **0.006-0.01 s** |

    **The whole marginal cost is one MCP server.** `~\.kiro\settings\mcp.json` defines four servers, two enabled: **`playwright`** (stdio, `node …@playwright\mcp\cli.js`) and **`atlassian`** (HTTP, `mcp.atlassian.com`); `zoho` and `msgraph` are `disabled: true`. That maps exactly onto the measured `+3 procs` per session — `node.exe` + `cmd.exe` + `conhost.exe`, i.e. one stdio server spawned through a shell — while the HTTP server costs **zero** processes, consistent with `mcpCapabilities: {http: true}` from the handshake. Attribution is structural rather than per-process (tree totals were measured, and `cmd`/`conhost` are single-digit MB), but it is tight: **dropping the one stdio server captures ~97% of the saving while HTTP MCP servers can be kept for free.** *Caveat on the counter*: 11 notifications for 2 servers across 3 sessions means `_kiro.dev/mcp/server_initialized` fires more than once per server per session — use it as a presence signal, not a count.

    Without MCP the **process count does not grow with session count at all** — it stays at `kiro-cli.exe` + `conhost.exe` however many sessions are open — and marginal memory falls **33x**. The cost model therefore is not one model but two: **~306 + 151N MB with MCP**, **~277 + 4.6N MB without**. `kiro_default.json` sets `mcpServers: null` but carries `includeMcpJson`, so the default agent's MCP set arrives from `mcp.json`; `kiro_planner` does not pull it.
    - *The actionable form is not "use `kiro_planner`"* — that is a planning agent with its own prompt and tool set, not a general dispatch target. It is that a **PowerAtlas-specific agent controlling its own MCP set** captures nearly all of the saving while keeping whatever prompt and tools are wanted. And the trade is much smaller than "lose MCP": since only the **stdio** server carries the cost, a dispatch agent can keep every HTTP MCP server for free and drop only `playwright`. What that costs is browser automation inside dispatched sessions — a narrow, checkable question rather than a general capability loss.
    - **Decided 2026-07-31 — keep the default MCP servers. The lean-agent lever is closed and should not be re-proposed.** Dispatched sessions get the same MCP set as any other kiro-cli session, browser automation included. The consequences are accepted knowingly: the applicable cost model is **~306 + 151N MB**, not the lean one; session creation stays at **~0.5-1.4 s** rather than ~0.006 s; and every resumed session pays MCP startup again.
    - *Consequence for `MAX_SESSIONS`* (currently hardcoded to 3 in `acp.py`, priced against the superseded ~254 MB figure): at the measured ~151 MB the old value is roughly 2.5x too conservative, and **8** is the defensible replacement — ~1.5 GB, about 18% of the 8.4 GB free on a machine already at 73% utilisation. The limit should still become a **runaway-dispatch safety valve** rather than a memory budget expressed as a constant, with an idle sweeper on `_kiro.dev/session/terminate` doing the real memory management, and it should be configurable rather than hardcoded.
    - *And the sweeper's TTL should be generous, precisely because MCP is kept.* Every sweep-then-resume cycle re-pays MCP startup, so an aggressive TTL trades ~151 MB of idle memory for repeated multi-second stalls on the user's next interaction. Tens of minutes, not minutes. In the lean configuration an aggressive sweeper would have been nearly free; that option went with the decision above.
  - **Verified — the memory lever is an undocumented kiro extension, and this is the largest risk to any rebuild that holds sessions open.** The cost model above is only bounded if sessions can be closed, and **`session/close` does not exist** on 2.14.2 (`-32601 Method not found`). What works is **`_kiro.dev/session/terminate`**, found by reading strings in the binary — a private method with no documented contract, which a release can withdraw without notice. It does work: one close measured **-5 processes and -253 MB**, and removed the session's `.lock`. A client that plans on N concurrent sessions is therefore betting the memory ceiling on a kiro-private extension; a build without it can only free memory by exiting the agent.
  - *Verified — other capabilities*: `session/cancel` interrupts a running turn cleanly (`stopReason: "cancelled"` at 8.01 s, after 118 chunks), and is best sent as a **notification** rather than a request — in that form, measured on 2.14.2, the outstanding `session/prompt` returns `stopReason: "cancelled"` in ~0.11 s and the session takes the next prompt on the same conversation. `session/set_mode` validates (`-32603` on a bad mode); **`session/set_model` accepts any string including nonsense without error, so its effect is unverified**. Absent (`-32601`): `session/history`, `session/list`, `session/select_model`, `session/set_effort`, `session/close`, `_kiro.dev/session/compact`, `authenticate`, `fs/read_text_file`.
  - **Product-relevant caveat: creating and prompting MUTATE the user's store — attaching, on its own, does not.** A load-plus-prompt grew a real user session's `.jsonl` from 17,057 to 17,665 bytes and moved `updated_at`, so any surface that *prompts* leaves turns in the user's history even if it presents as read-only. Two refinements measured 2026-07-26. **`session/new` writes `<sid>.json`, `<sid>.jsonl` and `<sid>.lock` at creation, before any prompt** — every session such a surface opens is a permanent artifact among the ~13,300 already there, and the prototype had to enumerate the store by `cwd` before and after each phase to keep its own count honest. But **`session/load` alone leaves the transcript byte-identical** — `.json` and `.jsonl` are untouched and only the `.lock` is rewritten, which a graceful close then removes. So a genuinely read-only viewer of exited sessions *is* available; it is the turn and the creation that write, not the attach.
  - *Risks*: young protocol subject to breaking changes; the binary **self-updated mid-spike** (2.14.0→2.14.1, then →2.14.2 during the prototype), which a long-lived launcher will hit; Windows consumers must force UTF-8 and strip ANSI escapes.
  - **Verified — under `--trust-all-tools` an unprompted `shell` command executes, outside the session's own recorded scope.** Read back from the store's `.jsonl` transcripts 2026-07-26: kiro-cli ran a PowerShell command that read *and wrote* `<sid>.json` in the real session store, while that session's own recorded permissions were `allowed_write_paths: []` and `trusted_tools: []` — so `-a` overrides the recorded filesystem scoping rather than merely skipping a prompt. It fired on 2 of 3 sessions in one phase, and again mid-session on a turn that was not the first, so **the trigger condition is unknown**. Plan on an ungated command running on any turn; `-a` has to be re-decided before anything ships, and `session/request_permission` (which the protocol offers and the prototype never exercised) is the alternative.
  - *Product note*: a full ACP client turns PowerAtlas into an agent chat panel, a different product from the current launcher/dashboard, overlapping what Zed ships. That run has now happened — the state/control learning is what the bullets above record; committing the product to it is a separate decision, and still open.
  - *Effort — measured, not estimated.* The vertical slice ("drive a kiro-cli session from PowerAtlas, including one the user started") was built as `260725_KIRO_CLI_ACP_CLIENT_PROTOTYPE` across **two calendar days** — roughly **13 hours of elapsed working time** once the overnight gap is removed, first phase commit 2026-07-25 17:48, last 2026-07-26 16:17. That is elapsed time on a plan run with heavy parallel review rather than a solo hand-coded week, so it is not like-for-like with the hand-written day figures elsewhere on this page; read it as a floor. It also bought a *throwaway* — the stated deliverable was knowledge, and the code is to be rebuilt. **And it includes work nobody decided to do.** The plan's own design table records `Tests | None` for the prototype, with the waiver scoped to `acp.py` and the ACP routes; the `TestAcp*` suite that now exists was never re-decided, it accumulated because each review round wanted mutation-verified evidence before it would accept a fix. So the figure over-predicts a rebuild that keeps the waiver and under-predicts one that plans for tests from the start — it is a poor estimator in either direction, not merely a floor.
  - *Where the cost actually sat*, which the ~1 week estimate got wrong: transport and process supervision — the two lines that estimate was mostly spent on — were the cheap parts. The expensive ones were **securing the surface** (a WebSocket bypasses `same_origin_guard` structurally, and reviewing that surfaced four `Host`-validation defects in *shipped* code, three of them predating the work entirely) and **verification traps**, each of which produced a confident false pass before it was understood: `httpx` and the `websockets` client both silently drop a header-supplied `Host`, and `StaticFiles` sets no `Cache-Control`. A rebuild inherits the design but not that review — **and inherits less of the test suite than the raw growth suggests.** The suite went **622 → 964 passing**, not 611 → 964: the plan's 611 baseline was never true, and clean `main` collected 625 and passed 622 with 2 pre-existing failures, verified by stashing every change and re-running at `HEAD`. Nor did the growth land mostly on shipped code. Phase 3a's 145 Host-validation regressions did, which is where that reading came from; but counted at `d5cd128`, `tests/test_web.py` holds 453 test definitions of which **161 sit in 32 `TestAcp*` classes** and 128 name `acp_mod` — so the larger part of the growth tests the throwaway module, and a rebuild inherits neither it nor them. Re-derive rather than trusting those figures: the discriminators are the `TestAcp` class prefix and the `acp_mod` reference, and parametrisation expands definitions into more collected tests than definitions, so the collected-count ratio is higher again.
  - *Not delivered, so still to be paid for*: the **Claude Code half** (never started — the two gate-open bullets above are still static analysis), permission prompts (the prototype runs `-a`), `presence.py` integration so ACP-owned sessions read correctly in the dashboard, README, and a session browser.
  - **The rebuild answer, on the measurements above — 2026-07-26: do not rebuild the chat client. Split this item in two, ship the small half, gate the large one.** Ten of ten success criteria met in ~13 h against a 7-10 day estimate reads as *ACP is cheap, go build it*, and that reading is wrong. Those criteria measured whether an ACP **chat surface** works; the reason this item exists is **Automation & Workflows**, which means *unattended* dispatch. The prototype answered the first decisively and the second not at all — by design, since its own `-a` justification is "a prototype driven by a human watching it", and automation is the absence of that human. The decision a rebuild turns on is therefore the one thing the prototype was scoped never to test.
    - **Ship now — a read-only viewer of exited sessions.** `initialize` + `session/load` + replay + terminate. No `-a`, no prompting, no `session/new`, no permanent artifacts in the user's store, no memory accumulation beyond the one session held open. It is *provably* non-mutating rather than intended to be: Phase 6 measured the session's `.json` and `.jsonl` byte-identical by sha256 across a load, with only the `.lock` rewritten and a graceful close removing it. Roughly a fifth of what the prototype built, and it delivers the one capability nothing else on this page reaches — reading a terminal-created session's full history without tailing JSONL.
      - *Re-confirmed on kiro-cli 2.16.0, 2026-07-31* (this half is the one still scheduled, so its assumptions were re-checked while the permission spike had an agent up): `agentCapabilities.loadSession` is **still `true`**, `authMethods` still `[]`, `protocolVersion` still 1. Two findings make it cheaper than the estimate above. **The `read` tool never issues a permission request at all**, so a replay-only client needs no permission handling even after `-a` is dropped — the `-32601` catch-all is sufficient for this half, exactly as shipped. And `session/new` fell from 5.4–5.84 s to **1.5–2.2 s**, so the latency budget the viewer was sized against is roughly a third of what was assumed. The `session-tab-title.md` steering does not apply here either: its own guards say new sessions only, skip on resume.
    - **Gate the chat client on the permission experiment** (its own item below). Four things stand between the measured chat surface and unattended dispatch, and the prototype closed none of them. **`session/request_permission` has never round-tripped** — the prototype's client refuses every agent→client request with `-32601`, and under `-a` the agent never asks; flip the trust flag in a rebuild and, on this client, every tool call fails. An **unprompted `shell` command executed and wrote outside its session's cwd**, overriding that session's own recorded `allowed_write_paths: []`, on a trigger nobody identified — tolerable with a human watching, not on a schedule. The **memory lever is `_kiro.dev/session/terminate`**, a kiro-private extension found by reading strings in the binary, with no fallback in the prototype and no idle sweeper. And the evidence decays: kiro-cli self-updated **twice** mid-project and one measured behaviour *regressed* (the typed in-use refusal became a bare `-32603`), so a rebuild scheduled months out inherits a measurement set with a known half-life.
    - **Status of those four blockers as of 2026-07-31, after the permission spike ran** (full results in the `[SPIKE]` entry below; all on kiro-cli **2.16.0**, not the 2.14.2 the rest of this entry is pinned to). **Blocker 1 — closed.** The round trip works in both directions: a standard `session/request_permission` arrives, allow is honoured, and **deny is enforced** (`tool_call_update: failed`, no side effect, clean `end_turn`). The prototype's own `clientCapabilities` do not suppress it, and no other agent→client request type appeared, so the rebuild's protocol delta is one handler rather than a family. **Blocker 2 — explained, and it was never kiro's fault.** The unattributed `shell` write into the session store is `~\.kiro\steering\session-tab-title.md`, matched line for line once prompts made the command visible; `allowed_write_paths` turns out to be descriptive metadata with nothing enforcing it, and the trigger was `-a`. It does not disappear, though: that steering is global, so **every** ACP dispatch attempts one session-store write on its first turn and a scheduler needs a standing rule for it. **Blocker 3 — exercised 2026-07-31, and it resolves in favour of building.** `_kiro.dev/session/terminate` still exists on 2.16.0, works (`{}` in ~2 ms), is **per-session**, and genuinely reclaims (**3 processes and 172.6 MB** freed) while removing the `.lock` and leaving the `.json` intact, so a terminated session stays resumable. The ceiling also fell: the marginal session is now **~3 procs / ~150 MB**, not ~5 / ~254, putting three concurrent sessions at **754 MB** rather than 1,045. What does *not* improve is the dependency itself — `session/close`, `session/terminate` and `_kiro.dev/session/close` all still answer **-32601**, so there is still exactly one way to free a session and it is undocumented. Details under the process-model bullet above. **Blocker 4 — confirmed as a real hazard, and it cut both ways.** The binary moved 2.14.2 → 2.16.0 before the spike ran, and `session/new` *improved* from 5.4–5.84 s to **1.5–2.2 s** — decay is not only degradation, and a rebuild estimated on the old figure was over-costed on latency.
      - **The new blocker the spike found, which is the one to design against**: `--trust-tools` is a tool allowlist, **not a path boundary**. `--trust-tools shell` let a write land outside the session cwd with the client never consulted. So the safe unattended configuration is *no trust flags plus a client-side policy engine* — which is viable, because the permission payload carries the full command line and `_meta.trustOptions` exposes a persistent regex allowlist (`setting_key: allowedCommands`). It is the round trip, not the trust flag, that makes unattended dispatch decidable.
    - **What would change this** — live inputs to the decision, not a closing argument: ~~the permission experiment returning clean~~ (**it did, 2026-07-31** — see the blocker status above; the argument for the gate is now blockers 3 and 4 plus the product question, not the protocol); kiro-cli shipping a documented session close in place of the private extension; the `messagingSocketPath` spike under `## Misc` succeeding, which would collapse ACP's remaining unique value to exited-session resume; or the product intent turning out to be a chat panel after all — which the product note above already says makes PowerAtlas a different product, overlapping what Zed ships.

- **[SPIKE — RUN 2026-07-31, kiro-cli 2.16.0] The permission round trip. Result: the round trip works; `--trust-tools` does not do what the gate assumed.** Run headless, as a standalone probe that never touched `src/power_atlas/acp.py` — the spike decides whether that module is rebuilt, so writing rebuild code first would have inverted the gate. 12 probe sessions were created, all identifiable in the store by a cwd containing `acp_spike`. Measurement (1) passes decisively, (2) fails in a way that changes the design rather than closing it, (3) turns out not to be a latency question at all.
  - **(1) The round trip completes, in both directions — this is the finding that reopens the rebuild.** `session/request_permission` arrives as **standard ACP**, not a kiro extension, and a standard response is honoured. *Allow*: the tool ran and returned its nonce, `stopReason: end_turn`. *Deny*: `tool_call_update` reported `failed`, the nonce never appeared anywhere in the transcript, and the turn still ended cleanly rather than hanging or erroring. **The denied run is the one that matters** — an agent treating rejection as advisory would look identical to a working gate until the moment it counted. Options offered are `allow_once` / `allow_always` / `reject_once`; **there is no `reject_always`**, so a policy engine can permanently say yes and never permanently no.
  - *And the prototype's own handshake does not suppress it.* Run with `acp.py`'s exact `clientCapabilities` (`fs.readTextFile:false`, `fs.writeTextFile:false`, `terminal:false`), permission requests still arrive — and **zero** other agent→client requests did, so the `fs/*` and `terminal/*` traffic the catch-all responder was built to refuse never materialised. The rebuild's protocol delta is one handler, not a family of them.
  - *The permission payload is richer than the spec requires, and this is the lever for unattended dispatch.* The `toolCall.title` carries **the full command line**, not just a tool name (`Running: Set-Content -Path "C:\…"`), so a policy engine can inspect what it is approving. Alongside it kiro ships a private `_meta.trustOptions` block: three regex granularities (full command, `cmd *`, `base *`) plus `setting_key: "allowedCommands"` — a **persistent, pattern-based allowlist**, which is a far more precise instrument than the coarse tool flag this spike was written to test.
  - **(2) `--trust-tools` is a genuine tool allowlist and NOT a path boundary.** The allowlist half works: `--trust-tools` naming a different tool left `shell` fully gated and prompting. The boundary half does not exist. Under `--trust-tools shell`, with the client configured to **reject everything it was asked**, a bounded canary test wrote a file **one directory above the session cwd with zero permission requests** — the client was never consulted. Trusting `shell` means trusting the whole filesystem at the user's privilege level. Two operational notes: tool names are `shell`, `write`, `read`; **`read` never prompts at all** (relevant to the read-only viewer, which therefore needs no permission handling); and `--trust-tools` **silently accepts names that do not exist** — a typo fails safe but gives no signal.
  - *This also retires the "unidentified trigger".* The 2026-07-26 incident — an unprompted `shell` command writing into the session store, overriding that session's `allowed_write_paths: []` — was **PowerAtlas's own machine configuration**, not agent misbehaviour. Once permission prompts are on, the command is visible in full: it matches `~\.kiro\steering\session-tab-title.md` line for line (same variables, same `"title"` regex, same `WriteAllText`, same OSC-0 escape), and that file's own guards include *"Silent: do not mention these steps"*. So `allowed_write_paths` in the session record is **descriptive metadata with no enforcement behind it**, and the trigger was simply `-a`. **Consequence for automation**: this steering is global and fires on the first turn of every new CLI session, so *every* ACP dispatch PowerAtlas makes will attempt one session-store write — a policy engine needs a standing rule for it, and it is the single most frequent prompt a scheduler will see.
  - **(3) Not a latency problem — an attention problem.** Same 4-file task, two trials each: gated **21.43 s / 20.59 s** against `-a` **20.37 s / 19.44 s**, i.e. roughly **1.1 s spread across 4 prompts**, which is inside the run-to-run variance of a ~20 s turn. With an automated policy the client decision itself cost **0.036–0.11 ms**. So the round trip is free for a scheduler. What it is not free for is a human: **4 prompts for a 4-file task**, 5 tool calls of which only the `read` was silent. A prompt-per-call is viable unattended and hostile attended — the opposite of the shape the gate assumed.
  - *Re-measurements on 2.16.0, since the evidence-decay warning was well founded — the version moved 2.14.2 → 2.16.0 before this ran.* `session/new` now takes **1.5–2.2 s**, against the 5.4 s cold / 5.84 s figures on 2.14.2, with MCP servers still initialising (three `_kiro.dev/mcp/server_initialized` notifications) — so the headline latency of a dispatch is roughly a third of what the rebuild estimate assumed. `initialize` 0.6–0.9 s. `protocolVersion` still 1, `authMethods` still `[]`, and **`agentCapabilities.loadSession` still `true`**, so the read-only viewer's core method survives the version bump.
  - *What this does and does not settle.* It removes the first of the four blockers on the entry above and explains the second. It says nothing about `_kiro.dev/session/terminate` (blocker three), which was not exercised, and nothing about the memory ceiling. Re-run rather than trusting this: the probe is throwaway, kiro-cli self-updates without asking, and one measured behaviour has already regressed once across a version bump.

- **A PowerAtlas dispatch agent — cut what every dispatched session drags into context before it reads a word of the task.** kiro-cli agents are declarative JSON (`prompt`, `tools`, `allowedTools`, `resources`, `includeMcpJson`, `model`, `hooks`), and `--agent` is now known to bind to **every** session on a process, so one agent definition covers a whole dispatch client. Today PowerAtlas would dispatch under `kiro_default`, which is configured for an interactive developer at a terminal and is close to maximal on every axis.
  - **Measured 2026-07-31, and the steering figure alone justifies the item.** `kiro_default.json` sets `tools: ["*"]` (every tool advertised), `includeMcpJson: true` (MCP tool schemas for `playwright` and `atlassian` in context), and a `resources` list that globs **`file://.kiro/steering/**/*.md`**, `skill://.../SKILL.md`, `file://memory/**/*.md`, `AGENTS.md` and `README.md`. The steering glob alone resolves to **11 files totalling 104,668 bytes** — roughly **26k tokens**, before the task, on every dispatched session. Skills and MCP tool schemas are on top of that and were not measured.
  - *Why it bites PowerAtlas specifically.* A dispatched session is usually narrow and single-purpose — run a check, summarise a diff, answer one question — but pays the full interactive-developer preamble regardless. And the waste is **visible in the product**: the prototype already renders a live context-window percentage from `session/update`, so a user watching a dispatched session watches it start most of the way full. It also compounds with the decision above to keep MCP: servers stay, so their tool schemas stay in context too.
  - **Boundary — this does not reopen the keep-default-MCP-servers decision (2026-07-31).** Servers stay enabled and browser automation stays available. The question here is what a *dispatch* agent advertises and loads into the context window, which is `resources` and `tools`, not `mcp.json`. Any proposal that removes a server belongs to that closed decision, not this item.
  - *Open questions, none of which should be guessed*: what the real token cost is per resource class (steering vs skills vs MCP schemas) — measure via the context-window percentage on a fresh session under each agent, do not estimate from byte counts; whether narrowing `resources` breaks skills the dispatched work actually invokes; whether `session-tab-title.md` belongs in a dispatch agent at all, given it costs a shell tool call on every session's first turn and its session-title half lands about 1 time in 11; and whether a dispatch agent should pin `model` (currently `null`) so dispatch cost is predictable rather than inheriting whatever the default is.
  - *Cheapest first step*: create one session under `kiro_default` and one under a stripped candidate agent, read the first context-window figure from each, and difference them. That is a single probe run and it converts this whole item from plausible to sized.

- **[P2b] Session stores PowerAtlas cannot currently see — a real coverage gap, quantified**
  - **23 v3 sessions** under `~\.kiro\sessions\<hash>\sess_*\` are invisible to PowerAtlas *and* to `_kiro.dev/session/list`. PowerAtlas has a v3 **read** path (the v3 branch of `_resolve_jsonl_path_uncached` in `status_classifier.py`) but no v3 **discovery** path — `data_kiro.py` only globs `sessions\cli\*.json`.
  - *Who writes that store — answered 2026-07-24: not Kiro IDE, and nothing currently.* A live test creating a fresh Kiro IDE agent session left the count at **23**; the IDE writes to its own tree, `%APPDATA%\Kiro\User\globalStorage\kiro.kiroagent\workspace-sessions\<base64-cwd>\<uuid>.json`, which `data_kiro_ide.py:24` **already reads** — which is why that session appeared in the UI immediately. Combined with the 2026-06-18 → 2026-07-16 date range, the v3 store is a dormant artifact of the v3 migration period, not a live blind spot.
  - **11 "classic" sqlite conversations** in `conversations_v2` (`%LOCALAPPDATA%\Kiro-Cli\data.sqlite3`) have **no file on disk at all** and appear in neither candidate. PowerAtlas already merges their *cwds* for workspace discovery but not the sessions themselves.
  - **`kiro-cli chat --list-sessions -f json` is the only source that unifies all three stores**, tagging each entry with `source` (`v2`/`v3`/`classic`) — per-entry shape is the ACP shape plus `source`. From `C:\Users\QSylvestre.POLESTAR` it returned 277 sessions: 256 v2, **18 v3, 3 classic**. Cost ~2.13 s per query and it is **cwd-scoped, not global**, so covering 58 workspaces means 58 spawns. Worth considering as a targeted supplement for a single workspace, not as a global replacement.

- **Open experiments** — rounds 1 and 2 closed all but one; the last closed on 2026-07-26. The closures and their findings are in git history (`23b31a3`, `20e19c1`, `87a742b`, `fc43f47`)
  - **Does an interactive `kiro-cli chat` write a transcript? — answered 2026-07-26: yes, but only from a real console.** Four variants were run. `--no-interactive` piped, interactive with piped stdio and a graceful `/quit` (exit 0), and `--no-interactive` in a real console window all wrote **zero files**; only a **fully interactive TTY session** persisted, and the session it wrote was afterwards resumed over ACP with its history intact. So transcripts exist for the sessions PowerAtlas actually watches, and status classification for the kiro-cli half is not blocked. Two consequences follow. For tooling: **any automated verification of terminal sessions has to allocate a real console**, since piping stdio silently produces nothing to observe. For the product: it re-shapes **Dispatch no-interactive tasks** under `## Automation & Workflows`, which is specified on exactly the flag that persists nothing.

## Misc

- **[SECURITY — open, live] No NetBird access policy restricts this host.** Measured 2026-07-31: `netbird status` reports `Peers count: 4/17 Connected` and `netbird status -d` enumerates **all 17** account peers in this host's network map, including machines belonging to other people (`akita`, `paros-g`, `nuc-chicago`, `ec2amaz-tv495hp`, `macbook-air-de-polestar`, …). NetBird's management server only distributes map entries for peers a policy connects, so the stock `Default` (All → All) policy is almost certainly still enabled.
  - *Why it is on the roadmap rather than in a plan*: it is an external console change administered by others, and `260731_ACP_REMOTE_CLIENT_PRODUCTIZATION` shipped its remote surface on the device cookie as the **sole** authorization layer on that basis (D33), rather than the two independent layers D3 designed. The cookie held against 60+ executed attacks — the boundary is not broken — but a single credential now carries the whole model, and what sits behind it is `kiro-cli acp -a`.
  - *Cost to close*: ~5 minutes. app.netbird.io → Access Control → Policies; create a group holding the laptop, `iphone-quentin` (`100.78.26.204`) and `ipad-quentin` (`100.78.66.16`); scope a policy to it; disable `Default`. Verify with `netbird status` — the peer count should collapse to your own devices. **Nothing in the implementation depends on the policy's absence**, so this re-establishes the second layer with no code change.
- **[SECURITY — open] TCP 139 (NetBIOS Session Service) listens on the NetBird address**, owned by the Windows `System` process (pid 4). Found incidentally during a socket census on 2026-08-01; it was the only listener left on `100.78.142.124` after PowerAtlas teardown. Predates and is unrelated to PowerAtlas, but it is reachable by the same 17 peers as the item above, and the item above would also cover it.

- **Visualize/interact with opened sessions from PowerAtlas** — the word **opened** is load-bearing and is what keeps this item alive: a session *live in a terminal* still cannot be attached, because `kiro-cli chat` is itself an ACP wrapper and its grandchild holds the lock. What is no longer true is "reachable without ACP" — as of 2026-07-26 an ACP path exists for **exited** sessions, including ones a terminal created, and renders them at `/acp` (`260725_KIRO_CLI_ACP_CLIENT_PROTOTYPE`). So the messaging-socket lead below is now the candidate for the *live* half only.
- **Local network access to mimic claude code remote control in semi-remote fashion** — **this is about kiro-cli**, which has no such feature; Claude Code already ships one (`remoteControlAtStartup`). The kiro-cli side is currently unreachable — see `plans/CLOSED_INVESTIGATIONS.md` for the two walls and what would reopen it. The `messagingSocketPath` spike below is the live thread for the Claude Code side.

- **[SPIKE] `messagingSocketPath` — a possible path to driving live sessions without ACP**
  - *What was found 2026-07-24*: the reader for `~\.claude\sessions\<pid>.json` accepts a `messagingSocketPath` field (aliased `sock`), and the binary carries a `[uds-client]` that sends `{type:"user", message:{role:"user", content}, priority:"next", from}` to such a socket — i.e. **injecting a prompt into an already-running session**.
  - *Why it matters*: that is interaction with sessions the user started in a terminal, which ACP cannot do without `session/load` taking ownership and mutating history, and which hooks cannot do at all. It would collapse the two Misc items above into something far cheaper than the ~1 week ACP slice.
  - *Status: unverified and currently absent.* No live session file on this machine carries the field — observed keys are `pid, sessionId, cwd, startedAt, version, peerProtocol, kind, entrypoint, name, nameSource, status, updatedAt, statusUpdatedAt, bridgeSessionId`. So publication is gated on something. `remoteControlAtStartup` is the obvious candidate; adjacent settings in the same block are `isolatePeerMachines`, `daemonColdStart`, `autoUploadSessions`, `inputNeededNotifEnabled`, `agentPushNotifEnabled`.
  - *Cheapest next step*: toggle `remoteControlAtStartup`, start a session, and check whether `messagingSocketPath` appears. If it does, the follow-on question is what the socket accepts and whether writing to it is supported or merely possible.
  - *Caveat*: this is undocumented internal IPC in a self-updating binary — a heavier dependency than reading a status field, and one that can vanish without notice. Treat as a spike, not a plan.

- **Claude Code session-sidecar fields — full inventory and verdict**
  - Every field the binary's reader accepts from `~\.claude\sessions\<pid>.json`. PowerAtlas consumes four today (`pid`, `sessionId`, `cwd`, `startedAt`) plus `status`/`waitingFor`. Recorded so this does not need re-deriving.

  | Field | Observed? | Verdict |
  |---|---|---|
  | `pid`, `sessionId`, `cwd`, `startedAt` | yes | **in use** — identity + the PID-reuse guard |
  | `status`, `statusUpdatedAt` | yes | **in use** — `busy`/`shell`/`idle`/`waiting` |
  | `waitingFor` | yes | **in use** — approval vs question |
  | `kind` | yes | **adopt** — `interactive`/`bg`/`daemon`/`daemon-worker` (`vDy`). Background and daemon sessions currently render identically to real ones. Cheap filter or badge |
  | `messagingSocketPath` (alias `sock`) | **no** | **spike** — see the item above; the one field that could unlock interaction |
  | `updatedAt` | yes | marginal — `statusUpdatedAt` is the useful one |
  | `name`, `nameSource` | yes | skip — a short derived slug (`poweratlas-d2`); the existing session title is better |
  | `entrypoint`, `peerProtocol`, `version` | yes | skip — no product value |
  | `bridgeSessionId` | yes | unknown — links to a Claude Code bridge/web session; no known use yet |
  | `jobId`, `parkedJobId` | no | possible relevance to **Dispatch no-interactive tasks** under Automation & Workflows if background jobs are ever launched from the UI |
  | `procStart`, `state`, `detail`, `tempo`, `logPath` | no | in the reader, in no observed file. Semantics unknown |

  - kiro-cli's lock file is far poorer by comparison — `{pid, started_at}` only, with `cwd`/`title` in the sibling `.json` and **no status field at all**.

